# TODO

このファイルには、実施する方針が決まっている作業を優先順位別に記載します。各項目は、記載された「完了条件」をすべて満たしたときに完了とします。実装が決まっていない案は、通常のTODOにせず末尾の「将来アイデア」で管理します。

## 優先順位: 高

- [ ] **主要操作をブラウザで確認する**
  - 対象: メモの作成・編集・削除、日付移動、キーワード検索、検索画面の複数タグ絞り込み、画像の縮小・保存・再表示、バックアップの書き出し・復元
  - 完了条件: PC幅とスマートフォン幅で対象操作を一通り実行し、データと表示に問題がなく、確認結果を `DEVLOG.md` に記録している

- [ ] **バックアップ読み込み時のデータ検証を強化する**
  - 対象: バックアップのバージョン、必須項目、日付、ID、画像Data URL、タグの検証と、読み込み前の件数・上書き案内
  - 完了条件: 正常なバックアップを復元でき、不正または未対応のデータは既存データを壊さず停止・補正され、その理由が画面に表示される

- [ ] **データ操作のエラー通知を揃える**
  - 対象: IndexedDBの読み込み・保存・削除、バックアップの書き出し、保存容量不足
  - 完了条件: 各処理の失敗を握りつぶさず、利用者が次に取る行動を分かる日本語で画面に表示できる

- [ ] **主要ロジックとデータ操作の自動テストを追加する**
  - 対象: 日付処理、検索、タグ絞り込み、IndexedDBを使う作成・編集・削除、バックアップ復元
  - 完了条件: 代表的な正常系と異常系をテストでき、定めたテストコマンドがすべて成功し、その実行方法が文書化されている

## 優先順位: 中

- [ ] **PWAとオフライン動作を実環境で確認する**
  - 対象: 初回オンライン表示後のオフライン起動、更新時のキャッシュ、ホーム画面への追加、アイコン表示
  - 完了条件: 対応するスマートフォンまたは同等の検証環境で対象項目が動作し、問題と確認環境を `DEVLOG.md` に記録している

- [ ] **GitHub Pagesの本番ビルドと公開フローを確認する**
  - 対象: `npm run build`、GitHub Actions、相対パス、Service Worker、Manifest、アイコンの配信
  - 完了条件: ビルドとデプロイが成功し、公開URLからアプリを開いて主要な静的ファイルがエラーなく取得できる

- [ ] **バックアップを安全に使える導線へ改善する**
  - 対象: 読み込み前の確認、必要に応じた事前バックアップ、同じIDを持つ既存データの扱いの明示
  - 完了条件: 利用者が読み込みによる追加・上書きを事前に理解でき、誤操作時にデータを戻せる手段が用意されている

- [ ] **`App.tsx` に集中している責務を分割する**
  - 対象: 画面部品、データ操作、画像処理、バックアップ処理
  - 完了条件: 動作と見た目を変えずに責務が適切なファイルへ分かれ、型チェック・ビルド・関連テストが成功する

- [ ] **基本的なアクセシビリティを確認・改善する**
  - 対象: キーボード操作、フォーカス移動、スクリーンリーダー向けラベル、色のコントラスト、操作結果の通知
  - 完了条件: 主要操作をキーボードだけで実行でき、検出した重大な問題を修正し、確認結果を `DEVLOG.md` に記録している

## 優先順位: 低

- [ ] **保存済み画像を編集できるようにする**
  - 対象: 編集画面からの画像の差し替えと削除
  - 完了条件: 本文やタグを保持したまま画像を差し替え・削除でき、保存後と再読み込み後に正しく反映される

- [ ] **現在の画像1枚制限を利用者向けに明記する**
  - 対象: 画像選択部分または利用案内への仕様表示
  - 完了条件: 1件のメモに保存できる画像が1枚であることを、添付前に利用者が確認できる

- [ ] **大量データ時の検索処理を改善する**
  - 対象: 現在の全件読み込みとブラウザ上での絞り込み
  - 完了条件: 想定データ件数を決め、その件数でも検索画面の待ち時間とメモリ使用量に実用上の問題がない方法へ変更し、計測結果を記録している

- [ ] **バージョン更新とデータ移行の方針を決める**
  - 対象: IndexedDBスキーマ、バックアップ形式、互換性を壊す変更の手順
  - 完了条件: バージョン番号の更新基準、既存データの移行方法、バックアップ互換性の扱いが文書化されている

## 将来アイデア

以下はすべて実装未決定の「将来候補」です。調査や相談によって採用が決まった場合に、目的・優先順位・完了条件を定めて通常のTODOへ移します。

- **音声入力**: 練習直後の文字入力を減らし、短時間で記録する
- **AIによる定期的な振り返りと要約**: 一定期間のメモから傾向や次の課題をまとめる
- **選手の成長記録**: 時系列で変化、継続、達成を見やすくする
- **画像・動画記録の拡張**: 複数画像、動画、過去記録との比較などを扱う
- **複数端末利用**: スマートフォンとPCなどで同じ記録を安全に共有する
- **検索・絞り込みの拡張**: タグのAND条件、期間指定、並び順などを追加する
- **ホーム画面の簡易フィルター**: 必要性が確認できた場合に「打撃だけ見る」などの短い導線を追加し、検索画面と同じタグ絞り込み部品を再利用する

## 完了した項目

- [x] **本文を主役にしたタグUIへ整理する**（2026-07-11）
  - 完了内容: 通常画面上部の常設タグ絞り込みを削除し、キーワード検索とタグ絞り込みを検索画面へ集約した。入力・編集時の任意タグ、保存済みタグ、IndexedDBとバックアップ形式は維持した
  - 確認結果: 公開中のGitHub PagesでPC幅1,280×800とスマートフォン幅390×844を確認し、横方向のはみ出しやブラウザエラーがないことを確認した
  - 残る確認: 実データを使った作成・編集・削除、タグ検索、画像、バックアップの操作確認は「主要操作をブラウザで確認する」に継続して残す
