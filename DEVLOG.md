# 開発ログ

## これまでに完成している内容

### 基本機能

- React、TypeScript、Viteを使ったアプリの土台
- IndexedDBへの日付別メモ保存と読み込み
- メモの新規作成、一覧表示、本文・タグの編集、削除
- 今日への移動、日付選択、前日・翌日への移動
- メモ件数と絞り込み後の件数表示

### 整理・検索

- 6種類のタグ付け
- 複数タグによる日付内の絞り込み（いずれかのタグに一致）
- 全期間のメモを対象にした検索専用画面
- 本文、タグ、日付文字列を対象にした部分一致検索
- 検索結果から該当日へ移動し、対象メモを一時的に強調表示
- 検索語がない場合の最新20件表示

### 画像

- 画像1枚の添付、送信前プレビュー、取り消し、保存後の表示
- 保存前の画像縮小とJPEG変換による容量削減
- 画像だけのメモ作成

### データ保全

- メモと画像を含むJSONバックアップの書き出し
- JSONバックアップの読み込み
- 過去データに画像やタグがない場合の補完処理

### UI・配布

- PC向けサイドバーとスマートフォン向け三本線メニュー
- 画面幅760px以下、420px以下を考慮したレスポンシブスタイル
- PWA用Manifest、アプリアイコン、Service Worker
- 本番環境でのService Worker登録
- GitHub Actionsによる `main` ブランチからGitHub Pagesへの自動公開設定
- READMEへのローカル実行、公開、バックアップ手順の記載

## 現在確認できる状態

確認日: 2026-07-17

- `README.md`、`package.json`、`src` 配下の全ファイル、PWA関連ファイル、GitHub Actions設定を確認済みです。
- 現在のバージョン表記は `0.1.0` です。
- `npm exec tsc -- --noEmit` によるTypeScript型チェックと `npm run build` はエラーなく完了しています。
- 今回の作業開始時点のGit作業ツリーに未コミット変更はありませんでした。
- `main` の最新コミットは振り返りの期間選択フロー簡略化で、`origin/main` と一致しています。
- コード上では、メモの作成・閲覧・検索・編集・削除、タグ絞り込み、画像保存、自由期間の「記録を振り返る」画面、バックアップ入出力、データ操作失敗時の案内まで実装されています。
- データは端末内のブラウザにのみ保存され、バックエンドAPI、ログイン、クラウド同期はありません。
- 自動テスト用の設定やテストファイルはありません。
- 実画像と実JSONを使う主要操作、不正なJSON読込時のエラー表示、既存メモの保持、通知を閉じる操作は利用者の実操作で確認済みです。GitHub Pagesへの最新変更の実デプロイ確認は実施していません。
- サイドメニューはPC・スマートフォンとも「振り返り」「検索」「バックアップ」「読み込み」の4項目で、Lucideの線画アイコン、文字サイズ、行間、余白、アイコンと文字の間隔を統一しています。上部には「ホームベース×B」の塗り仮ロゴを表示しています。

## 日付別の開発記録

新しい作業は、この章の先頭に新しい日付の記録を追加します。同じ日に複数回作業した場合は、時刻または短い作業名を見出しに加えます。未実施の内容を「実施内容」や「確認結果」に書かず、次回へ持ち越す内容は「残っている問題」または「次回やること」に記載します。

### 2026-07-17: ロゴのGitHub Pagesサブパス対応

#### 実施内容

- 公開中のGitHub Pagesでロゴが壊れた画像になる原因を確認しました。`/baseball-note-logo.svg` はドメイン直下の `https://seista-jp.github.io/baseball-note-logo.svg` へ解決され、GitHub Pagesの404になっていました。
- `public/baseball-note-logo.svg` が小文字・ハイフンを含めて指定名と完全一致し、SVGファイルとして存在することを確認しました。
- `vite.config.js` の `base: "./"` に合わせ、ロゴの読み込みを `${import.meta.env.BASE_URL}baseball-note-logo.svg` へ変更しました。
- PWAアイコン、Manifest、favicon、Service Worker、ロゴの寸法とレイアウトは変更していません。

#### 変更したファイル

- `src/App.tsx`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- ビルド済みJavaScriptではロゴURLが `./baseball-note-logo.svg` になり、`public` のSVGと同一内容の `dist/baseball-note-logo.svg` が生成されることを確認しました。
- ローカル開発環境 `http://127.0.0.1:5173/` では `http://127.0.0.1:5173/baseball-note-logo.svg` から読み込まれ、28×28pxで表示されました。
- GitHub Pages相当のサブパス `http://127.0.0.1:4173/baseball-note/` では `http://127.0.0.1:4173/baseball-note/baseball-note-logo.svg` から読み込まれ、28×28pxで表示されました。
- ローカル開発環境とサブパス環境のブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 修正版は未コミット・未プッシュのため、実際のGitHub Pagesでの修正後表示はまだ確認できません。コミット・PushとGitHub Actionsのデプロイ後に最終確認が必要です。

#### 次回やること

- 利用者のローカル確認後、指示を受けてコミット・Pushします。
- デプロイ後、公開URLが `https://seista-jp.github.io/baseball-note/baseball-note-logo.svg` へ解決され、ロゴが表示されることを確認します。

### 2026-07-17: オリジナルロゴをサイドメニューへ反映

#### 実施内容

- PCサイドバーとスマートフォンのドロワーで共用しているブランド表示を、CSS製の「ホームベース×B」仮ロゴから `public/baseball-note-logo.svg` へ置き換えました。
- SVGは `/baseball-note-logo.svg` から読み込み、装飾画像として空の代替テキストを設定しました。
- ロゴを28×28px、文字との間隔を既存の10px、縦中央揃えとし、ロゴが縮まない既存レイアウトを維持しました。
- `public/icon.svg`、`public/icon-192.png`、`public/icon-512.png`、`public/manifest.webmanifest`、`index.html` のfavicon指定は変更していません。

#### 変更したファイル

- `public/baseball-note-logo.svg`（利用者が配置済み）
- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- PC幅1,280×800pxで、ロゴ画像が `/baseball-note-logo.svg` から読み込まれ、28×28px、文字との間隔10px、縦中央のずれ0pxで表示されることを確認しました。既存のサイドバー幅260pxも維持しています。
- スマートフォン幅390×844pxと320×844pxでドロワーを開き、ロゴが28×28pxのまま縮まず、文字、閉じるボタン、4つのメニュー項目と自然に収まることを確認しました。
- 3つの確認幅すべてで画面の `clientWidth` と `scrollWidth` が一致し、横方向のはみ出しがないことを確認しました。
- PC・スマートフォンの確認中にブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- 実スマートフォンのタッチ操作、OSの文字拡大、画面回転は利用者環境での確認が必要です。
- PWAアイコンとfaviconへのロゴ反映は今回の対象外です。ロゴ表示の確認後、必要な場合に別作業として検討します。

#### 次回やること

- 利用者のローカル画面でロゴ表示を確認し、PWAアイコンも統一するか相談します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-17: サイドメニューへLucide Iconsと仮ロゴを追加

#### 実施内容

- `lucide-react` を依存関係へ追加し、サイドメニューの「振り返り」に `CalendarDays`、「検索」に `Search`、「バックアップ」に `Download`、「読み込み」に `Upload` を表示しました。
- 4アイコンを21px、`strokeWidth` 1.8へ統一しました。メニュー4項目も高さ48px、文字15px、行高21px、内側余白12px、アイコンと文字の間隔12pxへ統一しました。
- 選択中とホバー時にだけ薄い背景を表示し、キーボード操作用のフォーカス表示を追加しました。
- Baseball Noteの左へ、28pxのホームベース形状に白いBを置いた塗り仮ロゴをCSSで追加しました。小さい表示で形と文字を保つため、画像や線画ではなく単色のシルエットにしています。
- 振り返り、検索、バックアップ、読み込みの既存イベント処理、期間指定、IndexedDB、バックアップ形式、PCの画面構成には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `package.json`
- `package-lock.json`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- PC幅1,280×800pxで、サイドバー幅260pxと既存画面構成を維持し、4つのSVGがLucideの `calendar-days`、`search`、`download`、`upload` であることを確認しました。全項目のアイコン、文字、余白、行高、間隔も同じ値になっています。
- スマートフォン幅390×844pxと320×844pxでドロワーを開き、ロゴと4項目が自然に収まること、項目の左位置と高さが一致すること、横方向のはみ出しがないことを確認しました。
- 28×28pxの仮ロゴでホームベースの輪郭と中央のBを目視できることを、PCとスマートフォンの両方で確認しました。
- PC・スマートフォンで「振り返り」から既存の期間選択カレンダーが開くこと、スマートフォンではドロワーが閉じることを確認しました。検索画面の表示、スマートフォンで検索時にドロワーが閉じることも確認しました。
- バックアップで「18件を書き出しました」と表示されること、読み込み用の非表示ファイル入力が従来どおりJSONだけを受け付けることを確認しました。
- PC・スマートフォンの確認中にブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- 実スマートフォンのタッチ操作、OSの文字拡大、画面回転は未確認です。
- バックアップは完了表示まで確認しましたが、今回生成されたファイルそのものの再読み込みは行っていません。書き出し・読み込み処理には変更を加えていません。
- 今回のロゴは仮デザインです。正式な形状、色、文字バランスのブラッシュアップは今後の作業として残します。

#### 次回やること

- 利用者の普段使うスマートフォンとPCで、アイコンと仮ロゴの大きさ、余白、タップ時の印象を最終確認します。
- 仮ロゴの改善方針が決まったら、現在の認識しやすさを維持して正式版へ更新します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-17: サイドメニューを文字中心の4項目へ整理

#### 実施内容

- PCサイドバーとスマートフォンのドロワーメニューから「今日」と日付入力を削除しました。
- 上部のBマークと、各メニュー項目の記号・文字アイコンを削除し、「振り返り」「検索」「バックアップ」「読み込み」の4項目を文字だけで表示するようにしました。「Baseball Note」とスマートフォン用の「閉じる」は維持しています。
- メニューの「記録を振り返る」を「振り返り」へ変更し、既存の `showRecordReview` をそのまま使用して期間選択カレンダーを開く構造を維持しました。
- 4項目を高さ48px、文字15px、行間1.5、上下10px・左右12pxの余白へ統一しました。選択中の項目はカード状の背景や枠を付けず、文字の太さだけで示すようにしました。
- 検索、バックアップ書き出し、JSON読み込みのイベント処理と非表示ファイル入力、振り返りの期間選択、IndexedDB、バックアップ形式には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- PC幅1,280×800pxで、メニューが4項目だけであること、全アイコンと「今日」「日付」がないこと、各項目の左位置・高さ・文字サイズ・行間・余白が一致すること、選択中項目にカード状の背景・枠・影がないことを確認しました。
- スマートフォン幅390×844pxと320×844pxで、Baseball Noteと「閉じる」が残っていること、4項目の位置と寸法が揃っていること、画面全体に横スクロールがないことを確認しました。
- 「振り返り」を押すとPC・スマートフォンとも既存の期間選択カレンダーが開き、スマートフォンではドロワーが閉じることを確認しました。
- 2026年7月10日を開始日・終了日として同じ日を2回選び、その1日だけの期間表示になることを確認しました。2026年7月9日〜7月12日の複数日指定と期間表示も確認しました。
- 検索画面を開き、実データを「右足」で検索して1件へ絞り込まれることを確認しました。検索を閉じると今日の日別画面へ戻ることも確認しました。
- バックアップを押して「1件を書き出しました」と表示されること、読み込みを押せること、JSONだけを受け付ける既存ファイル入力が維持されていることを確認しました。
- PC・スマートフォンの確認中に横方向のはみ出し、ブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- ブラウザ自動操作環境ではバックアップのダウンロードイベントを取得できなかったため、今回生成されたファイルそのものの再読込は行っていません。実JSONの書き出し・読み込みは2026-07-13に利用者操作で確認済みで、今回は関連処理を変更していません。
- 実端末のタッチ操作、OSの文字拡大、画面回転は今回も未確認です。

#### 次回やること

- 利用者のローカル画面で、普段使うスマートフォンとPCのメニュー表示・操作感を最終確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-16: 振り返りの期間選択を1操作短縮

#### 実施内容

- メニューの「記録を振り返る」を押した時点では画面を切り替えず、現在の画面上へ期間選択カレンダーを直接開くようにしました。
- 「この期間を表示」で期間を確定した場合だけ、選択期間を保存して振り返り一覧へ移動するようにしました。
- カレンダーをキャンセルした場合は画面状態を変更しないため、追加の復帰用stateを持たずに直前の日別画面、検索画面、振り返り一覧へ戻る構造にしました。
- 日別画面に未保存の編集がある場合も、カレンダーを開く時点では編集状態を保持し、期間を確定して実際に移動する時点でだけ既存の未保存確認を行うようにしました。移動を取りやめた場合はカレンダーを閉じて編集画面へ戻ります。
- 振り返り一覧上部の「期間を選ぶ」を「期間を変更」へ変更しました。記録一覧のDOM、レイアウト、CSS、データ取得、カレンダー本体には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- PC表示で、日別画面のメニューからカレンダーが直接開き、背面が日別画面のままであること、キャンセル後も同じ日別画面が表示されることを確認しました。
- 期間確定後だけ振り返り一覧へ移動し、上部に「期間を変更」が表示され、既存の記録項目と日付グループが維持されることを確認しました。
- 「期間を変更」から開いたカレンダーをキャンセルすると、同じ期間と記録一覧が維持されることを確認しました。
- 検索画面からメニューでカレンダーを開き、キャンセル後も検索画面へ戻ることを確認しました。
- 日別画面で本文を未保存のままカレンダーを開いてキャンセルし、編集本文と保存バーが維持されることを確認しました。確認用の変更は保存せずキャンセルし、既存記録を元の状態に戻しました。
- スマートフォン幅390×844pxで、ドロワーメニューの「記録を振り返る」を押すとドロワーが閉じてカレンダーが直接開き、キャンセル後は元の検索画面へ戻ることを確認しました。画面の横スクロールとブラウザコンソールの警告・エラーはありませんでした。
- 自動テストコマンドとテストファイルは既存構成にないため、今回は型チェック、ビルド、ブラウザ操作で確認しました。

#### 残っている問題

- なし

#### 次回やること

- 利用者のローカル画面で、普段の操作感として1操作短縮されていることを確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-16: 週間記録を自由期間の「記録を振り返る」へ変更

#### 実施内容

- メニューと画面名を「記録を振り返る」へ変更し、「今週」「前の週」「次の週」などの週単位の操作と表示を削除しました。
- 開始日を選んでから終了日を選ぶ期間選択カレンダーを追加しました。月の前後移動、未来日の無効化、選択途中の案内、開始日・終了日の強調、期間内の日付の色付き表示、同日選択、開始日より前を押した場合の選び直しに対応しました。
- 期間の確定は「この期間を表示」だけで行い、「キャンセル」とカレンダー外側の操作では確定しない構造にしました。
- 初回は期間を自動選択せず、「期間を選んで、記録を振り返りましょう」と表示します。確定した期間は `baseball-note-record-review-range` をキーとして `localStorage` に保存し、再表示時に復元します。
- Dexieの既存 `date` インデックスへ、開始日と終了日の両方を含む範囲検索を行うよう変更しました。保存済みの `YYYY-MM-DD` の日付キー同士を比較するため、時刻やUTC変換を介さずローカル日付の両端を保持します。
- 記録の日付順・同日内の作成時刻順、日付ごとのグループ、通常一覧と同じ2列構成、画像サムネイル、記録から日別画面への移動を維持しました。
- 週の開始日計算、週範囲表示、前週・次週移動、週間専用stateとCSSを削除し、振り返り用途の名前へ整理しました。IndexedDBスキーマ、メモ型、画像形式、バックアップ形式、新しい依存関係は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/RecordReviewCalendar.tsx`（新規）
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- 既存の自動テストコマンドとテストファイルはありません。代わりに日付処理を一時コンパイルして、日・月・年の繰り上がり、実在日付判定、月カレンダー、両端を含む比較の10ケースを確認しました。
- PC幅1,280×800px、スマートフォン幅390×844pxと320×844pxで、振り返り画面とカレンダーに横スクロールがなく、期間選択ボタン44px、カレンダー日付42px以上、確定ボタン48pxの操作領域があることを確認しました。
- 初回案内、開始日・終了日の自由選択、同日だけの選択、月またぎ、2025年12月31日〜2026年1月1日の年またぎ、開始日より前を押した選び直し、未来日と未来月の無効化、キャンセル、カレンダー外側で確定されないことを確認しました。
- 期間の確定後に画面を再読み込みし、前回の期間が復元されることを確認しました。記録がない2026年6月20日だけを選び、「この期間の記録はありません」と「期間を変更する」が表示されることも確認しました。
- 2026年7月6日と7月16日にテスト記録を作り、7月6日〜7月16日を選ぶと開始日・終了日の両方が日付順で表示されることを確認しました。通常画面で新規作成、本文の直接編集、削除も確認し、作成した2件は削除して既存記録を保持しました。
- ブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 今回は新しい実画像ファイルの選択操作を再実施していません。振り返り画面は既存の `ImagePreview` と保存画像をそのまま再利用し、表示用クラス名と最大サムネイルサイズだけを維持する構造です。実画像付き記録の保存・再表示自体は2026-07-13に確認済みですが、変更後の画面での最終目視は利用者環境でも確認します。
- 実端末のタッチ操作、OSの文字拡大、画面回転は未確認です。

#### 次回やること

- 利用者のローカル画面で、実画像付き記録、実スマートフォンのタップ操作、PC表示の最終的な余白を確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-15: 週間記録を通常一覧に合わせてコンパクト化

#### 実施内容

- 週間記録の各メモを、通常一覧と同じ左列に時刻とタグ、右列に本文と画像を置く2列構成へ変更しました。
- 通常一覧の `log-entry`、`log-meta`、`log-content`、`log-body` を週間記録でも再利用し、PCの左列72px・列間14px、スマートフォンの左列62px・列間10px、本文幅、文字サイズ、行間、上下余白を揃えました。
- 週間記録のカード用の四辺の枠線、角丸、白背景を廃止し、各メモの下に薄い区切り線だけを表示するようにしました。日付見出しは維持し、日付グループ間の余白を縮めました。
- 画像表示部品に週間記録用の表示バリエーションを追加し、縦横比を維持したままPCでは最大160×100px、スマートフォンでは最大120×80pxのサムネイルとして表示するようにしました。通常一覧の画像サイズは変更していません。
- 記録全体を押して日別画面へ移動する操作、キーボードのフォーカス表示、週移動、日付指定、並び順、データ取得、IndexedDB、バックアップ形式は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- コード確認で、週間記録が通常一覧のレイアウト用クラスを再利用し、画像だけ週間用の最大サイズが適用される構造になっていることを確認しました。

#### 残っている問題

- PC幅とスマートフォン幅での最終的な見た目、画像サムネイルの大きさ、記録全体を押した日別遷移は、利用者のローカル画面での確認が必要です。

#### 次回やること

- 利用者のローカル画面確認結果に応じて余白や画像サイズを調整します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-13: メモ一覧の直接編集と時刻下タグ表示

#### 実施内容

- 日別メモ一覧を、左列に時刻と保存済みタグ、右列に本文と画像を置く2列構成へ変更しました。通常表示の本文下にはタグ行を作らず、編集・削除アイコンも表示しません。
- 本文を押すと同じ位置へ自動伸長する `textarea` を表示し、タップした文字位置へカーソルを移すようにしました。標準の `caretPositionFromPoint` とSafari向けの `caretRangeFromPoint` を使い、新しいライブラリは追加していません。
- 本文はEnterで改行でき、メモの編集開始はEnterまたはSpace、編集終了はEscape、変更の保存はCtrl/Cmd+Enterでも操作できるようにしました。一度に編集できるメモは1件です。
- 本文またはタグが実際に変わった場合だけ、既存の新規入力欄の直上へ「キャンセル」「保存」の保存バーを表示するようにしました。保存失敗時は既存のエラー案内を使い、編集内容と編集状態を残します。
- 元々本文があるメモを空にして保存した場合は「本文が空です。このメモを削除しますか？」と確認し、了承された場合だけ既存のDexie削除処理を使うようにしました。画像だけのメモを含め、編集状態から削除できるテキスト操作も残しました。
- 未保存変更がある状態で、日付移動、検索・週間記録への移動、別メモの編集、バックアップ読み込みを行う場合だけ確認するようにしました。メニューを開閉するだけでは確認せず、編集内容を保持します。ブラウザ終了・再読み込みには `beforeunload` の標準警告を登録しました。
- 初回だけ「メモをタップすると編集できます」と表示し、閉じた状態または本文を編集したことを `localStorage` へ保存するようにしました。この設定はIndexedDBとJSONバックアップには含まれません。
- スマートフォンでは可視領域に追従する `100dvh` の画面構成にし、保存バーを新規入力欄の上へ独立した行として配置しました。320px幅で編集用タグが横にはみ出さないよう、編集時だけ3列2行にしました。
- `LogEntry`、IndexedDBスキーマ、JSONバックアップ形式、画像形式、検索条件、週間記録、PWA、公開設定、バージョン番号は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- PC幅1,280×800px: 時刻と本文先頭の上端が一致し、左72pxの時刻・タグ列と右680pxの本文列が重ならないこと、通常時の編集・削除アイコンが0件であること、長文・改行・タグ6件・区切り線・新規入力欄を確認しました。
- スマートフォン幅390×844px: 左62pxの時刻・タグ列と本文286pxが重ならず、長文が自然に折り返すこと、画面全体の横スクロールがないことを確認しました。
- スマートフォン幅320×844px: 本文216px、編集欄、3列2行の編集タグ、削除操作、保存バー、新規入力欄が画面内に収まり、一覧の `clientWidth` と `scrollWidth` がともに320pxであることを確認しました。
- 本文を押した文字位置へカーソルが移り、その位置で日本語を追記できること、改行、長文に応じた編集欄の伸長、Enterキーでの編集開始、Escapeキーでの編集終了を確認しました。
- 本文を押しただけでは保存バーがなく、文字変更後だけ表示されること、保存後に本文へ反映されること、キャンセル後に変更前の本文へ戻ることを確認しました。
- 本文を空にして保存した場合の削除確認について、確認のキャンセルでは空の編集内容とメモが残り、確認の了承では削除されて件数が更新されることを確認しました。
- 編集状態の削除操作についても、削除確認のキャンセルではメモが残り、了承した場合だけ削除されることを確認しました。
- 未保存のまま検索へ移動する確認を表示し、キャンセルでは編集内容を保持、了承では変更を保存せず検索画面へ移動することを確認しました。メニューの開閉だけでは未保存内容が残ることも確認しました。
- 初回案内を閉じて再読み込みした後に再表示されないことを確認しました。
- 既存機能の回帰確認として、新規メモ入力、キーワード検索、週間記録、バックアップ書き出し完了表示を確認しました。ブラウザ操作環境ではダウンロードイベントを取得できませんでしたが、アプリ上では対象1件の書き出し完了が表示されました。
- ブラウザコンソールの警告・エラーはありませんでした。
- コード確認: IndexedDB保存・削除の失敗時は編集内容または対象メモを残して既存のエラー通知を表示すること、画像表示、バックアップ読込、エラー通知、PWA関連の処理を変更していないことを確認しました。
- 画面確認で作成した当日分のテストメモはすべて削除し、確認前の0件へ戻しました。

#### 残っている問題

- 実スマートフォンでソフトウェアキーボードを表示した状態の保存バー操作は未確認です。`100dvh` と一覧内スクロールで追従する構造は実ブラウザの指定幅で確認しています。
- ブラウザ操作環境の再読み込みAPIはページの `beforeunload` 確認を表示しなかったため、実ブラウザを閉じる・再読み込みする際の標準警告は利用者環境での確認が必要です。
- IndexedDBの保存・削除失敗は実ブラウザでは発生させていません。失敗時に編集内容とメモを残す分岐はコードで確認しています。

#### 次回やること

- 利用者のスマートフォンで、日本語入力中の改行、保存バー、実ブラウザの再読み込み警告を確認します。
- 利用者の実ブラウザ確認が完了するまではコミットとPushを行いません。

### 2026-07-13: スマートフォンのメモ一覧をコンパクト化

#### 実施内容

- 日別メモ一覧の本文・画像の下を、左側に保存済みタグ、右側に編集・削除操作を置く1行構成へ変更しました。
- 保存済みタグ側は折り返し可能、操作側は縮まない構造にし、タグが多い場合も操作ボタンと重ならないようにしました。
- 通常表示時の編集を鉛筆、削除をゴミ箱のインラインSVGアイコンへ変更しました。新しいライブラリは追加していません。
- アイコンを17px、各ボタンの押せる範囲を44×44pxとし、2ボタン間に6px、タグ側との間に12pxの間隔を設けました。
- ボタン要素、時刻を含む編集・削除の `aria-label`、キーボード操作時のフォーカス表示を維持しました。
- 本文からタグ・操作行までの間隔を8pxにし、メモ間の間隔と下余白を狭めました。メモ同士は薄い区切り線で判別できるようにしました。
- 編集・削除処理、削除確認、保存形式、IndexedDB、バックアップ、検索、週間記録、画像、エラー通知、ヘッダー、入力欄は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- スマートフォン幅390×844px: 短いメモ、長いメモ、タグ1件、タグ3件、タグ6件を表示し、横方向のはみ出しがないことを確認しました。
- スマートフォン幅320×844px: 横方向のはみ出しがなく、タグ右端と操作部の間に12pxの間隔があり、操作部が押しつぶされないことを確認しました。
- タグ6件は390px幅で2行に折り返し、編集・削除操作と重ならないことを確認しました。
- PC幅1,280×800px: PC向けサイドバーと入力欄を維持し、本文、タグ、操作部に横方向のはみ出しや不自然な重なりがないことを確認しました。
- 各編集・削除ボタンの実測値は44×44px、SVGアイコンは17×17pxでした。
- 鉛筆アイコンから編集画面を開き、タグを追加して保存できることを確認しました。
- ゴミ箱アイコンからテストメモを削除し、件数が2件から1件、0件へ更新されることを確認しました。ブラウザ操作環境では確認ダイアログが自動処理されたため文言の目視確認はできませんでしたが、既存の `window.confirm` 処理を変更していないことをコードで確認しました。
- 画面確認後、作成したテストメモはすべて削除し、確認前の0件へ戻しました。
- ブラウザコンソールの警告・エラーはありませんでした。
- 差分確認: データ操作とエラー通知の処理に変更がなく、通常表示時のDOMとメモ一覧用CSSだけが変更されていることを確認しました。

#### 残っている問題

- なし

#### 次回やること

- 高優先TODOの「主要ロジックとデータ操作の自動テストを追加する」へ進みます。

### 2026-07-13: データ操作のエラー通知を統一

#### 実施内容

- 実画像付きメモの保存、日別・週間表示、アプリを開き直した後の画像保持、実JSONの書き出し・読み込み、同じデータの上書き案内、画像とタグを含む復元が、利用者の実操作ですべて問題なく動作したことを引き継ぎました。
- 日別画面、検索画面、週間記録のIndexedDB読み込みを例外処理で囲み、失敗時は空の記録と誤認させず、再読み込みや画面の開き直しを案内するようにしました。
- メモの新規保存と編集に失敗した場合は、入力・編集内容が残っていることを案内するようにしました。
- 保存容量不足を通常の保存失敗と区別し、先にバックアップを書き出してから不要な画像付きメモを整理する案内を追加しました。
- 削除に失敗した場合はメモを画面から消さず、「メモは残っています」と表示するようにしました。
- バックアップの書き出し・読み込みに失敗した場合は、スマートフォンでメニューを閉じた後でも確認できる共通エラー通知を表示するようにしました。
- 共通エラー通知には閉じる操作と `role="alert"` を追加しました。通知は失敗した場合だけ描画され、正常時の画面構成は変更していません。
- IndexedDBスキーマ、保存データ、バックアップ形式、画像処理、検索条件、週間記録の仕様は変更していません。新しいライブラリも追加していません。

#### 変更したファイル

- `src/App.tsx`
- `src/dataError.ts`（新規）
- `src/styles.css`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- 容量不足判定: Dexieを想定した入れ子の `QuotaExceededError`、通常エラーの誤検出防止、容量不足の案内文、通常時の代替文言の4ケースが成功しました。
- コード確認: 日別・検索・週間の読み込みでは、失敗時にもローディング表示を終了し、各画面内に日本語の案内が表示される構造を確認しました。
- コード確認: 削除はIndexedDBから削除できた場合だけ画面から除去し、失敗時は対象メモを残す構造を確認しました。
- コード確認: 共通通知と読み込みエラー表示は、エラー文言がある場合だけ描画されるため、正常時の画面構成に新しい要素は追加されません。
- レスポンシブ確認: エラー通知はPCではサイドバーを避け、760px以下ではスマートフォンヘッダーの下へ表示するCSSになっていることを確認しました。
- 本番ビルド済み画面をPC幅・スマートフォン幅で開くためローカルプレビューを起動しましたが、実行環境のローカルポート待受制限で起動できませんでした。ビルド済みファイルの直接表示もブラウザの安全制限で拒否されたため、変更後の実画面確認は未実施です。

#### 残っている問題

- 変更後のエラー通知について、PC幅とスマートフォン幅での実表示、長い案内文の折り返し、閉じる操作の実画面確認が残っています。
- IndexedDBや保存容量の実際の失敗をブラウザ上で発生させた確認は未実施です。

#### 次回やること

- ローカル画面を開ける環境で、PC幅とスマートフォン幅のエラー通知を確認します。
- 高優先TODOの「主要ロジックとデータ操作の自動テストを追加する」へ進みます。

### 2026-07-11: 週間記録画面の追加

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` と現在のコードを確認し、既存の設計、未コミット変更、開発ルールを維持して実装しました。
- 三本線メニューとPCサイドバーへ「週間記録」を追加しました。
- 月曜日から日曜日までを1週間とし、Dexieの既存 `date` インデックスから対象期間の記録だけを読み込む週間記録画面を追加しました。
- 記録を日付の古い順、同じ日では作成時刻の古い順に並べ、日付ごとにタグ、本文全文、保存画像を表示する構造にしました。
- 前の週・次の週への移動、今週より未来への移動防止、期間表示からの日付指定に対応しました。
- 週間記録のメモを押すと、編集画面ではなく対象日の日別画面へ移動し、対象メモを一時的に強調する既存処理を再利用しました。
- 記録がない週には「この週の記録はありません」と表示します。
- バックアップ読込を週間画面で行った場合も、対象週の一覧を即時更新するようにしました。
- AI、外部API、新しい要約処理、新しい保存データは追加していません。IndexedDBスキーマとバックアップ形式も変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで34モジュールを変換）
- 週計算: 土曜・日曜・月曜、月をまたぐ日付、期間表示、曜日見出しの6ケースが想定どおりになりました。
- PC幅: サイドバーの「週間記録」から今週を開き、`7/6〜7/12`、月曜日の日付見出し、保存済み本文、無効な次週ボタンを確認しました。
- スマートフォン幅390px: 三本線メニューから週間記録を開き、専用画面の表示、無効な次週ボタン、入力欄が表示されないこと、横方向のはみ出しがないことを確認しました。
- スマートフォン幅320px: 週移動3列、本文カード、画面全体に横方向のはみ出しがないことを確認しました。
- 週移動: 前週へ移動すると `6/29〜7/5` と空週案内が表示され、次週ボタンで戻れる状態になることを確認しました。
- 日付指定: 2026-06-18を指定すると、その日を含む月曜日から日曜日の `6/15〜6/21` が表示されました。
- 日付順・複数件・タグ: 7月6日の既存記録と7月11日のテスト記録2件が古い日付順に並び、同日の2件と「打撃」「守備」タグがすべて表示されました。
- 日別遷移: 7月6日の週間メモを押すと `2026/07/06` の日別画面が開きました。
- 既存操作: 日別画面でテスト記録2件の作成・保存・削除を行い、確認後に削除しました。
- 画像表示: 既存の `ImagePreview` を週間カード内でも使用し、画像幅をカードと画面幅以内に制限する既存CSSが適用される構造を確認しました。
- ブラウザコンソールエラーはありませんでした。

#### 残っている問題

- ブラウザ操作環境でローカル画像ファイルを選択できないため、実画像を保存した記録の週間画面表示は実操作で未確認です。既存の画像表示部品を変更せず再利用しています。

#### 次回やること

- ローカルファイル選択を操作できる環境で、画像の保存・日別表示・週間表示を一連で確認します。
- 高優先TODOのデータ操作エラー通知または自動テスト追加へ進みます。

### 2026-07-11: バックアップ読込検証の強化と画面回帰確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を確認し、仕様、前回作業、残件、開発ルールを引き継ぎました。
- バックアップのバージョン、必須項目、実在日付、ID、画像Data URL、タグ、メモID重複を、IndexedDBへの書き込み前に検証する処理を追加しました。
- 過去データで画像またはタグが省略されている場合は、従来どおり空配列へ補完します。
- 読込前に新規件数と上書き件数を表示し、利用者が確認した場合だけ読み込むようにしました。
- 画像変換を含む全データの準備後、Dexieトランザクション内で一括反映するようにしました。
- 不正ファイル、未対応バージョン、利用者による中止では、理由と既存データを変更していないことを画面に表示します。

#### 変更したファイル

- `src/backup.ts`（新規）
- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで34モジュールを変換）
- 検証ロジック: 正常なバージョン1、画像・タグを省略した旧形式、未対応バージョン、不正日付、不正ID、不正画像Data URL、不正タグ、重複IDの8ケースが想定どおり成功または停止しました。
- PC幅1,280×800: サイドバーと通常ヘッダーが表示され、横スクロールがないことを確認しました。
- スマートフォン幅320・390・420・760px: 1行ヘッダーが表示され、横スクロールがないことを確認しました。390×844では実画面も目視確認しました。
- ブラウザ操作: テストメモの作成、タグ付与、本文・タグ編集、キーワード検索、削除を確認し、作成したテストメモは削除しました。
- バックアップ書き出し: 画面に対象件数の完了表示が出ることを確認しました。ブラウザ操作環境ではダウンロードイベントを取得できなかったため、保存ファイルそのものは未確認です。
- ブラウザコンソールエラーはありませんでした。

#### 残っている問題

- ブラウザ操作環境でローカルファイルを選択できないため、画像の縮小・保存・再表示と、正常・不正なJSONファイルを選択した読込画面の一連の操作は未確認です。
- 上記が残るため、`TODO.md` の「主要操作をブラウザで確認する」と「バックアップ読み込み時のデータ検証を強化する」は未完了のままです。

#### 次回やること

- ローカルファイル選択を操作できる環境で画像とバックアップ復元を確認します。
- バックアップ読込で新規・上書き件数、確認の中止、検証エラー理由、正常復元を実画面で確認し、問題がなければ該当TODOを完了にします。

### 2026-07-11: スマートフォン上部UIの最終1行化

#### 実施内容

- スマートフォンヘッダーからBアイコンを外し、メニューボタン、Baseball Note、前日、短縮日付、今日表示、翌日の順に1行で配置しました。
- 「今日」は選択日が今日の場合だけ表示し、過去日では表示しないようにしました。
- 検索画面では日付操作全体を描画せず、メニューボタンとBaseball Noteだけを表示する構造にしました。
- 760px以下では従来の `.topbar` を非表示にし、区切り線、件数、日付操作、余白を画面から除きました。
- スマートフォン用の「今日のログ／過去のログ」は、画面には表示されないスクリーンリーダー向け `h1` として残しました。
- 前後移動には既存の `moveSelectedDate` を使用し、今日の翌日ボタンを無効のまま維持しました。
- PC表示、メモ一覧、下部入力欄、データ・検索・編集・削除・タグ・画像・バックアップ処理は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）
- コード確認: 今日では `M/D 今日` と無効な翌日ボタン、過去日では `M/D` と有効な翌日ボタンになる条件を確認しました。
- コード確認: 検索画面では日付操作をレンダリングせず、通常画面へ戻ると選択日に応じた日付操作が再表示される構造を確認しました。
- コード確認: 0件・複数件にかかわらずスマートフォンの件数表示はなく、メモ一覧とデータ処理のコードに変更がないことを確認しました。
- 幅確認: 760px以下は44px、可変幅、固定幅の3列です。420px以下と340px以下で余白を段階的に狭め、タイトルは18pxのまま省略可能、日付操作と40pxの前後ボタンは縮まない指定にしました。
- PC確認: スマートフォン専用要素は非表示で、従来の `.topbar`、年月日、件数、36pxの前後ボタンを維持する構造を確認しました。
- 実ブラウザ確認: 実行環境のローカルサーバー待受制限とローカルファイル表示制限により実施できませんでした。

#### 残っている問題

- 320px、390px、420px、760px、PC幅での実画面表示、横スクロール、タップ操作の確認が残っています。
- 今日、過去日、0件、複数件、検索画面、検索からの復帰、前日・翌日移動の実ブラウザ回帰確認が残っています。

#### 次回やること

- ローカル画面を開ける環境で指定幅と状態を実確認します。
- 問題があればスマートフォン上部UIのCSSだけを必要最小限修正します。

### 2026-07-11: スマートフォン上部UIのコンパクト化

#### 実施内容

- 760px以下のスマートフォンヘッダーへ、選択日を `M/D` 形式で表示する短縮日付を追加しました。
- 検索画面では短縮日付を表示しないようにしました。
- スマートフォンの日付操作を、前日ボタン、ログ見出し、翌日ボタン、件数の1行表示にしました。
- スマートフォンでは年月日の大きな表示を隠し、「今日のログ」または「過去のログ」を `h1` 内の見出しとして表示するようにしました。
- 今日の翌日ボタンを無効化し、日付入力の最大値を今日に制限しました。
- PC向けの年月日表示と配置、メモ一覧、編集・削除、下部入力欄は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）
- 差分確認: `selectedDate`、`moveSelectedDate`、日付別のIndexedDB読み込み、保存データ型、編集・削除・タグ・画像・検索処理を変更していないことを確認しました。
- コード確認: 760px以下だけでコンパクト表示が有効になり、PCでは従来の年月日見出しが表示される構造を確認しました。
- コード確認: 320px幅では左右12pxの余白、40pxの前後ボタン、縮小可能な中央見出し、固定幅の件数表示となり、ヘッダーは44px、可変幅ブランド、固定幅短縮日付の3列となる構造を確認しました。
- 実ブラウザ確認: 実行環境でローカルサーバーのポート待受が許可されず、ビルド済みファイルの直接表示もブラウザの安全制限で拒否されたため実施できませんでした。

#### 残っている問題

- 320px、390px、420px、760px、PC幅での実表示と横スクロール、タップ領域の実画面確認が残っています。
- 今日、過去日、0件、複数件、検索からの復帰、前後移動、編集、削除、タグ、画像、日付入力のブラウザ回帰確認が残っています。
- 過去に未来日へ保存したデータや、未来日を含むバックアップを検索結果から開く場合の扱いは、既存データを隠さないため今回変更していません。通常の日付入力と前後ボタンから未来日へ進むことはできません。

#### 次回やること

- ローカル画面を開ける環境で指定された画面幅と操作状態を確認します。
- 表示上の問題が見つかった場合は、上部UIのCSSだけを必要最小限修正します。

### 2026-07-11: 引き継ぎと主要操作の追加確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を確認し、現在の仕様、前回までの作業、残件、開発ルールを引き継ぎました。
- ローカル開発画面でテスト用メモを作成し、編集、日付移動、検索、タグ絞り込み、削除を確認しました。
- 確認後にテスト用メモを削除し、作業前のデータ状態へ戻しました。

#### 変更したファイル

- `DEVLOG.md`

#### 確認結果

- PC幅1,280×800: 本文と「打撃」タグを付けたメモを作成でき、一覧へ本文とタグが表示されました。
- スマートフォン幅390×844: メモ本文の編集、「守備」タグの追加、前日・今日への移動、ドロワーメニューからの検索画面表示を確認しました。
- 検索: キーワードで対象メモが1件に絞られ、該当しない「投球」タグを追加すると0件表示になることを確認しました。
- 削除: テスト用メモを削除し、当日の件数が0件へ戻ることを確認しました。
- ブラウザコンソールエラーはありませんでした。
- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）

#### 残っている問題

- ローカルファイル選択を伴う画像の縮小・保存・再表示と、バックアップの書き出し・復元は今回のブラウザ操作環境では確認できていません。
- 上記が残っているため、`TODO.md` の「主要操作をブラウザで確認する」は未完了のままです。

#### 次回やること

- ローカルファイル選択を操作できる環境で画像とバックアップ入出力を確認し、主要操作確認を完了します。
- 完了後、バックアップ読み込み時のデータ検証強化へ着手します。

### 2026-07-11: 引き継ぎと公開画面のレスポンシブ確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を読み、現在の仕様・作業履歴・優先順位・開発ルールを確認しました。
- 公開中のGitHub Pagesで、タグUI整理後の通常画面と検索画面を確認しました。
- PC幅1,280×800とスマートフォン幅390×844でレスポンシブ表示を確認しました。

#### 変更したファイル

- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- 公開画面に通常画面上部の常設タグ絞り込みがなく、最新の変更がGitHub Pagesへ反映されていることを確認しました。
- PC幅ではサイドバー、スマートフォン幅では三本線メニューが表示されました。
- 両方の画面幅で横方向のはみ出しはありませんでした。
- 検索画面にキーワード入力欄と折りたたみ式のタグ絞り込みがあり、6種類のタグが表示されました。
- 表示確認中のブラウザコンソールエラーはありませんでした。
- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）

#### 残っている問題

- 実データを使ったメモの作成・編集・削除、タグ検索、画像、バックアップ入出力の一連の操作確認が残っています。
- GitHub Actionsの実行履歴そのものは今回確認していません。

#### 次回やること

- テスト用データを使い、主要操作をPC幅とスマートフォン幅で一通り確認します。
- 確認で問題が見つかった場合は、仕様を変えずに修正し、型チェック・ビルド・文書更新まで行います。

### 2026-07-11: 本文を主役にしたタグUIへの整理

#### 実施内容

- 通常画面上部に常時表示されていたタグ絞り込みを削除しました。
- キーワード検索とタグ絞り込みを検索画面へ集約しました。
- タグ絞り込みを再利用可能な `TagFilter` 部品にし、将来ホーム画面にも配置しやすい構成にしました。
- 保存済みメモの表示順を本文、画像、控えめなタグ、編集・削除へ変更しました。
- 入力時と編集時の任意タグ選択、保存済みタグ、検索対象としてのタグを維持しました。
- IndexedDBの型・スキーマとバックアップ形式は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換し、`dist` を生成）
- コード確認: 通常画面に常設タグ絞り込みがなく、検索画面ではキーワード単独・タグ単独・併用で絞り込める構造になっていることを確認しました。
- コード確認: メモ表示が本文、画像、タグの順であり、タグが11pxの控えめな色と背景になっていることを確認しました。
- データ互換性: `LogEntry`、Dexie定義、バックアップ入出力処理に変更がないことを確認しました。
- PC・スマートフォン表示: 開発サーバーの起動とビルド済み画面の表示を試みましたが、実行環境のローカルアクセス制限により実画面確認は実施できませんでした。

#### 残っている問題

- PC幅とスマートフォン幅での実画面確認が残っています。
- 実データを使った検索結果のタグ絞り込み操作確認が残っています。

#### 次回やること

- ローカル画面を開ける環境で、PC幅とスマートフォン幅の表示を確認します。
- キーワードのみ、タグのみ、キーワードとタグの併用、条件解除を実データで確認します。

### 2026-07-11: 長期開発向け文書の整備

#### 実施内容

- 現在の仕様と未確定の将来候補を `PROJECT.md` で分離しました。
- 設計方針、優先順位付きTODOの完了条件、開発ルールを明文化しました。
- 日付ごとに同じ形式で開発記録を残せるようにしました。

#### 変更したファイル

- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`
- `RULES.md`（新規）

#### 確認結果

- 指定された4つの文書だけを変更対象にしていることを確認しました。
- 現在の実装済み機能と将来候補が別の章に分かれていることを確認しました。
- アプリ本体のコードと依存関係は変更していません。
- 文書だけの変更のため、TypeScript型チェックとビルドは実施していません。

#### 残っている問題

- アプリの未完成・未確認項目は `TODO.md` に残っています。

#### 次回やること

- 次の開発作業を始める前に `PROJECT.md`、`TODO.md`、`DEVLOG.md` を確認します。
- 作業対象のTODOについて、完了条件と確認方法を先に決めます。

## 追記用テンプレート

以下をコピーし、`YYYY-MM-DD` と内容を書き換えて「日付別の開発記録」の先頭に追加します。

```markdown
### YYYY-MM-DD: 作業名

#### 実施内容

- 実際に行った作業を書く

#### 変更したファイル

- `変更したファイル名`

#### 確認結果

- 実行した型チェック、ビルド、テスト、画面確認とその結果を書く

#### 残っている問題

- 未解決の問題を書く。なければ「なし」と書く

#### 次回やること

- 次に着手する具体的な作業を書く
```
