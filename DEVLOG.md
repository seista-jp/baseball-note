# 開発ログ

## これまでに完成している内容

### 基本機能

- React、TypeScript、Viteを使ったアプリの土台
- IndexedDBへの日付別メモ保存と読み込み
- メモの新規作成、一覧表示、本文・タグの編集、削除
- 今日への移動、日付選択、前日・翌日への移動
- メモ件数と絞り込み後の件数表示

### 整理・検索

- 6種類のタグ付け
- 複数タグによる日付内の絞り込み（いずれかのタグに一致）
- 全期間のメモを対象にした検索専用画面
- 本文、タグ、日付文字列を対象にした部分一致検索
- 検索結果から該当日へ移動し、対象メモを一時的に強調表示
- 検索語がない場合の最新20件表示
- 自由期間の振り返りと、期間内メモのタグ絞り込み
- 選択中の期間・タグ・対象メモを分析用プロンプトと一緒に外部AI用の文章としてコピー
- 外部AIで得た分析結果を、対象期間と1つのタグと一緒に保存
- 保存したAI分析を新しい順の一覧と全文画面で閲覧

### 画像

- 画像1枚の添付、送信前プレビュー、取り消し、保存後の表示
- 保存前の画像縮小とJPEG変換による容量削減
- 画像だけのメモ作成

### データ保全

- メモと画像を含むJSONバックアップの書き出し
- JSONバックアップの読み込み
- 過去データに画像やタグがない場合の補完処理
- 通常メモと分離したAI分析用IndexedDBテーブル
- AI分析を含むバージョン2バックアップと、バージョン1バックアップの読込互換

### UI・配布

- 「振り返り」「AI分析」「検索」「バックアップ」「読み込み」を備えたPC向けサイドバーとスマートフォン向け三本線メニュー
- 画面幅760px以下、420px以下を考慮したレスポンシブスタイル
- PWA用Manifest、アプリアイコン、Service Worker
- 本番環境でのService Worker登録
- GitHub Actionsによる `main` ブランチからGitHub Pagesへの自動公開設定
- READMEへのローカル実行、公開、バックアップ手順の記載

## 現在確認できる状態

確認日: 2026-08-08

- `README.md`、`package.json`、`src` 配下の全ファイル、PWA関連ファイル、GitHub Actions設定を確認済みです。
- 現在のバージョン表記は `0.1.0` です。
- `npm exec tsc -- --noEmit` によるTypeScript型チェックと `npm run build` はエラーなく完了しています。
- 2026-08-08の作業開始時点では、利用者が追加した未追跡の `AGENTS.md` があり、内容を確認したうえで変更せず保持しています。
- `main` の確認時点の最新コミットは `519ef70 ロゴの再設定` です。
- コード上では、メモの作成・閲覧・検索・編集・削除、タグ絞り込み、画像保存、自由期間の「記録を振り返る」画面、期間＋タグの振り返り、外部AI用コピー、AI分析結果の保存・一覧・全文閲覧、バックアップ入出力、データ操作失敗時の案内まで実装されています。
- データは端末内のブラウザにのみ保存され、バックエンドAPI、ログイン、クラウド同期はありません。
- IndexedDBはバージョン2で、既存の `logs` と独立した `aiAnalyses` テーブルを使用します。通常メモの構造とインデックスは変更していません。
- 新規バックアップはバージョン2でメモ・画像・AI分析を含み、バージョン1もAI分析なしとして読み込めます。
- 自動テスト用の設定やテストファイルはありません。
- 実画像と実JSONを使う主要操作、不正なJSON読込時のエラー表示、既存メモの保持、通知を閉じる操作は利用者の実操作で確認済みです。GitHub Pagesへの最新変更の実デプロイ確認は実施していません。
- サイドメニューはPC・スマートフォンとも「振り返り」「AI分析」「検索」「バックアップ」「読み込み」の5項目で、Lucideの線画アイコン、文字サイズ、行間、余白、アイコンと文字の間隔を統一しています。上部には「ホームベース×B」の塗り仮ロゴを表示しています。

## 日付別の開発記録

新しい作業は、この章の先頭に新しい日付の記録を追加します。同じ日に複数回作業した場合は、時刻または短い作業名を見出しに加えます。未実施の内容を「実施内容」や「確認結果」に書かず、次回へ持ち越す内容は「残っている問題」または「次回やること」に記載します。

### 2026-09-08: 通常版へ音声入力を反映・公開

#### 実施内容

- 通常版の起動設定で音声入力を有効にした。通常版は従来どおり `baseballNote` と既存の `logs`、`aiAnalyses`、`composerDrafts` を使い、データベース名、表、保存項目、バックアップ形式は変更していない。
- 通常版では開発用診断ストアを作らず、開発用の診断表示・コピー・保存は動かない。開発版 `voice-record.html` と、分離された `baseballNoteVoiceIntegrationDevelopment`、`baseballNoteVoiceIntegrationDevelopmentDiagnostics` は残し、通常データへ移行・混入させない。
- 音声入力はWeb Speech APIによる文字起こしだけであり、録音保存、録音併用、共有音声トラック、Whisperを通常版へ追加していない。初回同意、マイク許可、3分上限、残り30秒案内、本文末尾への追加、終了時と中断時の下書き保護を通常版で使う。

#### 確認結果

- 通常版の公開前画面で、本文近くに「音声入力」「話し終わり」と通信説明が表示され、開発用診断欄が表示されないことを確認した。
- 既存テストを実行し、認識文字統合22/22、本文反映経路6/6、診断表示4/4が成功した。未確定文字の凍結、同じ結果番号の後着確定文字による置換、同じ通知を受けても本文へ二重追加しないことは、この自動テストで確認した。
- 実機確認は、手動終了後の本文保持・保存・手編集・下書き復元、3分上限の案内・自動終了・停止後の再開なし・確定文字保持、認識中の再読み込みでの確定文字の復元を確認済み。認識中の再読み込みは「確定文字の復元を確認。未確定文字の保持は実機未検証」とする。
- 未確定文字を含む手動終了、3分上限終了、認識中の再読み込みは、実機では未検証のままである。iPhone向け調整とPixelのピコン音改善も後日対応とし、今回変更していない。
- コミット `833b4db` を `origin/main` へPushし、GitHub Pagesのビルドと公開が成功した。通常公開URL `https://seista-jp.github.io/baseball-note/` を開き、「音声入力」「話し終わり」「音声入力の通信について」が表示され、開発用診断欄が表示されないことを確認した。

### 2026-09-08: Pixel 7a・通常記録画面の3分上限試験

#### 保存した診断原文

- 本人から渡された診断全文を、訂正・省略せず [2026-09-08-pixel-7a-integrated-voice-three-minute-limit.txt](voice-prototype-diagnostics/2026-09-08-pixel-7a-integrated-voice-three-minute-limit.txt) に保存した。通常の記録、下書き、AI用コピー、通常バックアップ、既存の分離試作・録音・モデル・診断原文は変更していない。

#### Pixel 7aで確認できたこと

- 開始日時は `2026-09-08T12:58:33.150Z`。本人が残り30秒の案内を確認し、「話し終わり」を押さずに待ったところ、`03:00.020` に3分上限へ到達して自動終了した。
- 診断では `03:00.228` に認識終了通知、`03:01.528` に最終結果の待機終了が残り、状態は `timed-out` である。本人は終了後に約10秒待っても勝手に再開せず、取得済みの文字が残ったことを確認した。
- 最終結果は「3分の上限で自動終了するか確認しています」。同じ全文が結果 #15 と #16 で再受信された後も、統合後の文字と本文へ反映した音声部分は1つの全文のままで、重複していない。
- 上限到達時に保持した途中結果は0件で、診断上も未確定の文字はない。未確定文字を保持して本文・下書きへ復元できることを、この実機試験で確認したとは扱わない。

#### 既存テストと処理の照合

- `RecognitionTranscriptState.freezeInterimResults()` は手動終了、3分上限、ページ中断のいずれも通る `stopVoiceInput` で使われる。取得済み文字は直後に本文と既存の下書き保存経路へ渡され、同じ結果番号の後着確定文字だけが途中文字を置き換える。
- 既存の認識統合テストは、この途中文字の凍結、後着確定文字による1回の置換、後着の途中文字を反映しないことを再現している。本文反映経路テストは、開始前本文へ統合済み文字だけを付け足し、同じ通知を受けても二重追加しないことを再現している。
- その後、テスト実行用の一時環境を復元して既存テストを再実行し、認識統合22/22、本文反映経路6/6、診断表示4/4が成功した。依存定義とロックファイルは変更していない。

#### 検証済みの範囲と残る確認

- 手動終了は、本人が「話し終わり」を押した後の本文保持、保存、手編集・再保存、終了後の下書き復元を確認済み。停止時に途中文字があった実機確認は未実施である。
- 3分上限は、残り30秒案内、自動終了、停止後に自動再開しないこと、確定済み文字の保持、同一全文再受信時の重複なしを今回確認した。未確定文字がある状態の上限終了と、その後の確定文字による置換は未確認である。
- 認識中の再読み込みは、本文全文の復元と「中断した前回の診断」としての表示・コピーを、開始日時 `2026-09-08T12:50:05.893Z` の本人報告として確認済み。この試験の診断原文を保存済みとは記録しない。中断時に未確定文字があったか、最後の通知まで診断へ残ったかは未確認である。
- 以上は開発版だけの確認であり、iPad/iPhone、Pixelのピコン音、未確定文字の実機確認、公開版への採用は未確認のまま残す。追加修正の根拠は現時点では見つからず、未確定文字を含む実機試験が必要になった場合だけ、その試験結果に基づいて判断する。

### 2026-09-08: 開発用音声診断の中断保存・復元・表示の修正

#### 実施内容

- 開発専用の診断IndexedDB `baseballNoteVoiceIntegrationDevelopmentDiagnostics` を版2へ更新し、認識中の途中経過だけを置く `pending` 領域を追加した。通常の記録、下書き、AI用コピー、通常バックアップ、分離試作の保存領域は変更していない。
- 音声入力の開始、認識開始、発話開始・終了、認識結果の統合、認識終了、再開要求、停止時に、診断途中経過をこの開発専用領域へ順次保存する。通常終了時は最終診断を完了済み診断として保存し、途中経過を消す。
- 再読み込み時に途中経過があれば、状態を `interrupted` として復元し、「中断した前回の診断」と明示する。対象の開始日時、最後に保存できた日時、状態を画面とコピー内容に含める。
- 今回の診断がない画面では、以前の完了済み診断を「前回完了した診断（今回の診断ではありません）」と表示する。ボタンも「前回完了した診断をコピー」とし、古い診断を「今回」として表示・コピーしない。
- 診断の復元中は開発版の「音声入力」開始を一時的に待たせる。復元処理と新しい認識開始が重なり、新しい途中診断を古い中断診断として扱わないためである。
- 再読み込み直前の最後の通知までIndexedDBへ保存できる保証はない。そのため中断診断には、この限界を画面とコピー内容に明記する。

#### 確認結果

- 診断表示の再現テストで、正常終了の今回診断、認識中の再読み込み後の中断診断、今回の診断がないときの前回完了診断、診断なしの4状態を確認した。中断または古い診断が「今回」と扱われないことを確認した。
- 既存の認識統合テスト22/22、本文反映経路テスト6/6、診断表示テスト4/4、型チェックを含む `npm run build`、`git diff --check` は成功した。
- Macでの再現テストは、Pixelで中断直前の全通知が保存されることを保証しない。次のPixel確認で、復元画面の表記とコピー内容を確認する。

### 2026-09-08: 音声入力中の再読み込み時の本文復元と診断の調査

#### 本人が確認したこと

- 既存の下書き「下書きが残るか確認しています」を残した状態で音声入力し、「音声入力の途中でも文字が残るか確認しています」と話した。文字が本文へ出た後、話し終わり・保存を押さずにChromeを再読み込みした。
- 再読み込み後に「書きかけを復元しました」と表示され、以前の下書きと今回追加した文字の両方が本文に残っていた。表示済みの音声文字が、認識中の中断後にも下書きとして復元されたことを確認した。
- 今回追加した文字が、認識上の確定文字だったか未確定文字だったかは確認していない。

#### 診断コピーの調査

- 再読み込み後の「今回の診断をコピー」は、日時 `2026-09-08T05:41:32.012Z`、結果「下書きが残るか確認しています」の前回試験の診断をコピーした。中断した今回の文字は含まれなかった。
- コードでは、`pagehide` 時に `stopVoiceInput("画面を中断したため終了しました")` を呼び、取得済み文字を下書きへ渡す。一方、診断は停止後に最大1.5秒待つ `completeVoiceInput` の中でIndexedDBへ非同期保存する。再読み込みでは待機中のJavaScriptが終了するため、今回の診断は保存されずに終わる。
- 再読み込み後は `voiceDiagnostics.latest()` が開発用診断保存領域に残る最後の1件、すなわち前回の診断を読み込む。画面の「今回の診断をコピー」という表記は、この場合には実際の今回を表さず、不正確である。

#### 必要な修正（未実施）

- 認識中断用の診断途中経過を、結果受信時と `pagehide` 時に同期的に読める開発専用の復元領域へ残す。再読み込み後は、その中断診断を「中断した前回の診断」と明示してコピー・保存できるようにする。
- 診断の保存に失敗または未完了の場合、古い診断を「今回」と表示・コピーしない。通常終了の診断と中断診断は別の状態として扱う。
- これは開発用診断だけの修正候補であり、本文・下書き・通常記録の保存処理、既存の分離試作データは変更しない。

### 2026-09-08: 通常記録画面・Pixel 7aの本文追加と下書き復元の確認

#### 本人が確認したこと

- 修正後の音声入力で追加された部分には、意図しない繰り返しはなかった。「今日は守備練習今日は守備の練習をしました」は、本人がつまずいて言い直した発話であり、アプリの重複として扱わない。
- 音声入力前から本文にあった文字は残り、話し終わり後に保存しても末尾の文章は残った。保存済みの記録を手で編集し、再保存できた。
- 外出先のPixel 7a・モバイル回線で、「下書きが残るか確認しています」と音声入力した後、話し終わり、本文と「書きかけを自動保存しました」の表示を確認した。保存を押さずに再読み込みすると「書きかけを復元しました」と表示され、同じ文章が本文に残った。

#### この確認の範囲

- 今回確認できたのは、音声入力を終了した後の下書き復元、既存本文の保護、保存、手編集・再保存である。認識中にページを閉じた場合、未確定文字を含む場合、3分上限、診断コピー、iPad/iPhoneでの動作は未確認として残す。
- Pixelのピコン音は未解決だが、後日対応とする。iPhone向けの調整も後日対応とし、今回の成功を全端末対応や公開版採用の確認とは扱わない。

### 2026-09-08: 通常記録画面・Pixel 7aの大量重複の調査と診断強化

#### 受け取った実機報告

- 対象日時は `2026-09-08T03:40:57.563Z`。本人から、通常記録画面の本文に実際の発話以上の同じ言葉が大量に表示されたこと、ピコンピコンという音も鳴ったことを受け取った。ピコン音の音源は未確定であり、消音の変更はしていない。
- 診断原文を訂正・省略せず [2026-09-08-pixel-7a-integrated-voice-duplicate.txt](voice-prototype-diagnostics/2026-09-08-pixel-7a-integrated-voice-duplicate.txt) に保存した。認識 #1、#2、#3、#4への自動再開と、#1で6件、#3で22件の確定結果が記録されている。ただし各通知の受信文字はこの版の診断に含まれない。

#### 調査と変更

- 分離試作と組み込み版は、どちらも `src/recognition-result-merge.ts` の `RecognitionTranscriptState` を使用していた。一方で、組み込み版には分離試作の発話開始・終了通知の記録がなく、生の受信文字も保存していなかった。
- Web Speech APIの `event.resultIndex` から後ろの結果を統合する処理を `applyRecognitionEvent` として共通化した。分離試作と組み込み版の両方が同じ入力範囲・同じ統合処理を通る。
- 認識の自動再開だけでは新しい発話に分けず、ブラウザの発話終了後に次の発話開始通知を受けた場合だけ発話番号を進めるよう、両画面をそろえた。これにより、自動再開で同じ累積文字が届く場合は同じ発話内で統合できる。発話境界の通知が来ない環境では、実際の繰り返しとの区別には限界がある。
- 組み込み版では、認識番号、発話番号、`event.resultIndex`、各結果番号、受信文字、確定／途中、統合処理の結果、統合後の文字、本文へ反映した音声部分を開発用診断に残すようにした。発話開始・終了通知も記録する。診断は従来どおり `baseballNoteVoiceIntegrationDevelopmentDiagnostics` だけへ保存し、外部送信はしない。
- 本文の反映は、開始時に保持した本文 `baseText` と、その時点の統合済み音声文字をつなぐ処理であることを再確認した。更新済み本文へ同じ統合結果を繰り返し追加する処理は見つからなかった。

#### 確認結果と残る点

- 保存済みの分離試作Pixel生ログ `2026-09-07T01:26:50.593Z` に残る結果 #3・#10の文字列を、組み込み版と同じ統合・本文反映経路へ流した。同じ通知を再度流しても、開始前の本文は一度だけで、音声部分も二重追加されなかった。これは今回の実機経過そのものの再現ではない。
- 既存の統合テスト22/22と、本文反映までを含む追加テスト4/4、型チェックを含むビルド、差分チェックは成功した。
- 今回の実機診断には受信文字がないため、大量重複が認識サービスから届いた文字列なのか、再開をまたいだ発話境界の扱いなのかは、まだ確定できない。今回の実機経過そのものを完全再現したとは扱わない。更新後の診断で区別する。既存本文、既存下書き、記録、分離試作データは変更していない。

### 2026-09-08: 通常記録画面の文字起こしだけの音声入力（開発版）

#### 実施内容

- 同じ記録画面を開く開発版 `voice-record.html` を追加した。この画面だけで音声入力を有効にし、開発用の記録・下書きはIndexedDB `baseballNoteVoiceIntegrationDevelopment`、本文を含む音声入力診断は `baseballNoteVoiceIntegrationDevelopmentDiagnostics` へ保存する。公開版と通常の `baseballNote`、AI用コピー、通常バックアップ、既存の分離試作領域は変更しない。
- 本文欄の近くにマイクアイコン付きの「音声入力」と「話し終わり」を追加した。Web Speech APIだけを使い、音声ファイルの録音・保存、MediaRecorder、共有トラック、whisper.cppは通常記録画面から使わない。
- 最初の開始前に、通信の可能性、音声ファイルを保存しないこと、認識文字は下書き・記録として保存することを説明し、同意または使用しない選択を求める。同意は説明文の版 `2026-09-08` とともにブラウザ内へ記憶し、版を変えた時は再確認する。画面内の「音声入力の通信について」から後で読み直せる。
- 音声文字は開始時点の本文の末尾だけへ追加する。認識中は本文・保存・破棄などの競合する操作を止め、結果ごとに既存の下書き保存を通して保護する。停止時は途中結果を凍結し、対応する確定結果だけで置き換える。終了後に古い通知が本文を戻さないよう、終了した認識器の通知は無視する。
- 本人が「話し終わり」を押した時、画面移動時、pagehide時、3分上限時は再開予約を取り消して認識を止める。停止後は最大1.5秒、確定結果だけを待つ。無音でアプリ側から終了はせず、ブラウザ側の正常終了・`no-speech` では、本人の入力時間内だけ200ms後に再開する。アプリが終了のために受けた `aborted` と、実際の認識エラーは診断と画面で分ける。
- 開発版には、入力条件、生の通知列、結果、未確定部分、終了理由を別保存しコピーする欄を付けた。診断が本文を含むこと、通常の記録・バックアップとは分かれること、外部へ自動送信しないことを画面で案内する。

#### 確認結果

- `voice-record.html` をローカルの開発配信で開き、記録本文欄の近くに音声入力欄、同意画面、通信説明、開発診断欄が出ることを確認した。「使用しない」を選んだ後も本文の手入力が使える表示になった。
- 既存の認識文字統合を `src/recognition-result-merge.ts` として通常記録画面と分離試作で共有した。Androidの累積結果、停止時の途中結果保持、確定結果による置換、停止後の再開防止に関する再現テストは22/22成功した。
- `npm run build`（型チェックを含む）と `git diff --check` は成功した。Macでの画面確認は、Pixelでの実際のマイク許可・認識・再開・3分上限・診断保存の成功を意味しない。

#### 残っている問題と次回

- Pixel 7aでの文字起こし精度、本文追加、停止後の確定結果、再開、3分上限、診断コピーは未確認。iPadとiPhoneは未確認であり、iPadの過去の分離試作成功をiPhone確認済みとは扱わない。Pixelのピコン音も未解決のまま残す。
- 既存の分離試作、録音、モデル、診断原文は変更していない。公開版、コミット、Push、GitHub Pages公開は行っていない。

### 2026-09-08: Pixel 7a・保存済み30秒録音のbeam search比較結果

#### 保存した診断原文

- 対象録音ID `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061` のbeam search比較の診断原文を、訂正・省略せず [2026-09-07-pixel-7a-offline-tiny-30-second-beam-search.txt](voice-prototype-diagnostics/2026-09-07-pixel-7a-offline-tiny-30-second-beam-search.txt) に保存した。元のgreedy診断原文と別ファイルであり、録音・モデル・既存結果は変更していない。

#### 実機で確認できた比較

- 同じ31.8秒の変換後音声、同じtinyモデル、日本語指定、翻訳しない指定、2スレッドで、候補選択だけを既存のgreedy（`best_of=5`）からbeam search（`beam_size=5`）へ替えて実行した。診断には、実行用が `sampling = beam-search, beam_size = 5` と記録した。
- greedyは文字起こし処理30,405ms、開始から完了まで32.659秒だった。beam searchは文字起こし処理43,905ms、開始から完了まで46.945秒で、完了までの時間は14.286秒長かった。
- beam search結果にも意味を取りにくい箇所が多い。正しい発話全文を用いた厳密な精度比較は行っていないため、数値としての精度差は確定しない。

#### 本人確認（認識原文とは別の記録）

- 認識結果末尾の「ご視聴ありがとうございました。」について、本人は「話していません」と回答した。発話にない文章が認識結果へ追加された事例として記録する。
- この本人確認を理由に、診断原文や保存済み認識結果を削除・訂正しない。

#### 判断

- 今回の `beam_size=5` は通常アプリへ採用しない。同じ条件での追加比較試験はいったん止める。
- この1回だけでbeam search全般や端末内文字起こし全般が不可能とは判断しない。音声と文字を両方残す目標は維持する。モデル変更や別方式の実装へ自動的には進めない。

### 2026-09-07: 保存済み30秒録音のbeam search比較を準備

#### 実施内容

- 対象録音ID `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061` にだけ、「beam searchでこの録音を比較する」ボタンを追加した。ページを開いただけでは文字起こしを開始しない。保存済みのgreedy結果、元録音、モデル、既存診断は上書き・削除しない。
- 採用中の公式 `ggml-org/whisper.cpp` コミット `52a939a2a762224e255d366c1182b2af4dd1a032`（1.9.3-dev）のWebAssembly用入口を、候補選択を明示して受け取れるよう最小変更した。`greedy` は既定の `best_of=5`、`beam-search` は同版の既定の `beam_size=5` を使う。今回の比較では後者を採用する。値を独自に増やさず、現行greedyと同じ候補数5を基準に候補選択方式だけを比べるためである。
- 実行用ファイルは同じRelease・O3・Pthread設定で再ビルドした。モデル、音声変換（16kHz・モノラル）、日本語指定、翻訳しない指定、2スレッドは既存のgreedy結果から変更しない。実行用ログにも、実際の方式と `beam_size=5` または `best_of=5` を出す。
- 新しい結果には方式と設定値、モデル読込時間、文字起こし時間、認識原文、エラー、通知列を保存し、個別の診断コピーにも含める。中止・失敗時も別結果として保存し、元録音と既存結果を残す。モデル再取得、新録音、音声の外部送信は行わない。

#### 確認結果

- TypeScript型チェックと `npm run build` は成功した。変更後のWebAssembly実行用ファイルには、greedyとbeam searchの方式・既定値を出す文字列が含まれることを確認した。`git diff --check` も成功した。
- この確認はMac上のビルド・コード確認であり、Pixelでbeam searchが動作したこと、精度が改善すること、待ち時間が変わらないことを意味しない。

#### 次回の実機確認

- 本人が対象録音の「beam searchでこの録音を比較する」を一度だけ押す。完了または中止後、greedy結果と別に保存されたbeam search結果の認識原文・文字起こし時間・診断を比較する。改善がなくても、候補選択に関する工夫すべてが無効とは断定せず、モデル変更も自動的には進めない。

### 2026-09-07: 端末内Whisper方式の精度・待ち時間の調査（実装・再実行なし）

#### 確認した構成

- 採用中の実行用ファイルは、公式 `ggml-org/whisper.cpp` のコミット `52a939a2a762224e255d366c1182b2af4dd1a032`（2026-09-04、1.9.3-dev）をRelease・O3でWebAssembly化したもの。公式例に対する試作固有の変更は、中止、実行中判定、結果文字取得を追加した部分だけで、認識パラメータは変えていない。
- 多言語モデル `ggml-tiny.bin`（77,691,713バイト、公式表で75MiB）を使用する。言語は `ja` を明示し、翻訳はしない。英語専用の `.en` モデルではないため、日本語指定として不適切なモデル指定は確認されなかった。
- 認識はgreedy方式（既定の候補数5）、温度0からの既定フォールバック、初期プロンプトなし、文法指定なし、`no_context=true`、2スレッドで実行している。実機ログはCPU・WASM SIMD・OpenMPを示し、GPUは見つからなかった。WebAssembly実行用メモリは初期512MB、必要に応じ最大2GBまで増えるビルドである。

#### 精度について分かったこと

- 短い録音と30秒程度の録音は、どちらも変換後の全サンプル数がWorkerの処理開始記録と一致した。30秒回は508,800サンプル＝31.8秒がそのまま渡っており、既知の15秒・20秒での切り捨てはない。
- Whisperは公式実装でも30秒の窓で処理する。31.8秒回でencodeが2回なのはこの境目と整合する。今回の採用版では `no_context=true` は `whisper_full()` の開始時に保持済みの文脈を消す指定であり、同じ呼び出しの中で前の区切りから作られる文脈まで毎回消す指定ではない。したがって、31.8秒の1回の文字起こしでの30秒境目だけを改善する設定ではない。
- ただし30秒回の誤認識は最初の区間から見られ、本人も普段より滑舌を崩したと報告している。短い録音と条件が異なるため、長さ、変換、発話、tinyモデルの限界のどれが主因かは確定できない。
- `initial_prompt` は公式実装で固有名詞・専門語を出しやすくする用途を持つが、単語リストが誤った候補を誘導する危険もある。野球用語を常時入れる設定は、今回のように認識全体が不安定な場合の最初の対策にはしない。beam searchの比較結果は、この文書の2026-09-08記録を参照する。
- より大きい多言語 `base` は公式表で142MiB（tinyより約67MiB増）、`small` は466MiB（約391MiB増）。公式のnative目安メモリはtiny約273MB、base約388MB、small約852MBである。モデルを大きくすれば精度改善を期待できる余地はあるが、Pixelでの速度・メモリ・発熱は未確認であり、今回の構成ではまずbaseを比較対象にとどめる。モデルが複数保存されれば、その合計の保存領域も必要になる。

#### 待ち時間について分かったこと

- 短い録音では、変換完了は0.122秒、モデル読込完了は3.320秒、文字起こしは15.804秒、全体は19.208秒だった。30秒回では、変換完了は0.143秒、モデル読込完了は2.169秒、文字起こしは30.405秒、全体は32.659秒だった。待ち時間の大半は音声変換ではなく、文字起こし本体である。
- whisper.cpp自身の計測では、短い録音の本体15.180秒中、encodeが12.421秒。30秒回の本体29.834秒中、encodeが24.048秒で最も大きい。30秒回は1秒当たりの処理時間が短い回より極端に悪化しておらず、長い録音だけで処理が急に遅くなったとは言えない。
- 現在の2スレッドは、端末が8論理コアと報告する中で固定されている。スレッド数を増やす余地はあるが、端末の実際の高速コア、Pthreadの負担、発熱、電池、他の画面操作との競合により、速くなるとは限らない。比較なしに変更しない。
- 処理自体を短くすることと、待ちやすくすることは別である。現在のログは主に結果区間が出た時に動き、30秒回では最後の区間が32秒付近まで出ない。進行率・現在の段階・中止可能表示を細かくすることは待ちやすさに寄与し得るが、認識時間そのものを短縮する証拠にはならない。
- 音声を外側で短く分割すると、各部分を早く表示できる可能性はある一方、30秒窓の文脈を失い、境目の欠落・重複・誤認識を増やし、モデル読込を繰り返せば総時間も増え得る。3分対応を考える前に、単純分割を先に採用しない。

#### 優先順位の見直しと次に比較する案（未実施）

- Workerは録音ごとに認識用のcontextを新規作成し、終了・失敗・中止のいずれでも破棄する。このため `no_context=false` が保持できるのは同じcontextで先に実行した別の `whisper_full()` の結果だけであり、現在の試作では該当する前回結果がない。31.8秒回の冒頭の誤認識にも影響しないため、次の比較候補から外す。
- 同じ1回の `whisper_full()` の内部では、採用版は各区切りの認識後に動的な文脈を更新し、次の区切りで使う。`no_context=true` でも開始時に1回消去するだけなので、30秒境目のつながりだけを `false` で改善するという前の見立ては誤りだった。
- 次に比較する価値が最も高い案は、対象録音ID `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061` を再利用し、モデル・音声・2スレッドを変えず、認識の候補選択だけを現在のgreedyからbeam search（既定の候補数5）へ替えた別結果を保存して比較することとする。追加ダウンロード量と新録音は0である。
- この比較は最初の区切りを含む全体の候補選択に影響するため、冒頭からの誤認識を確認できる。固有語を与えて特定の言葉を誘導する初期プロンプトより、まず比較しやすい。一方、精度改善は保証できず、decode時間を増やして待ち時間を悪化させる可能性がある。30秒回で最も長かったencode時間を短縮する案でもない。
- 比較で確認するのは、最初の区切り、全文の本人評価、文字起こし時間、結果の繰り返しの有無である。改善が乏しい場合は、候補選択だけでは不足と分かる。そこで初めて、モデル容量・待ち時間・端末負担を分けた上でbaseモデル比較を判断する。モデル大型化を自動的な次の手順にはしない。

#### 参照

- [whisper.cppモデル容量・多言語モデル一覧](https://github.com/ggml-org/whisper.cpp/blob/master/models/README.md)
- [whisper.cppのモデル別メモリ目安・量子化](https://github.com/ggml-org/whisper.cpp/blob/master/README.md)
- [whisper.cppの認識オプション](https://github.com/ggml-org/whisper.cpp/blob/master/examples/cli/README.md)
- [OpenAI Whisperの30秒窓・言語指定](https://github.com/openai/whisper/blob/main/README.md)
- [OpenAI Whisperの前文脈・初期プロンプトの説明](https://github.com/openai/whisper/blob/main/whisper/transcribe.py)

### 2026-09-07: 30秒試験の変換前後の音を聞き比べる確認欄

#### 実施内容

- 対象録音ID `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061` だけに、元録音（変換前）と変換後音声を本人操作で再生できる確認欄を追加した。元録音の実際の音声時間はブラウザの音声メタデータから表示する。試験の経過時間とは別の値として表示する。
- 「変換後の音声を用意する」を押した時だけ、文字起こしと同じ `decodeAudioData` と `OfflineAudioContext` による16kHz・モノラル変換を実行する。変換後の実際の音声時間とサンプル数を表示し、再生用に16bit WAVを画面内だけで作る。WAV化は聞き比べ用であり、文字起こしへ渡すFloat32の変換処理は再実行しない。
- 文字起こしの再実行、Worker起動、モデル取得、音声の外部送信、IndexedDBへの追加保存は行わない。元録音、既存文字、診断原文、保存済みモデルは変更しない。変換失敗時も元録音を残し、理由を画面へ表示する。

#### 確認結果と未確認事項

- 対象IDによる表示条件、変換後の音声時間が `サンプル数 ÷ 16000` で表示されること、変換前・変換後の再生操作が別であることをコードで確認した。型チェック、`npm run build`、既存の文字統合テスト22/22、`git diff --check` は成功した。
- 本人による元録音と変換後音声の聞き比べは未実施である。この機能を追加しただけでは、変換後の音質・欠落・速度変化がないとは判断しない。

#### 本人による聞き比べ結果

- 対象録音ID `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061` で、本人が元録音と変換後音声を聞き比べた。本人評価は「少し歯擦音みたいなのが強いですが、それ以外は元の音声とあまり変わりません。」である。
- これは本人の聴感による評価として記録する。変換が完全に正常である証明とは扱わず、歯擦音の違いと今回の認識精度低下に関係があるかも未確定である。

### 2026-09-07: Pixel 7a・保存音声の端末内文字起こし（30秒程度・1件）

#### 受け取った実機結果

- 対象録音IDは `93692e0a-fbf4-4cf0-84bf-9bf4d33ab061`。診断上の開始から完了までは32.659秒、文字起こし処理は30,405msだった。
- 本人評価は、文字起こしが話した内容とかなり違ったというものだった。今回は普段より滑舌を崩して話したとのこと。保存録音の再生では本人の言葉を聞き取れた一方、完了までの待ち時間は気になった。今回のピコン音の有無は未確認である。
- 認識原文は訂正しない。受け取った診断原文は `voice-prototype-diagnostics/2026-09-07-pixel-7a-offline-tiny-30-second.txt` に訂正・省略なしで保存した。

#### 音声の変換・受け渡しの確認（コード確認のみ）

- 録音Blobは `decodeAudioData` でデコードし、`OfflineAudioContext(1, ceil(デコード後の秒数 × 16000), 16000)` でモノラル・16kHzへ変換する。再生速度を変える指定や、15秒・20秒・30秒で切り取る指定はない。
- 変換後の全サンプルを `Float32Array` としてWorkerへ渡し、Workerは配列の全長をwhisper.cppの `full_default` にコピーして渡す。Worker側にも、音声長による自動中止や省略の設定はない。利用者の中止、ページを閉じる操作、Workerの開始失敗だけが中止経路である。
- 一方、画面の「録音表示時間」は、録音開始前のマイク許可・準備を始めた時点から停止時点までの経過時間である。変換後の実音声秒数と常に一致する表示ではない。短い成功例でも録音表示17秒に対し、変換後は15.8秒だった。
- 今回の診断では、変換後は508,800サンプル、16kHzで31.8秒であり、直後にwhisper.cpp側も同じ508,800サンプル・31.8秒を処理すると記録している。この範囲では、変換後からWorkerへの受け渡しでサンプル数が欠けたり、15秒・20秒で切られたりしていないことを確認できる。
- 一方、保存録音を本人が聞き取れたことは元の録音Blobが再生できる確認である。変換後の音声を再生して元録音と聴き比べてはいないため、このログだけで変換後の音質、内容の欠落、速度の変化がないとは確認できない。出力区間が30.960秒までであることも、末尾が無音だった可能性などを含み、欠落の証明・反証には使わない。
- したがって、現在のコードに既知の切り捨て・速度変更は見つからず、今回の変換後サンプル数はWorker側まで一致した。ただし今回の精度低下を滑舌だけ、または変換だけのどちらかに原因と断定しない。

### 2026-09-07: Pixel 7a・保存音声の端末内文字起こし（短い録音1件）

#### 受け取った実機結果

- 対象録音IDは `67fd74de-e991-44d8-b36d-d054b326e1a9`。録音表示は17秒、実際に処理した音声は15.8秒だった。
- 端末内文字起こしは成功し、エラーはなかった。文字起こし処理は15.804秒、準備を含めた所要時間は約19.2秒だった。
- 認識原文はここで訂正しない。本人の訂正として、「逃げる」は「握る」、「図まえて」は「踏まえて」が意図した言葉だったと別に記録する。
- 本人評価は「これぐらいの精度があれば多少はいける」。今回の録音中・文字起こし中にはピコン音は鳴らなかったとの確認がある。
- 開始から完了まで約19.2秒だったが、本人は待ち時間を長く感じなかった。途中の表示が動いていたことが理由の可能性として本人から挙げられたが、今回だけでは理由を確定しない。

#### この結果から言える範囲

- Pixel 7a・Chromeで、保存済みの短い録音1件を端末内tinyモデルで文字起こしし、別結果として残せることを実機で確認した。
- 長文、3分対応、iPhone対応、ピコン音の全面的な解決、方式の正式採用は未確認であり、この結果だけから判断しない。

#### 30秒試験前のコード確認（実機試験・コード変更は未実施）

- 録音Blobを読み込む処理は、デコード後の音声時間に応じて `ceil(音声秒数 × 16000)` 個のサンプルを作る。15秒で切り捨てる処理は確認されなかった。
- 同梱した実行用Workerは、このサンプル配列の全長を `full_default` へ渡す。実行用ファイルにも15秒・20秒・30秒で音声を省略する設定は確認されなかった。利用者の中止、ページを閉じる操作、Worker開始失敗時だけが処理を止める経路であり、文字起こし時間による自動中止はない。
- ただし試作画面側の `offlineMaxDurationMs` は現在20秒で、20秒を超える録音は実行ボタンを無効にし、処理開始時にも拒否する。このため、現在のままでは30秒録音を処理できない。
- 30秒実機試験には、試作用の上限を、手動停止時の表示誤差も含めて30秒を少し超える値（例: 35秒）へ上げ、画面の「15秒試作」という案内も30秒用へ直す最小変更が必要だった。変更後も、構成上30秒を最後まで渡せることと、Pixelで30秒が実際に成功することは別であり、後者は未確認である。

#### 30秒程度・上限35秒への変更

- 分離試作の対象上限だけを20秒から35秒へ上げた。35秒は、30秒程度で話し終わりを押す時の表示時刻の誤差を含む試験用上限である。
- 30秒程度・上限35秒と分かるよう、モデル保存後の案内、試作説明、各録音の実行欄を更新した。35秒を超える録音は実行ボタンを無効にし、開始経路でも理由を表示して文字起こしせず、元録音を残す。
- 音声変換は音声全体を16kHz・モノラルへ変換し、Workerは全サンプルを渡す方式のまま維持した。中止、失敗時の録音保護、別結果保存、診断記録、保存済みモデルの再利用は変更していない。今回の変更でモデル再取得を要求しない。
- 上限35秒の判定と表示をコード上で確認した。Pixelで30秒音声の速度、精度、安定性、発熱、電池消費、ピコン音の有無は実機未確認である。

#### 診断原文について

- 受け取った診断原文を訂正・省略せず、`voice-prototype-diagnostics/2026-09-07-pixel-7a-offline-tiny-success.txt` に保存した。このファイルが、上記の対象録音IDの実機結果に対応する原文である。

### 2026-09-07: Pixelでモデル取得の表示が変わらなかった件

#### 受け取った状況

- Pixel 7aで「日本語tinyモデルを取得して端末へ保存」を一度押した後、4〜5分待っても、保存前の説明と診断欄の初期表示のままだったと報告を受けた。約17秒の録音は保存され、本人の声を再生できている。Pixelの画面内ログ、IndexedDB、通信記録はMacから直接読むことはできないため、この一回について取得要求が届いたか、保存領域の確認で待機したか、端末側でJavaScriptが止まったかは確定できない。

#### 修正

- 旧コードは、ボタン処理の最初に非同期の「保存済みモデル確認」を待っていた。この確認がブラウザ保存領域の初期化・更新待ちになると、受付表示、診断欄の有効化、ダウンロード開始表示がいずれも出ない順序だった。今回の画面と整合する原因候補として修正した。ただしPixelの直接ログがないため、これだけが原因とは断定しない。
- 新しい処理は押した直後に「受付ました」と表示し、コピー用診断を有効化する。その後、「保存済みモデル確認」「保存領域の空き見込み確認」「サーバー接続」「ダウンロード進行量」「端末内保存」を順に画面と診断へ出す。保存済み確認は60秒、空き容量確認は30秒、ダウンロードは5分の時間切れを区別する。空き容量不足、HTTP失敗、取得サイズ不一致、保存失敗は診断に残す。
- 処理中の二度押しは受け付けず、保存済みモデルを見つけた場合も再取得しない。録音、既存の文字、既存の診断、既に保存済みのモデルは変更しない。

#### Macでの確認

- 保存していない別のローカル確認用URLでボタンを一度押し、0秒時点の受付表示とコピー有効化、保存領域確認、サーバー応答、進行量、77,691,713バイトの保存完了までを確認した。このMac確認はPixelでの成功を意味しない。
- 型チェック、`npm run build`、`git diff --check` は成功した。現在のCloudflare一時URLは変更していない。

#### 次回やること

- Pixelではまずページを一度再読み込みし、更新後の段階表示が出ることだけを確認する。その後のモデル取得は、表示を確認してから別の指示として案内する。

### 2026-09-07: 保存音声を端末内で文字起こしするPixel向け最小試作

#### 実施内容

- 分離試作だけに、保存済み録音を話し終わり後に端末内で文字起こしする機能を追加した。録音中のWeb Speech API、共有トラック、自動再開は使わない。
- 公式 `whisper.cpp` のWebAssembly版を一時ビルドし、日本語対応の多言語モデル `ggml-tiny.bin` を採用した。モデルのSHA-1は公式一覧の `bd577a113a864445d4c299885e0cb97d4ba92b5f` と一致した。
- 同梱したWebAssembly実行用ファイルには、公式 `whisper.cpp` のMITライセンス文を `tests/whisper-assets/LICENSE-whisper.cpp.txt` として添付した。
- モデルは画面を開いただけでは取得しない。「日本語tinyモデルを取得して端末へ保存」を本人が押した時だけ、同じ試験URLから取得して専用IndexedDBへ保存する。取得・保存失敗、モデル容量の不一致、ブラウザの保存容量不足は画面とコピー用診断へ残す。
- 採用モデルの実容量は77,691,713バイト、WebAssembly実行用ファイルは1,833,147バイト、初回合計は79,524,860バイト（約75.84MiB）である。音声と認識文字を外部サービスへ送信しない。
- 録音Blobは既存のまま残し、読み込み時だけ16kHz・モノラルの一時音声データへ変換する。変換失敗も区別して別結果に残す。成功・中止・失敗の文字起こし結果は、元録音や既存の認識文字を上書きしない別テーブル `offlineResults` に保存する。
- 最小試作は約15秒、最大20秒の保存録音だけを対象にした。日本語指定・翻訳なしで実行する。3分とiPhone向けの追加実装は行っていない。
- 文字起こしは専用Workerで行い、画面操作を継続できる。中止時はwhisper.cppの中止要求を出し、完了後にモデルの実行用コンテキストとWorkerを終了する。ブラウザが確保するWebAssembly用メモリの完全な返却は画面を閉じるまで保証できないことを画面に明記した。
- Pthread Workerに必要なCOOP/COEP/CORPヘッダーは、分離試作用のVite開発サーバーだけへ追加した。通常アプリの本番設定、GitHub Pages、通常のIndexedDB、バックアップには変更していない。

#### 確認結果

- 公式モデルの取得、SHA-1照合、WebAssembly版のビルド、実行用ファイルとモデルの同一生成元配信を確認した。Cloudflare経由の試験ページ、Worker、モデルでCOOP/COEP/CORPヘッダーが付くことを確認した。
- Macの確認用ブラウザで、HTTPS・Worker・WebAssembly・SharedArrayBufferの必要条件が検出されること、ページを開いただけでモデル取得を始めないこと、本人操作に相当する専用ボタン後にモデル取得・IndexedDB保存・コピー用診断が完了することを確認した。スマートフォン幅390pxでも横方向のはみ出しなく表示されることを確認した。
- 既存の文字統合、停止後の再開防止、iPadの途中結果保持を含む再現テストは22/22成功した。TypeScript型チェック、`npm run build`、`git diff --check` は成功した。
- Pixel実機での録音、モデル取得、文字起こし、日本語精度、処理時間、発熱、電池消費、ピコン音の変化は未確認である。Macでの文字起こし実行も、保存録音を使う試験としては実施していない。

#### 一時URL

- 既存のCloudflareトンネルを維持し、ポート43189のVite試験サーバーだけを同じポートで再起動した。現在の試験URLは `https://authentication-lesson-slightly-approximately.trycloudflare.com/voice-input.html`。以前のURL由来の試験データや診断ログは削除していない。

#### 次回やること

- Pixelで「録音だけ」を約15秒保存し、モデル取得後にその録音を端末内で文字起こしする。診断情報でモデル読込時間、文字起こし時間、中止時の挙動、録音保持を確認する。結果だけで3分対応・iPhone対応・ピコン音解決とは判断しない。

### 2026-09-07: Pixel 7aの日本語端末内文字起こし利用可否と、録音後の文字起こし候補

#### 利用可否の記録

- 利用者から、Pixel 7a・Chromeで「日本語の端末内文字起こしを確認」の結果が `unavailable` だったとの報告を受けた。この環境では、`SpeechRecognition.available({ langs: ["ja-JP"], processLocally: true })` による日本語の端末内認識を利用できる状態ではなかったものとして記録する。同じ確認は繰り返さない。
- 診断情報の原文を受領後、`voice-prototype-diagnostics/2026-09-07-pixel-7a-on-device-japanese-unavailable.txt` に一字も変えずに保存した。既存の診断ログとの重複はない。

#### 比較用の候補（未実施・方式変更は未決定）

- 候補は、録音Blobを先に保存してから、ブラウザ内で動く `whisper.cpp` のWebAssembly版と、日本語対応の小型多言語Whisperモデル `tiny` で文字起こしする方式である。録音中には認識を起動せず、「話し終わり」の後にだけ処理する。文字起こしが失敗しても、先に保存した録音は残す。
- Whisper本体と重みはMITライセンスで、API利用料はない。`tiny` の初回モデル取得は約75MBで、実装用のWebAssembly等も別に必要になる。モデルをブラウザに残せた場合、以後の録音ごとに音声を外部へ送る通信は不要だが、ブラウザの保存領域整理で再取得になる可能性は未確認である。
- `whisper.cpp` の公式WebAssembly例は、現代的なCPU・ブラウザでtiny/baseなら60秒音声に約20〜30秒という例を示す。ただしこれはスマートフォンを測定した値ではない。同例は最大音声長を120秒に制限しており、基本目標の3分にはそのまま使えない。Pixel 7aとiPhoneの実時間、発熱、電池消費、長い録音での安定性は未確認である。WebAssembly版があることと、各端末のブラウザで十分に動くことは別なので、方式を決める前にAndroid ChromeとiPhone Safariで確認が必要になる。
- 録音中のWeb Speech APIの終了・再開を使わないため、ピコン音の候補だった終了・再開を避けられる可能性はある。ただし音源は未確定であり、この候補で音が消えるとは扱わない。
- 参照: [OpenAI Whisperのモデル・ライセンス](https://github.com/openai/whisper/blob/main/README.md)、[whisper.cppのWebAssembly例](https://github.com/ggml-org/whisper.cpp/blob/master/examples/whisper.wasm/README.md)、[whisper.cppのモデル容量](https://github.com/ggml-org/whisper.cpp/blob/master/models/README.md)。

### 2026-09-07: 試験用Cloudflare一時URLの再接続

- ポート43189の試験サーバーは稼働していたが、以前の一時URL `pressed-backing-anime-nevertheless.trycloudflare.com` はDNS解決できず、既存トンネルのプロセスも見当たらなかった。同じURLでの復旧はできないと確認した。
- 新しいCloudflare Quick Tunnelをポート43189へ接続し、`https://authentication-lesson-slightly-approximately.trycloudflare.com/voice-input.html` を作成した。Viteの試験サーバー許可リストへこのホスト名だけを追加した。
- 新URLはHTTP 200で応答し、`voice-input.html` の遷移先に「日本語の端末内文字起こしを確認」ボタンが配信されていることを確認した。既存の試験データ・診断ログは削除していない。通常アプリ、コミット、Push、公開には変更を加えていない。

### 2026-09-07: 日本語の端末内文字起こし利用可否の確認を追加

#### 実施内容

- 分離試作に「日本語の端末内文字起こしを確認」ボタンを追加した。押された時だけ `SpeechRecognition.available({ langs: ["ja-JP"], processLocally: true })` を呼ぶ。
- 利用可否確認APIがない場合、APIが返した状態、問い合わせエラーを分け、返された状態は文字列を変えずに診断情報へ残す。結果は専用ボタンでコピーできる。
- この確認では、録音、マイク取得、`start()` による認識開始、言語データの導入、外部認識サービスへの自動切替を行わない。画面でも、利用可能という結果だけでは録音との同時利用やピコン音が解決したとは扱わないことを案内する。

#### 確認結果

- 既存の文字統合、停止後の再開防止、途中結果保持を含む再現テストは22/22成功した。型チェック、`npm run build`、`git diff --check` は成功した。
- 実機での利用可否確認はまだ実行していない。既存の試験データ、通常アプリ、コミット・Push・公開は変更していない。
- 確認時点でポート43189の試験サーバーは停止していたため、既存のCloudflareトンネルを変更せず同じポートで再開した。ローカル配信には新しい確認ボタンが含まれる。一方、既存の一時URL `pressed-backing-anime-nevertheless.trycloudflare.com` はDNS解決できず、トンネル側の復旧は確認できなかった。新しいトンネルやURLは作成していない。

### 2026-09-07: Pixel 7aの終了済み共有トラック対応確認

#### 保存した診断情報

- 利用者から受け取った対応確認の日時は `2026-09-07T02:26:30.068Z`。原文は `voice-prototype-diagnostics/2026-09-07-pixel-7a-ended-track-support-check.txt` に保存した。
- `readyState=ended` を記録した確認用音声トラックを、アプリ側の事前拒否なしで `start(audioTrack)` へ渡した。同期例外はなく、開始通知が届いた。
- 800ミリ秒後に確認処理自身が `abort()` を要求し、その直後の `aborted` は確認処理による意図した非同期エラー、続く終了通知も予定どおりである。
- この結果は、Pixel 7aでは共有トラック指定が実際に有効に働いていない可能性を示す。ただし、引数が無視された、Android全体で非対応である、実音声の受け渡しが不可能である、とは断定しない。共有トラック方式の同じ実機試験は、この時点でいったん止める。

#### 次の候補（未実施）

- 無料・低通信量を優先する次の候補は、Chromeの端末内文字起こしがPixel 7aで日本語に利用可能かだけを確認すること。利用可能なら、共有トラック方式を使わず、録音と端末内文字起こしを別々のマイク経路で同時に使えるかを次の段階で検討する。
- Chrome 139以降では、Web Speech APIが言語ごとの端末内文字起こしの利用可否確認と必要な言語データの導入を扱えると公式に案内されている。端末内方式なら、認識中の音声・文字を外部サービスへ送らない選択肢になる。ただし、日本語の利用可否、必要な言語データの容量、録音との同時利用の可否、ピコン音が減るかは資料だけでは分からない。
- 参照: [Chrome 139の端末内Web Speech API案内](https://developer.chrome.com/blog/new-in-chrome-139/)、[Web Speech API草案の利用可否・導入API](https://webaudio.github.io/web-speech-api/)。

#### 残っている問題

- ピコン音の音源は未確定のまま。端末内文字起こしが使える場合でも、音が減るとは予測しない。
- 音声と文字を両方残す目標は維持し、Android全体で不可能とも、同時保存を諦めるとも判断しない。

### 2026-09-07: 終了済み音声トラックの対応確認を分離試作へ追加

#### 実施内容

- 通常の試験とは別に「終了済みトラックで対応を確認」ボタンを追加した。本人がボタンを押したときだけ、その画面を開いている間に1回実行する。
- この操作専用にマイクから短時間だけ音声トラックを取得し、すぐ停止して `readyState=ended` を記録した後、アプリ側の事前拒否をせず実際に `start(audioTrack)` を呼ぶ。通常試験で使うトラック、録音、試験データ、下書きには触れない。
- 同期的な例外、開始通知、認識結果、非同期エラー、終了通知を別々に時刻付きで表示する。開始してしまった場合は800ミリ秒後にアプリが `abort()` を要求し、終了通知が来ない場合も300ミリ秒後に後始末してマイクを使い続けない。
- この確認は録音保存・自動再開を行わず、結果は専用の「この確認結果をコピー」ボタンからコピーできる。終了済みトラックが拒否されても実音声の受け渡し成功とは扱わず、拒否されない場合も引数無視とは断定しない案内を画面に表示する。

#### 確認結果

- 既存の文字統合、停止後の再開防止、途中結果保持を含む再現テストは22/22成功した。型チェック、`npm run build`、`git diff --check` は成功した。
- 実機の対応確認はまだ実行していない。既存の試験データ、一時URL、通常アプリ、コミット・Push・公開は変更していない。

### 2026-09-07: Pixel 7aの共有トラック再開統一後の実機ログとAndroid実装調査

#### 保存した診断情報

- 利用者から受け取った診断情報の日時は `2026-09-07T02:09:10.306Z`。条件は「録音と文字起こし／録音 → 文字起こし／録音と同じ音声トラックを渡す」、時間は17秒だった。原文は `voice-prototype-diagnostics/2026-09-07-pixel-7a-shared-track-restart-fixed.txt` に保存した。
- 初回と認識 #2〜#4 の再開すべてで、同じIDの `live` な共有トラックを `start(audioTrack)` の引数として渡した記録を確認した。停止要求後の再開はない。
- それでも音検出・発話検出・認識結果は0件で、認識は約5.4秒ごとに終了・再開した。再開時の引数を統一しただけでは解決しなかったと記録する。
- 利用者には約2秒おきにピコン音が鳴り続けるように聞こえたが、音の時刻は計測していない。今回の録音再生は未確認であり、前回の同条件で声を再生できた確認とは分けて扱う。

#### Android版Chromiumの公開資料による調査

- Chrome 135は `SpeechRecognition.start(MediaStreamTrack)` を追加し、別の音声トラックを文字起こしに使えるようにしたと公式リリースノートで案内している。ただし、Chromiumの出荷計画は当初Windows・macOS・Linuxのみを対象とし、Androidは対象外としていた。
- 現在のChromium公開ソースでも、この機能 `MediaStreamTrackWebSpeech` はWindows・macOS・Linuxでは安定扱いだが、Androidを含む既定の扱いは実験段階になっている。したがって、Pixel 7aのChrome 152で共有トラック入力が通常有効かどうかは、公開資料だけでは確認できない。
- Chromiumの実装は、当該機能が有効でトラックが渡された場合に、そのトラックへ音声受信用の処理を接続して認識サービスへ渡す。機能が無効の場合は、従来の開始経路へ進むコードになっている。このため、今回のように呼び出しが例外にならず開始通知だけが来ることは、共有トラックが実際に使われた証拠にならない。
- 参照: [Chrome 135 リリースノート](https://developer.chrome.com/release-notes/135)、[Chromiumの出荷計画](https://groups.google.com/a/chromium.org/g/blink-dev/c/4ibjEVQ-i0s/m/2OsaIhf3BAAJ)、[Chromiumの実装追加](https://chromium.googlesource.com/chromium/src/+/99e74c70784b2b25182a05fff268a7ff8ac416f0%5E!/)、[現在の実行時機能設定](https://chromium.googlesource.com/chromium/src/+/947e1642d5559160e207c4dc6f5702d48de6c32f/third_party/blink/renderer/platform/runtime_enabled_features.json5)。

#### 次に確認する候補（未実施）

- 同時録音の再試験ではなく、終了済みの音声トラックを `start(audioTrack)` へ渡す短い対応確認を一度だけ行う。対応する実装なら、仕様どおり `InvalidStateError` で開始を拒否するはずである。開始通知が来るなど拒否されない場合は、Android版で引数が認識されていない、または従来の入力経路へ進んでいる可能性が高くなる。この確認は、共有トラック入力そのものの有効性と、同時利用時の音声処理の問題を分けるためのものであり、まだ実装・実機試験は行っていない。

#### 残っている問題

- Android全体で共有トラック入力が不可能とは判断しない。Pixel 7aの実際の有効状態、共有トラックが認識サービスへ届くか、音検出がない理由、ピコン音の音源はいずれも未確認である。

### 2026-09-07: 共有トラック方式の再開入力を統一

#### 実施内容

- 共有トラック方式では、初回だけでなく自動再開時も、初回に保持した同じ `MediaStreamTrack` を `start(audioTrack)` の引数として渡すようにした。従来方式だけは、これまでどおり引数なしの `start()` を使う。
- 共有トラックが終了または未取得の場合は、引数なしの方式へ自動で切り替えない。再開を行わず、「共有トラックを渡せない状態なので入力方式を変えずに終了した」と診断情報へ残す。
- 初回・再開ごとに、実際に呼んだ `start(audioTrack)` または `start()`、引数として渡したトラックの状態、初回か再開かを通知列へ記録するようにした。開始通知時の「保持しているトラック」は、引数として渡した証拠ではないことも明記した。
- `onsoundstart` と `onsoundend` を通知列へ追加した。これらと既存の音声開始通知は、音声・発話の検出状況を知る補助情報であり、共有トラックが実際の認識入力として採用された証拠にはしない。
- 停止後に予約済み再開を取り消す処理、Androidの文字統合、停止時の途中結果保持は変更していない。

#### 確認結果

- 再現テストで、初回と再開が同じ共有トラックをそのまま渡すこと、共有トラックが終了した場合に引数なしへ切り替えないこと、停止要求後に再開しないことを確認した。既存のiPad途中結果保持、Android累積結果統合、終了状態の確認を含めて22/22成功した。
- 試作コードの型チェック、`npm run build`、`git diff --check` は成功した。既存の試験データ、一時URL、通常アプリには変更を加えていない。コミット、Push、公開、追加の実機試験は行っていない。

#### 残っている問題

- Pixel 7aでは初回の `start(audioTrack)` の時点から発話検出・結果が0件だった問題は、今回の修正では解決したと扱わない。再開後の入力経路を揃えたことで、次回の確認で比較しやすくなっただけである。
- Pixel 7aで聞こえるピコン音と、30秒付近の音は未解決のまま。今回、自動再開を無効化したり、端末の音を変えたりはしていない。

### 2026-09-07: Pixel 7aの同時利用・共有トラック実機ログ

#### 保存した診断情報

- 利用者から受け取った診断情報の日時は `2026-09-07T01:48:54.214Z`。条件は「録音と文字起こし」、開始順は「録音 → 文字起こし」、入力は「録音と同じ音声トラックを渡す」、時間は17秒だった。原文の通知列は `voice-prototype-diagnostics/2026-09-07-pixel-7a-shared-track.txt` に保存した。
- 録音は `00:01.661` に開始し、利用者が保存済み音声を再生したところ、自分の声は普通に聞こえた。一方、文字起こしは認識 #1〜#3 で開始通知と音声開始通知までは届いたが、発話検出・認識結果は0件だった。`00:06.903` と `00:12.337` に終了し、それぞれ約0.2秒後に再開している。
- 共有トラックは各記録時点で `kind=audio`、`readyState=live`、`enabled=true`、`muted=false` だった。ただし、これはトラックが生きている状態を示すだけで、文字起こしサービスが実際にそのトラックの音を使った証拠ではない。
- 利用者から、再開に近い時刻に繰り返す「ピコン」音が非常に気になるとの報告を受けた。音源は未確定であり、30秒付近の音の問題も未解決のまま扱う。

#### コードとの照合（変更はしていない）

- 初回の認識 #1 は `SpeechRecognition.start(sharedTrack)` を呼び出している。今回の `00:01.665` の開始要求と一致する。
- ただし、自動再開の認識 #2 と #3 は、現行コードでは `SpeechRecognition.start()` を引数なしで呼び出している。したがって、今回の再開後に共有トラックを渡したとは言えない。開始通知時の診断表示は、最初に受け取ったトラックを閉じ込めて表示するため、再開呼び出しにトラックを渡した証拠にはならない。
- W3Cの現行Web Speech API草案は、`start()` を端末マイクからの開始、`start(audioTrack)` を指定した音声トラックからの開始として分けている。後者は、トラックが音声であり、かつ `live` であることを条件にしている。今回の記録はこの二つの基本条件を満たすが、Pixel 7aのChromeが実際にこの経路で認識できることまでは示さない。

#### 次に確認すること（未実施）

- 次回は、再開時にも同じトラックを明示的に渡すよう診断表示を正してから、同じ「録音 → 文字起こし／共有トラック」の1回だけを確認する。この1回なら、初回と再開後で入力経路が混ざっていた現在のログを切り分けられる。今回、この変更や追加の実機試験は行っていない。

### 2026-09-07: Pixel 7aの30秒付近の音と終了状態の確認

#### 保存した診断情報

- Pixel 7aの文字起こしだけの診断情報について、利用者から「途中の細かな音はなく、約30秒で音が鳴った」と報告を受けた。通知列では `00:30.030` に認識終了、`00:30.243` に再開要求があり、聞こえた時刻と近い。ただし、時刻の一致だけでは音源や30秒固定の制限とは断定しない。
- 別の保存済み診断情報では、`00:34.025` に終了を要求し、`00:34.232` に認識終了した。停止後の再開、1.5秒の時間切れ、`aborted` はなく、最後の確定文章と保存内容は一致していた。これは、前回の停止後に予約済み再開が走ったケースとは別の正常終了として扱う。

#### 調査結果

- 試作コードには30秒で動くタイマーはない。時間に関する処理は、2分30秒の残り30秒案内と、3分の自動終了だけである。
- Web Speech APIの仕様は、音声認識サービスの開始・終了、音声や発話の検出をイベントとして定めるが、端末へ通知音を鳴らすことは定めていない。またWebページ側が、ブラウザ・認識サービス・端末の通知音を無音にするAPIも見つからなかった。`continuous` を指定しても、実装側が認識サービスを終了することはあり得る。
- したがって、30秒付近の音は認識サービスの終了・再開に伴うブラウザまたは端末側の音である可能性はあるが、今回の資料だけでは未確定である。自動再開の無効化や端末全体の消音は実施しない。
- 参照: [Web Speech API仕様](https://dvcs.w3.org/hg/speech-api/raw-file/tip/webspeechapi)、[ChromeのWeb Speech API解説](https://developer.chrome.com/blog/voice-driven-web-apps-introduction-to-the-web-speech-api?hl=en)。

#### 実施内容と確認結果

- 保存する「文字起こし状態」は、以前は最後に受け取った途中結果の状態（例: `listening`）を保持したままで、認識終了の状態を表していなかった。終了通知を受けた正常終了は `ended`、アプリが終了通知待ちの時間切れで中止した場合は `aborted-by-app`、実際の認識エラーは `failed` として保存するよう修正した。
- 停止後の再開防止、iPadの途中結果保持、Androidの累積結果統合、正常終了・アプリ中止・実エラーの状態を再現テストで確認した。18/18成功。型チェック、`npm run build`、`git diff --check` は成功した。

#### 残っている問題

- Pixel 7aの音源と、アプリ側で音を抑えられる方法は未確定。追加の実機試験は、この記録時点では行っていない。

### 2026-09-07: 音声入力のPixel 7a実機ログと停止後の再開防止

#### 保存した診断情報と原因

- 利用者から受け取ったPixel 7aの診断情報の日時は `2026-09-07T01:26:50.593Z`。条件は「文字起こしだけ」、時間は31秒で、利用者の確認では目立つ重複なく最後まで保存できた。録音と文字起こしの同時試験としては扱わない。
- 通知列では、認識 #3 が `00:29.474` に終了し、再開予約が `00:29.491` に作られた後、`00:29.532` に利用者が終了を要求していた。しかし以前のコードは予約を取り消さなかったため、`00:29.703` に認識 #4 の再開を要求していた。
- その再開後、最終結果または終了通知を1.5秒待って時間切れとなり、アプリが `00:31.094` に中止を要求、ブラウザから `aborted` が届いていた。停止後の再開が、不要な待ち時間と `failed` 表示の原因だった。

#### 実施内容

- 「話し終わり」を押した時に、予約済みの再開を取り消すようにした。予約の実行直前にも、停止中か・次の試験へ変わっていないかを確認し、停止後の再開を防ぐ。
- 終了通知が来ないためにアプリ自身が `abort()` を要求した場合は、「アプリが中止しました」と診断情報へ残し、`aborted` を実際の認識エラーとして `failed` にしない。アプリから中止していない場合の認識エラーは、従来どおり「エラー」として表示・保存する。
- 文字の統合処理、停止時の途中結果保持、停止後に届く確定結果での置き換えは変更していない。
- 試作コードに、通知音・音声合成・自動再生の処理は見つからなかった。保存済み録音を利用者が操作して聞くための`audio`要素だけを使っている。Pixelで聞こえたピコピコ音は、約13秒・19秒・29秒の認識終了／再開と時間が近いが、音源は未確定として扱う。

#### 確認結果

- 停止と再開予約が重なるケースを再現し、停止後は予約済み再開を実行しないことをテストへ追加した。既存のiPad途中結果保持とAndroidの累積結果統合を含むテストは15/15成功した。
- 試作コードの型チェック、`npm run build`、`git diff --check` は成功した。実機の追加試験、コミット、Push、公開は行っていない。

### 2026-09-07: 音声入力のiPad実機ログ（文字起こしだけ）

#### 保存した診断情報

- 利用者から受け取った診断情報の日時は `2026-09-07T01:04:38.875Z`。条件は「文字起こしだけ」、時間は34秒、文字起こし入力は従来方式（文字起こしがマイクを直接使う）だった。録音と文字起こしの同時試験としては扱わない。
- 修正前の認識結果は、利用者の確認で抜けや目立つ重複なく保存された。認識結果の表記は音声認識の出力のまま保持し、本文中の「パット」を含めて訂正していない。
- 生の通知列では、認識 #1・発話 #1・途中結果 #0 が全文まで更新され、`00:34.312` に終了を要求した。`00:34.330` に途中結果1件を未確定として保持し、`00:34.592` に同じ結果 #0 の確定結果を受信、`00:34.626` に結果 #1 の空の確定結果を受信した。`00:34.663` に「停止後の最終結果を受信しました」と終了した。
- 保存時点の診断情報では未確定の文字・未確定の認識結果はいずれも「なし」。停止時の途中結果が同じ結果番号の確定結果で置き換わり、全文が保存される流れをiPad実機で確認できた。

#### 確認範囲と残っている問題

- 今回確認できたのは、停止後に確定結果が届くケースだけである。確定結果が届かないまま認識終了し、未確定の文字を保存するケースは実機では未確認で、既存の再現テストでのみ確認済みとして区別する。
- Android Chromeで録音と文字起こしを同時に使うと結果0件になる原因、iPhoneでの実機動作、保存済み音声からの再文字起こしは未解決。

### 2026-09-06: 音声入力の分離試作・iPadの途中結果保持

#### 実施内容

- `文字起こしだけ` のiPad試験で、画面に出た後半の途中結果が停止・保存時に落ちる問題を修正した。停止した瞬間の途中結果は未確定として保持し、同じ認識番号・結果番号の確定結果が後から届いた時だけ、その部分を置き換える。
- 保存する「修正前の認識結果」は画面と同じ統合処理を使う。確定せず認識終了した場合も、確定済みの前半と未確定の後半を一度ずつ保存する。保存済みカードと診断情報には、未確定の有無と認識番号・発話番号・結果番号を残す。
- 「認識終了」と「停止後の最終結果を受信」を区別した。終了通知だけの場合は、最終結果を確認したとは表示しない。

#### 確認結果

- 元のiPad実機ログは作業場所に見当たらなかったため、引き継ぎ文の「結果 #0 は確定、結果 #1 は途中、停止後に認識終了」の流れを再現テストにした。前半と後半が一度ずつ残ること、停止後の途中更新で保持分を上書きしないこと、停止後の確定結果で保持分を一度だけ置き換えることを確認した。
- Androidの累積確定結果を重複なく統合する既存ケースも同じテストで確認した。統合テストは13/13成功。試作コードの型チェック、`npm run build`、`git diff --check` は成功した。
- 既存のCloudflare一時HTTPS URLはHTTP 200で応答し、ローカルのVite配信もポート5173で動作していた。URL、試験用IndexedDB、通常アプリのデータ、GitHub Pagesは変更していない。

#### 残っている問題

- この修正は再現テストでの確認であり、iPad実機で長文を停止した時の保存確認はこれから行う。Web Speech APIが停止後に確定結果を送らなければ、その部分は未確定として保存される。
- Android Chromeで録音と文字起こしを同時に使うと結果0件になる原因、iPhoneでの実機動作、保存済み音声からの再文字起こしは未解決。

#### 次回やること

- iPadで「文字起こしだけ」の長文を試し、停止後の保存カードに未確定表示が出る場合も後半まで残るか確認する。診断情報をコピーして、実際の通知列と再現ケースの差を確認する。

### 2026-09-06: 音声入力の分離試作（HTTPS確認待ち）

#### 実施内容

- `tests/voice-input.html` に、通常の記録画面と分離した音声入力の試作画面を追加した。保存先は専用IndexedDB `baseballNoteVoicePrototype` のみで、既存の `baseballNote`、通常記録、下書き、AI分析、バックアップには触れない。
- MediaRecorderによる録音、Web Speech APIによる途中認識文字、3分上限と残り30秒の案内、音声の聞き直し、修正前・修正後の文字の保持、文字修正、個別削除を用意した。文字起こしが止まっても録音はアプリ側から終了しない。
- 途中で画面が終了する場合に備え、MediaRecorderからすでに受け取った音声チャンクと認識文字を専用下書きとして保存する。終了直前の未到着部分まで保証できないため、復元後の再生は実機で確認する。
- Web Speech APIは保存済みBlobを入力に指定する仕組みを持たないため、保存済み音声の再文字起こしは今回の試作の対象外として画面に明記した。
- 「話して記録」の直前に、音声・文字は端末内へ保存すること、文字起こし時はブラウザの音声認識サービスへ音声が送られる可能性があることを案内する表示を追加した。
- Cloudflare Quick Tunnelで使う一時URLだけを、Viteの開発サーバー設定で許可した。本番ビルド・GitHub Pages・通常アプリの通信先には影響しない。
- Android確認で録音・再生は成功した一方、認識文字が空だったため、認識の開始・音声受信・発話検出・結果・エラー・終了を画面上へ表示する確認項目を追加した。停止時は録音停止後に最大1.5秒、音声認識の最終結果または終了通知を待ってから保存するようにした。実機での再確認は次回行う。
- Android再試験では、録音・再生は成功し、音声認識は `listening` から結果0件のまま終了した。録音と認識の同時利用が原因かを比較するため、録音だけ／文字起こしだけ／両方の3条件を追加した。各回の開始・発話検出・エラー・結果・終了を経過時間つきで保存し、終了時は録音トラックと認識処理を停止してから次の試験を可能にする。録音品質の条件は変更していない。
- 認識の終了通知が1.5秒以内に来ない場合は、既に届いた結果を保持したまま中止も要求し、次の条件の試験へ前の認識処理を持ち越さないようにした。
- 文字起こし単独では認識文字が出たが、同じ途中・確定結果の更新が重なって表示された。ブラウザが過去の確定結果を含む一覧を再通知しても重複追記しないよう、認識開始ごとの結果番号を記録し、確定結果は番号ごとに一度だけ保存するようにした。途中結果は毎回置き換える。実際の繰り返し発話は別の結果番号として残す。
- 再試験でも重複が続いた。画像で修正版の認識セッション表示と結果16件は確認できたが、個々の結果番号・途中／確定・認識文字の通知列は取得できていない。試験データから認識結果・通知列・ブラウザ情報だけをクリップボードへコピーするボタンを追加し、音声や試験データは送信・削除しない。通知列を受け取ってから、実際の累積通知を再現するテストと重複修正を行う。
- Androidから取得した通知列では、認識 #1 内で別番号の確定結果が `今日は`、`今日は 軸足`、全文という累積形で連続して届いていた。同じ番号の二重登録や認識再開ではない。生の通知は残しつつ、同じ認識内の累積確定結果だけを最新の長い文へ置き換える。取得した16件の通知列を使う再現テストを追加し、実際の繰り返しを含む累積結果と、前の文を含まない別結果もテストする。
- 同時実行の従来順は「録音のマイク取得・MediaRecorder開始 → Web Speech API開始」だった。録音設定は変えず、「文字起こし開始通知 → 録音開始」の逆順も選べる比較を追加した。両方モードでは両方の開始完了を待ってから「話し始めてください」と表示し、開始順と開始通知時刻を試験データ・診断情報へ残す。
- 同時実行には、従来の文字起こしが直接マイクを使う方式に加え、録音用に取得した同じ `MediaStreamTrack` を `SpeechRecognition.start(track)` へ渡す比較方式を追加した。トラックを取得した時点・渡す直前・認識開始通知時点の状態を保存する。呼び出しエラーや結果0件だけでは共有成功と扱わず、非対応時に従来方式へ自動切替もしない。
- Android Chromeの33秒・108件の通知列を再現データへ追加した。今回の列では同一の認識 #1・発話検出1回の中で、長い累積文の後に短い過去文が戻り、その後にさらに長い文が届いていた。各発話内では短い過去文を追記せず、より長い累積文だけへ更新する。前後関係を判定できない文は失わないよう残す。発話終了・次の発話開始、認識再開、同じ結果番号の訂正を新たに通知へ残し、発話が区切れた場合だけ別の文章として結合する。画面表示と保存は同じ統合処理を使う。

#### 確認結果

- 試作コードのTypeScript型チェック、`npm run build`、`git diff --check` は成功した。
- 累積通知の統合テストは6件成功。今回のAndroid通知列で、短い過去文の戻りによる重複を作らず、最後の「自分のリズムで振ることを試したいです」まで残ることを確認した。
- localhostの試作画面で、HTTPS/localhost判定、録音・文字起こしの対応表示、3分案内、保存済み音声の再文字起こしが未対応である案内、試験データ一覧の画面を確認した。

#### 残っている問題

- Android Chrome、iPad/iPhone Safariでのマイク許可、録音、再生、文字起こし精度、無音時の挙動、3分上限、中断後の音声再生は未確認。
- Web Speech APIが発話終了通知を出さない場合、別の発話と後から届いた認識訂正を完全には区別できない。今回の統合は、同じ結果番号の訂正と、前後関係が明確な累積通知だけを安全に置き換える。最後のAndroid認識エラーは通知本文が未取得のため原因未確認。
- LAN内HTTP URLはマイク利用の信頼された接続にならない。GitHub Pagesを更新せずに確認するには、外部HTTPSの一時トンネルまたは各端末へ信頼済み証明書を導入したローカルHTTPS環境が必要。

#### 次回やること

- 利用者の承認後、無料の一時HTTPSトンネルを用意し、まずAndroid Chromeで録音・文字起こし・再生を確認する。iPhone確認は同じURLでまとめて行う。

### 2026-09-05: 「言葉が浮かばないとき」を追加（ローカル確認まで）

#### 実施内容

- 記録画面の入力欄近くに「言葉が浮かばないとき」を追加した。8つのきっかけ、5つの活動、活動別の単語のまとまりを順に選び、複数の言葉を選択できる。
- 選択した言葉は「思い出すヒント」として入力欄近くへ表示し、個別解除と選び直しができる。本文・既存の分類・AI用コピー・まとめ・バックアップへは自動追加しない。
- 候補語は `src/wordHints.ts` にまとめ、表示部分から分離した。下書きの補助状態としてのみ保存するため、IndexedDBの表構成、保存済み記録、AI分析、バックアップ形式は変更していない。
- 書きかけを復元する時は選択語も戻し、保存成功時と書きかけの破棄時は下書きと一緒に消す。古い下書きにこの補助状態がなくても、本文を妨げず安全に開ける。

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド）。
- ローカル画面で、きっかけ→活動→単語のまとまり→選択→「この言葉を見ながら書く」の流れ、選択語の入力欄近くの表示、選択ボタンのチェック状態を確認した。
- 下書き保護の隔離ブラウザテスト: 18/18成功。本文・分類・ヒントの復元、ヒントだけでは保存しないこと、保存失敗時の保持、保存成功・破棄時の消去、既存メモ・AI分析・バックアップ互換を確認した。各テストの `baseball-note-draft-test-…` 一時データベースはテスト終了時に削除する処理を実行した。
- 画面改良の隔離ブラウザテスト: 3/3成功。AI分析の見出し表示と「次に試すこと」の反映を確認し、テスト中に使った端末内設定は元の値へ戻した。
- 隔離ブラウザの記録画面で、本文と既存分類「打撃」を入れたままヒントを選択・活動を切替・再読み込みし、本文、分類、ヒント「タイミング」が復元されることを確認した。テスト用下書きは破棄後の再読み込みで残っていないことを確認した。
- 同じWi-Fi用のHTTP開発URLで、初回下書きID生成に必要な `crypto.randomUUID()` が使えず、ヒントだけを選んだ時の自動保存が失敗することを実スマートフォンから報告された。安全な接続でないHTTPでも `crypto.getRandomValues()` を使ってUUID形式の下書きIDを作る代替処理を追加した。
- 修正後の下書き保護の隔離ブラウザテスト: 19/19成功。HTTP開発URL用のID生成テストを追加した。Wi-Fi用URLでヒント「タイミング」を選択し「書きかけを自動保存しました」と表示されることを確認後、確認用下書きを破棄した。

#### 残っている問題

- 実スマートフォンでのキーボード表示・タッチ操作は未確認。

#### 次回やること

- 利用者にスマートフォンでの確認を一つずつ案内し、承認を受けるまでコミット、Push、公開は行わない。

### 2026-09-05: 画面設計の実装（ローカル確認まで）

#### 実施内容

- 下部の4項目ナビゲーション、記録入力の優先表示、最近の記録から開く振り返り、「まとめ」表示を追加した。保存済みデータ名・バックアップ形式は変更していない。
- 見出しを確実に読めるAI分析だけ、本人が押した場合に限って「次に試すこと」を端末内の今日の意識として表示する。元の分析本文は変更しない。

#### 確認結果

- `npm run build`、下書き保護の隔離テスト15件、テストコードの型チェックが成功。ローカルのスマートフォン幅で下部ナビゲーション、記録、振り返り、まとめを確認した。

#### 残っている問題

- 実スマートフォンのホーム画面起動と、実データでのまとめの見出し表示は未確認。音声入力と感覚・用語選択は今回実装していない。

### 2026-09-05: 新規メモの下書き保護（ローカル検証まで）

#### 実施内容

- 作業開始時は `main`、未コミット変更なし。`improve/new-memo-drafts` を作成。中断後も同ブランチの変更を維持して再開した。
- 新規メモの本文・日付・タグ・画像を変更ごとに下書きへ自動保存し、再起動時に復元する。保存中・復元済み・失敗と再試行を表示する。
- 既存DB名・メモ・画像・AI分析の項目は変更せず、v3で `composerDrafts` テーブルのみ追加した。既存データの書き換えや削除はしない。v1・v2バックアップ互換を維持し、下書きは正式記録・バックアップと分離した。
- 正式保存と下書き内容の消去は一括処理。処理済みIDだけを残して二重保存・破棄済み内容の復活を防ぐ。保存中に変更された入力全体は次の下書きに残す。
- 複数タブの更新は改訂番号を比較し、競合した入力を別の下書きへ分離して保護する。別タブの内容で画面を自動上書きしない。ほかの書きかけは一覧から開ける。
- 「書きかけを破棄」後に「残す」「破棄する」の確認を表示。確認中は対象入力を変更できないようにする。
- 音声・メニュー再編・AIコピー範囲の変更は行っていない。依存関係の追加・更新もなし。

#### 変更したファイル

- `src/drafts.ts`、`src/db.ts`、`src/App.tsx`、`src/styles.css`
- `tests/drafts.html`、`tests/drafts.test.ts`
- `AGENTS.md`、`PROJECT.md`、`README.md`、`TODO.md`、`DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（型チェックとViteビルド）。テストコード単体の厳密なTypeScript型チェックも成功。
- `tests/drafts.html` の15ケースが成功。本文・過去日・タグ・画像の復元、保存失敗、一括消去の失敗、保存中の追加入力、連打、破棄、下書き保存失敗と再試行、複数タブの競合・二重保存防止、自動保存失敗後の競合、v2 DBからの追加更新、既存編集・検索・画像付きv2バックアップ往復・v1互換を確認した。
- 自動テストは `baseball-note-draft-test-*` のランダム名の専用IndexedDBを作り、終了時にそのテストDBだけを削除する。普段のブラウザ・公開版のデータは操作していない。
- ローカルURLの架空データで本文・日付・タグ・実画像の復元、正式保存後の再読み込みで下書きが出ないこと、確認画面の「残す」、破棄と再読み込みを確認した。ブラウザ操作環境の再接続後にも書きかけが復元された。
- 320×844、390×844、1280×800で確認した画面に横方向のはみ出しなし。スマートフォン幅の画面画像をチャットに提示した。確認中のアプリのブラウザ警告・エラーなし。
- `git diff --check`: 成功。Push・マージ・公開・デプロイなし。

#### 残っている問題

- 実Android / iPhone、ホーム画面起動、ソフトウェアキーボード、OSによる強制終了、実際の容量不足は未確認。入力・画像準備・自動保存が完了する前の強制終了まで保護を保証しない。
- 改良版UIで書き出したダウンロードファイルそのものを選び直す一巡は未確認。画像BlobとData URLを含む保存・読込処理の往復は隔離自動テストで確認済み。
- 下書きはバックアップに含めない。正式保存後の記録のみ既存バックアップに含める。
- 旧コードに戻す場合、v3 DBを開ける定義を維持する必要がある。DBの削除・初期化やバージョンの切り下げは行わない。

#### 次回やること

- 利用者のローカルプレビュー確認を受け、下書き保護に関係する変更だけを最終レビューしてコミットする。公開は別途の明確な依頼を待つ。
- プレビュー: `http://127.0.0.1:43188/`。起動コマンドとテスト手順はREADMEに記載。
- コミット用Summary: `新規メモの自動下書き保存と競合保護を追加`。

### 2026-08-27: iPhone幅でタグが重ならない配置に修正

#### 実施内容

- 画面幅420px以下で、入力欄の6個のタグを1段に固定していた配置を、3個ずつ2段の配置に変更しました。
- タグボタンの文字・押しやすい高さ・選択時の見た目は変更していません。PC表示は従来どおり1段です。
- IndexedDB、メモ、タグ、画像、バックアップの保存形式は変更していません。

#### 変更したファイル

- `src/styles.css`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,794モジュールを変換）
- スマートフォン幅320×844pxと390×844pxを確認し、タグが3個ずつ2段に並び、タグ同士に4px、タグと入力欄の間に8pxの間隔があることを確認しました。
- 両方のスマートフォン幅で画面全体の`clientWidth`と`scrollWidth`が一致し、横方向のはみ出しがないことを確認しました。
- スマートフォン幅430×844pxでは、6個を1段に表示してもタグ同士の重なりと横方向のはみ出しがないことを確認しました。
- PC幅1,280×800pxでは従来どおり6個が1段に並び、入力欄と10pxの間隔があることを確認しました。
- 確認中のブラウザコンソールに警告・エラーはありませんでした。
- コミット `70efaed`を`main`へPushし、GitHub ActionsのビルドとGitHub Pages公開が成功したことを確認しました。
- 公開URLを390×844pxで確認し、タグが3個ずつ2段で表示され、重なりと横方向のはみ出しがなく、入力欄と8pxの間隔があることを確認しました。公開画面のブラウザコンソールにも警告・エラーはありませんでした。

#### 残っている問題

- 実際のiPhoneでの表示は未確認です。

#### 次回やること

- 利用者のiPhoneでタグと入力欄の表示を最終確認します。

### 2026-08-23: 「次に試す」機能を現在の優先作業から外す

#### 決定内容

- 保存した過去の記録から1件を選び、練習前に「次に試す」として表示する使い方が必要か、利用者へ確認しました。
- 利用者本人にはその使い方がないことが分かったため、現在の優先作業から外しました。
- すでに今日の画面には「今、一番意識していること」が表示されるため、似た表示や操作を増やさず、現在のシンプルな画面を維持します。
- 将来、実際の利用確認で必要性が見つかった場合だけ再検討する項目として、`TODO.md` の「将来アイデア」へ移しました。
- アプリ本体、保存データ、バックアップ形式は変更していません。

#### 変更したファイル

- `TODO.md`
- `DEVLOG.md`

#### 次回やること

- 初めて使う人に説明なしで試してもらい、最初の記録、振り返り、データ保存で迷う箇所がないかを確認します。

### 2026-08-23: ロゴとアプリ名をホームボタンに変更

#### 実施内容

- スマートフォンヘッダーの「Baseball Note」と、PCサイドバーのロゴ・アプリ名をボタンへ変更しました。
- AI分析、検索、振り返り、安全ページ、過去の日付など、どの画面から押しても今日の日付の記録画面へ戻ります。
- 通常メモやAI分析が入力途中の場合は、既存の未保存確認を再利用し、確認を取り消した場合は入力中の画面へ残します。
- ボタンには「今日の記録へ戻る」という読み上げ用ラベル、キーボードフォーカス表示、PCのマウス操作時の背景表示を追加しました。
- IndexedDB、通常メモ、AI分析、localStorage、バックアップJSONの形式には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,794モジュールを変換）
- スマートフォン390×844pxで、AI分析、検索、安全ページ、前日の日付からホームボタンを押し、2026年8月23日の「今日の記録」へ戻ることを確認しました。320×844pxでもヘッダーとホームボタンが横方向にはみ出さないことを確認しました。
- PC 1,280×800pxで、AI分析からサイドバーのロゴ・アプリ名を押し、今日の記録へ戻ることを確認しました。
- PC・スマートフォンとも横方向のはみ出しはなく、確認中のブラウザコンソールに警告・エラーはありませんでした。
- コードレビューでは、ホーム移動が既存の通常メモ・AI分析の未保存確認を通ること、入力途中で移動を取り消せること、保存データやバックアップへ変更を加えていないことを確認しました。

#### 残っている問題

- 実スマートフォンでのタッチ操作は、公開後に利用者環境で確認が必要です。

#### 次回やること

- 利用者へスマートフォン確認画像を提示し、承認を受けます。承認を受けるまでコミット、Push、公開は行いません。

### 2026-08-23: 安全・データ説明とバックアップ案内を追加

#### 実施内容

- 初回の使い方案内へ「記録の保存について」を追加し、アカウント登録不要、記録と写真の端末内保存、運営者へ送られないこと、広告と独自アクセス解析がないことを短く案内しました。
- 初回案内の「詳しく見る」とメニューの「安全とデータについて」から、同じ詳細ページを開けるようにしました。公開URLの `#safety` から直接開いた場合は初回案内を重ねず、戻る操作で元の画面へ戻ります。
- 「安全とデータについて」に、公式URL、アプリ表示時の通信、端末内保存、写真選択、外部AI、バックアップ、GitHub Pagesのアクセス情報、安全上の注意、運営者・X・メールの問い合わせ先を掲載しました。
- アプリ内にプライバシーポリシーと利用規約を追加し、`#privacy` と `#terms` から直接開けるようにしました。運営者は `Taka_BsblWorks`、問い合わせメールは `taka.bsbl.works@gmail.com` としました。
- 「データを保存」の実行前に、バックアップファイルの役割、記録件数、AI分析件数、最後に保存した日時を表示する確認画面を追加しました。保存実行後の日時は `baseball-note-last-backup-at` のlocalStorageへ記録します。
- 「データを戻す」のファイル選択前に、既存記録をすべて消さず、同じIDは上書き、それ以外は追加されることと、心配な場合の事前保存を説明する確認画面を追加しました。
- 「AI用にコピー」の後に外部AIへ貼り付けて送信する案内は、今回実装せず `TODO.md` に残しました。また、有料化、会員登録、クラウド保存、広告、独自アクセス解析を将来追加する場合の規約・プライバシー見直しも将来候補へ記録しました。
- IndexedDBスキーマ、通常メモ、AI分析、バックアップJSONの形式には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/InformationScreen.tsx`
- `src/styles.css`
- `README.md`
- `AGENTS.md`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド）
- 初回案内の「詳しく見る」、メニュー、`#safety` の直接URLから安全ページを開けることを確認しました。安全ページからプライバシーポリシーと利用規約を開き、戻る操作で正しく戻れることも確認しました。
- 「データを保存」の確認画面に記録18件・AI分析0件が表示され、保存後に「最後にデータを保存した日」が `2026/8/23 15:57` と更新されることをローカル確認用データで確認しました。
- 「データを戻す」の確認画面に、ファイル選択前の説明と「ファイルを選ぶ」「キャンセル」が表示されることを確認しました。
- PC 1,280×800px、スマートフォン390×844px・320×844pxで主要画面を確認し、横方向のはみ出しがないこと、320px幅でも使い方案内全体が一画面内に収まることを確認しました。
- 確認中のブラウザコンソールに警告・エラーはありませんでした。
- コミット `9ae4d12` をPushし、GitHub ActionsのビルドとGitHub Pagesへの公開が成功しました。公開後に利用者がiPadでデータの保存・復元まで確認し、一旦問題なく使えることを確認しました。

#### 残っている問題

- 利用規約とプライバシーポリシーは現在の無料・会員登録なし・クラウド保存なし・広告なしの構成に合わせた案です。サービス内容を変更する場合は公開前に見直します。

#### 次回やること

- 当面の改善方針の次項目である、保存した記録を1件だけ「次に試す」に指定する仕組みを一問一答で決めます。

### 2026-08-23: 初めて使う人向けの使い方案内を追加

#### 実施内容

- 初めてアプリを開いたときだけ、「今日の感覚や、意識したことを記録しよう」という見出しと、「タグを選ぶ」「感じたことを書く」「あとから振り返る」の3項目を1画面で表示する案内を追加しました。
- 3項目には既存のLucide線画アイコンを使い、スマートフォンでは縦並び、PCでは3列で表示します。
- 「始める」を押した場合は、通常画面へ戻った後にタグ部分を示す吹き出しを出し、「次へ」の後に文章入力欄を示します。文章欄を押すと案内は終了し、キーボードを自動表示しません。
- 「あとで見る」を押した場合は吹き出しを表示せず、どちらのボタンでも次回以降は自動表示しません。
- PCサイドバーとスマートフォンメニューの「データを戻す」の下へ間を空け、丸い「？」アイコン付きの「使い方」を追加しました。ここから開いた案内は「閉じる」ボタン1つで元の画面へ戻ります。
- 初回案内の確認状態を `baseball-note-onboarding-completed` のlocalStorageへ保存します。メモ、画像、AI分析、IndexedDBスキーマ、JSONバックアップ形式には変更を加えていません。
- 一般利用者に分かりやすくするため、画面上の「送信」を「保存」、「今日のログ」「過去のログ」を「今日の記録」「過去の記録」、「バックアップ」「読み込み」を「データを保存」「データを戻す」へ変更しました。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `README.md`
- `AGENTS.md`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- 初回表示で1画面案内が開き、「始める」からタグ、文章入力欄の順に吹き出しが表示され、文章欄を押すと消えることを確認しました。再読み込み後は自動表示されません。
- 別の初回環境で「あとで見る」を押し、吹き出しが表示されず、再読み込み後も案内が自動表示されないことを確認しました。
- メニューの「使い方」から同じ案内を開けること、初回用の2ボタンではなく「閉じる」だけが表示されることを確認しました。
- PC 1,280×800px、スマートフォン390×844px・320×844pxで案内、吹き出し、メニューを確認し、横方向のはみ出しがなく、320px幅でも案内全体が縦スクロールなしで収まることを確認しました。
- 確認中のブラウザコンソールに警告・エラーはありませんでした。
- コードレビューでは、初回判定が案内専用のlocalStorage設定だけを使い、IndexedDBやJSONバックアップへ書き込まないこと、案内を開閉しても入力途中の本文を保持することを確認しました。今回の変更に、コミットを止める問題は見つかりませんでした。
- 確認では既存メモを読み取り表示しただけで、新しいメモの保存、既存メモの編集・削除、バックアップの書き出し・読み込みは行っていません。

#### 残っている問題

- `TODO.md` の完了条件である「初めて使う中学生が、説明を受けずに最初の記録を保存し、振り返りの入口を見つけられること」は未確認です。実際の利用者確認が終わるまでTODOは未完了のままにします。
- 実スマートフォンのタッチ操作、ソフトウェアキーボード、OSの文字拡大、画面回転は未確認です。

#### 次回やること

- 利用者が提示したスマートフォン画像とローカル画面を確認し、修正の必要がなければコミット用Summaryを確認します。
- 利用者の明確な承認を受けるまで、コミット、Push、公開は行いません。

### 2026-08-23: 「今、一番意識していること」の長期判定と表示を追加

#### 実施内容

- コミット前の標準手順として、ビルド、関係テスト、Git差分・AIレビュー、PC・スマートフォン実画面確認、スマートフォン画像の提示、利用者承認後のコミット・Push、公開後確認を `AGENTS.md` に追加しました。
- 利用者との進め方として、開発初心者向けの短い説明、一度に1つだけ質問する一問一答、長い会話を新しいチャットへ移す際の引き継ぎ文作成を `AGENTS.md` に追加しました。
- 利用者との一問一答で、アプリの中心目的を「練習や試合で感じたことを短く残し、今、一番意識していることを見つける」と整理しました。
- 毎日のテーマ登録や追加選択は継続の負担になるため追加せず、既存の「振り返り」から「AI用にコピー」を使うときだけ長期判定を行う構成にしました。
- 選択期間、選択タグ、選択対象メモによる従来の振り返りは変更せず、保存されている全期間・全カテゴリーのメモを独立した「長期判定用記録」としてコピー文章へ追加しました。
- 外部AIへ、意味の近い記録をまとめ、最近の記録だけで以前から続く意識を外さず、全期間で最も多く長く意識している内容を1つ判断するよう指定しました。判断根拠の日付と記録を詳しい振り返りへ示し、判断できない場合は無理に決めない条件も追加しました。
- AI回答のコードブロック1行目を `【今、一番意識していること】内容` の固定形式にし、その後へ既存の「まず見る要点」と「詳しく読む振り返り」を続けるよう指定しました。
- 保存済みAI分析を新しい順に確認し、固定行から読み取った内容を今日の記録画面上部へ表示しました。最新分析に固定行がない場合は、固定行を持つ直近の分析結果を維持します。
- IndexedDBスキーマ、通常メモ、AI分析の型、バックアップ形式、選択期間・タグの動作は変更していません。既存AI分析は固定行がなくても従来どおり保存・表示できます。

#### 変更したファイル

- `AGENTS.md`
- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- 実画面で2026年8月23日だけを選択し、「選択期間のBaseball Note記録」には23日の記録だけが入り、独立した「長期判定用記録」には22日と23日の全記録が入ることを確認しました。
- コピー文章に、長期判定の条件、全期間・全カテゴリーの見出し、固定形式の1行が含まれることを確認しました。
- 固定行 `【今、一番意識していること】かかとの使い方` を含むAI分析を保存し、再読み込み後も今日の記録画面上部へ「かかとの使い方」が表示されることを確認しました。
- スマートフォン幅390×844pxで、意識表示、通常メモ、下部入力欄が自然に収まり、画面全体の `clientWidth` と `scrollWidth` が一致することを確認しました。
- 確認中のブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 実際の全166件を外部AIへ貼り付けたときに、意味の近い記録を意図どおりまとめ、根拠を示して1つの意識を返せるかは利用者環境での確認が必要です。
- 記録が数年分まで増えた場合のコピー文章量は未確認です。現在は利用者の希望どおり全期間を対象とし、実使用で長すぎることが確認された場合に対象期間や要約方法を検討します。
- 実スマートフォンでのクリップボード権限、タッチ操作、OSの文字拡大、画面回転は未確認です。

#### 次回やること

- 利用者の実データで「AI用にコピー」を実行し、外部AIが返した固定行をAI分析として保存して、表示内容と長期判定の納得感を確認します。
- コミット、Push、公開は利用者の確認と指示を受けてから行います。

### 2026-08-16: AI用コピーを短い要点と詳しい振り返りの2段構成へ変更

#### 実施内容

- 「AI用にコピー」で生成するプロンプトを、30秒程度で読める「まず見る要点」と、同じ内容の根拠を1〜9で説明する「詳しく読む振り返り」の2段構成へ変更しました。
- 「まず見る要点」は、重要な内容を3〜5項目に絞り、一番の気づき、よく出てきたテーマ、まだ分からないこと、次に試すことを短い文で示すよう指定しました。
- 「詳しく読む振り返り」は、従来の1〜9を維持し、結論を先に、元記録の根拠を後に書き、短い要点をそのまま繰り返さず補足するよう指定しました。
- 記録にない内容を追加しない、無理に結論を出さないなどの既存条件と、回答全体を1つのコードブロックへ出力する形式を維持しました。
- UI、IndexedDB、通常メモ、AI分析、バックアップのデータ構造には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- コピー文章の構成確認: 「まず見る要点」「詳しく読む振り返り」、従来の1〜9、対象期間、対象タグ、Baseball Note記録、1つのコードブロックへの出力指定がすべて含まれることを確認しました。
- コピー文章の並びが、分析指示、対象期間、対象タグ、Baseball Note記録、出力形式指定の順になっていることを確認しました。
- `git diff --check`: 成功

#### 残っている問題

- 外部AIの回答は利用するAIや入力する記録によって変わるため、実際の記録で短い要点と詳しい振り返りが意図した分量と読みやすさになるかは、利用者環境での確認が必要です。

#### 次回やること

- 利用者が「AI用にコピー」した文章を外部AIへ貼り付け、2段構成の回答をBaseball Noteの「AI分析結果」へ保存して、実際の読みやすさを確認します。
- コミット、Push、公開は利用者の確認と指示を受けてから行います。

### 2026-08-08: AI用コピーの出力形式指定を追加

#### 実施内容

- 「AI用にコピー」で生成する文章の末尾へ、1〜9をすべて1つのコードブロック内へ出力し、コードブロック内へ分析結果以外の説明を入れないよう指定する2文を追加しました。
- 既存の1〜9の分析項目、5つの分析条件、対象期間、対象タグ、Baseball Note記録の内容と並びは変更していません。
- UI、IndexedDB、通常メモ、AI分析、バックアップのデータ構造には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- 実アプリで2026年8月8日・「すべて」・既存記録3件を表示して「AI用にコピー」を実行し、コピー成功表示を確認しました。
- コピー文字列の組み立て処理とビルド成果物で、新しい2文が対象期間・タグ・記録の後にあり、文章末尾に出力されることを確認しました。既存の1〜9、期間、タグ、タグ付き・タグなしの記録表記は維持されています。
- `git diff --check`: 成功

#### 残っている問題

- 自動操作環境のクリップボードはアプリのClipboard APIと共有されないため、コピー後の本文を自動取得する確認はできませんでした。実アプリのコピー成功表示と、実際にコピーへ渡す文字列の組み立て処理・ビルド成果物で確認しています。

#### 次回やること

- 利用者がプレビュー上で「AI用にコピー」した文章をChatGPTなどへ貼り付け、回答全体が1つのコードブロックになることを確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-08-08: 第二回「AI分析結果の保存・閲覧」改修

#### 実施内容

- `AGENTS.md`、`RULES.md`、`PROJECT.md`、`TODO.md`、`DEVLOG.md` を作業前に確認し、既存メモ、振り返り、AI用コピー、IndexedDB、バックアップ互換性、レスポンシブUIを保護対象として実装しました。
- PCサイドバーとスマートフォンのドロワーへ、既存と同じ寸法・Lucide線画アイコンの「AI分析」を追加しました。ホーム画面へ新しいボタンは追加していません。
- AI分析画面へ「＋ AI分析を保存」、保存済み分析の新しい順一覧、全文閲覧画面を追加しました。一覧には対象期間、1つのタグ、本文冒頭、保存日を表示し、本文はMarkdown変換せず改行を保ったプレーンテキストで表示します。
- 保存画面へ開始日、終了日、「すべて」・既存6タグ・「その他」の単一選択、分析本文、保存ボタンを追加しました。画面を開いた時点の振り返り期間とタグを初期値として引き継ぎ、振り返り側の状態は変更しません。
- AI分析入力中の本文がある場合は、一覧や別画面への移動と再読み込みで未保存確認を行うようにしました。既存メモの未保存確認も維持しています。
- 通常メモの `LogEntry` と `logs` テーブルを変更せず、IndexedDBバージョン2で `aiAnalyses` テーブルを追加しました。AI分析はID、開始日、終了日、タグ、本文、保存日時を持ちます。
- バックアップ書き出しをバージョン2へ更新し、メモ・画像・AI分析を含めました。読込はバージョン1と2に対応し、バージョン1でAI分析配列がない場合は空として扱います。両データは同じDexieトランザクションで追加・同一ID上書きし、バックアップにない既存データは削除しません。
- AI分析のID、期間、タグ、本文、保存日時、配列内ID重複を読込前に検証し、保存日時はISO形式へ正規化して新しい順のインデックス表示を維持します。
- 現在のデータ契約を正しく残すため、`AGENTS.md` のIndexedDB・バックアップ部分だけを更新しました。`RULES.md` と依存関係は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `src/types.ts`
- `src/db.ts`
- `src/backup.ts`
- `README.md`
- `AGENTS.md`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- `git diff --check`: 成功
- バックアップ純粋処理: 書き出しデータがバージョン2でAI分析を含むこと、バージョン1でAI分析省略を空配列として読むこと、バージョン2の往復、時差付き保存日時のISO正規化を確認しました。AI分析配列欠落、明示的な `null`、開始日と終了日の逆転、未対応タグ、重複ID、未対応バージョンはすべて書込前に拒否しました。
- IndexedDB移行: 旧版と同じバージョン1のデータベースへ通常メモを保存した後、同一originで改修版を開き、既存メモが残ること、空のAI分析一覧を開けること、AI分析を保存・再読み込みできることを確認しました。
- AI分析: 画面表示、複数件の新規保存、新しい順、対象期間・タグ・本文冒頭・保存日、全文と改行、再読み込み後の保持を確認しました。振り返りで選択した同日期間と「打撃」が保存画面へ引き継がれることも確認しました。
- 新バックアップ読込: バージョン2のテストJSONからメモ1件とAI分析1件を読み込み、読込前からあるメモとAI分析を残したまま両方が追加されることを確認しました。
- 旧バックアップ読込: `aiAnalyses`、画像、タグを持たないバージョン1のテストJSONを読み込み、通常メモが追加され、既存AI分析3件がそのまま残ることを確認しました。不正なバージョン2 JSONは理由を表示して停止し、既存AI分析を保持しました。
- 既存機能: 通常メモの新規保存と再読み込み、検索、同日期間の振り返り、「すべて」と「打撃」の件数、AI用コピーを確認しました。コピー本文には分析プロンプト、期間、選択タグ、元メモ本文が含まれ、成功表示も出ました。
- PC 1,280×800px、スマートフォン390×844px・320×844pxで、AI分析の一覧・保存・全文画面とメニューを確認しました。3幅とも画面全体の `clientWidth` と `scrollWidth` が一致し、長い連続文字も本文幅内で折り返しました。主要ボタンは44px以上、保存ボタンは48px、スマートフォンのタグは38pxでした。
- PC・スマートフォンの確認中にブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- バックアップ書き出しは「メモ1件、AI分析2件を書き出しました」の完了表示まで確認しましたが、ブラウザ自動操作環境でBlobダウンロードの実ファイルを取得できなかったため、その生成ファイル自体の再読み込みは未確認です。書き出し用データ生成がバージョン2とAI分析配列を返すこと、同じ構造のバージョン2 JSONを画面から復元できることは別々に確認しています。
- AI用コピーの成功表示と実クリップボード本文は確認しました。失敗表示の分岐は今回変更していないコードを確認しましたが、実ブラウザで権限エラーは発生させていません。
- 実スマートフォンのタッチ操作、ソフトウェアキーボード、OSの文字拡大、画面回転、実端末でのクリップボード権限は未確認です。

#### 次回やること

- 利用者環境で、AI用にコピーした記録を外部AIへ貼り付け、返ってきた分析を保存して見返す一連の操作を確認します。
- 利用者が実際に書き出したバージョン2バックアップを同じアプリへ読み込み、メモ・画像・AI分析が復元されることを最終確認します。
- AI分析の比較、編集・削除、複数タグ、元メモとの個別リンク、Markdown表示などは今回追加せず、実使用後に必要性が確認できた場合だけ別作業として検討します。

### 2026-08-08: 第一回「振り返り・外部AI連携」改修

#### 実施内容

- `AGENTS.md`、`RULES.md`、`PROJECT.md`、`TODO.md`、`DEVLOG.md` を作業前に確認し、既存タグ、タグなしメモ、IndexedDB、localStorage、バックアップ形式、現在の振り返り操作を引き継ぎました。
- 振り返り画面へ「すべて」、既存の「打撃」「守備」「走塁」「投球」「体調」「フィジカル」、「その他」の8択を追加し、選択期間と1つのタグを同時に指定できるようにしました。「その他」は保存タグを増やさず、既存のタグなしメモだけを対象にします。
- 複数タグが付いたメモは、選択したタグを含む場合に対象とし、「すべて」では期間内の全メモを日付・作成時刻順のまま表示します。
- 選択中の期間、タグ、対象件数を表示し、対象0件ではコピー操作を無効にして、条件に一致する記録がないことを案内するようにしました。
- 「AI用にコピー」を追加し、9つの分析観点と5つの重要条件をまとめたプロンプト、対象期間、対象タグ、全対象メモを1つの文章としてクリップボードへコピーするようにしました。プロンプト本文は `src/App.tsx` の定数1か所へまとめています。
- コピーする記録は `【YYYY-MM-DD｜タグ】` と保存本文で構成し、複数タグはすべて併記、タグなしは表示上「その他」としました。保存本文には整形・要約・書き換えを行っていません。
- Clipboard APIが利用できない環境向けの既存ブラウザ方式のフォールバック、成功表示「AI分析用の記録をコピーしました」、失敗時の権限案内を追加しました。期間・タグ・対象記録が変わった場合は古い成功表示を消します。
- IndexedDBスキーマ、メモ型、保存タグ、振り返り期間のlocalStorageキー、バックアップのバージョンとJSON構造は変更していません。アプリ内AI、外部API、音声、画像、まとめノートも追加していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `TODO.md`
- `DEVLOG.md`

#### 確認結果

- `npm run build`: 成功（TypeScript型チェックとViteプロダクションビルド、1,793モジュールを変換）
- `git diff --check`: 成功
- 既存メモ: 作業前からあった1件が検索と再読み込み後も表示されることを確認しました。
- 新規メモ: 2026年8月7日と8日に、単一タグ、複数タグ、タグなしのテストメモ4件を画面から追加し、保存後と再読み込み後に表示されることを確認しました。
- 期間＋タグ: 2026年8月7日〜8日で「すべて」4件、「打撃」2件、「その他」1件、「投球」0件になり、選択外の本文が表示されないことを確認しました。
- AI用コピー: 「打撃」では対象2件だけがコピーされ、「すべて」では期間、選択タグ、4件すべての日付・タグ・保存本文がコピーされました。複数タグとタグなしの表記、9つの分析観点、5つの重要条件、成功表示も確認しました。
- 元本文: 画面表示とコピー文字列を比較し、句読点を含むテスト本文が変更されていないことを確認しました。
- バックアップ書き出し: 画面に「5件を書き出しました」と表示されることを確認しました。自動操作環境ではダウンロードイベントを取得できなかったため、生成ファイルそのものは未確認です。
- バックアップ読み込み: バージョン1のテストJSONを選択し、新規1件・上書き0件の確認後、「1件を読み込みました」と表示され、振り返り一覧も即時更新されることを確認しました。
- 後片付け: 作成・読み込みしたテストメモ5件を既存の削除操作ですべて削除し、「AIテスト」の検索結果が0件に戻ったこと、作業前からあった既存メモ1件を保持していることを確認しました。
- PC幅1,280×800px、スマートフォン幅390×844px・320×844pxで、期間、8個のタグ、対象件数、コピー操作、完了表示、記録一覧が収まり、画面と振り返りヘッダーに横方向のはみ出しがないことを確認しました。コピー操作は44px、スマートフォンのタグ操作は38pxの高さでした。
- ブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 実スマートフォンでのクリップボード権限、タッチ操作、OSの文字拡大、画面回転は未確認です。
- バックアップ書き出しは完了表示まで確認しましたが、自動操作環境でダウンロードイベントを取得できなかったため、今回生成したファイルそのものの再読み込みは未確認です。別途用意した現行形式のJSON読み込みは成功しています。
- 自動テスト用の設定とテストファイルは既存構成にないため、今回は型チェック、ビルド、実ブラウザ操作で確認しました。

#### 次回やること

- 利用者が普段使うスマートフォンで「AI用にコピー」からChatGPTなどへ貼り付ける一連の操作を確認します。
- 「まとめノート」は今回の改修へ含めず、次の段階で目的、保存方法、元記録との関係を決めてから検討します。

### 2026-07-17: ロゴのGitHub Pagesサブパス対応

#### 実施内容

- 公開中のGitHub Pagesでロゴが壊れた画像になる原因を確認しました。`/baseball-note-logo.svg` はドメイン直下の `https://seista-jp.github.io/baseball-note-logo.svg` へ解決され、GitHub Pagesの404になっていました。
- `public/baseball-note-logo.svg` が小文字・ハイフンを含めて指定名と完全一致し、SVGファイルとして存在することを確認しました。
- `vite.config.js` の `base: "./"` に合わせ、ロゴの読み込みを `${import.meta.env.BASE_URL}baseball-note-logo.svg` へ変更しました。
- PWAアイコン、Manifest、favicon、Service Worker、ロゴの寸法とレイアウトは変更していません。

#### 変更したファイル

- `src/App.tsx`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- ビルド済みJavaScriptではロゴURLが `./baseball-note-logo.svg` になり、`public` のSVGと同一内容の `dist/baseball-note-logo.svg` が生成されることを確認しました。
- ローカル開発環境 `http://127.0.0.1:5173/` では `http://127.0.0.1:5173/baseball-note-logo.svg` から読み込まれ、28×28pxで表示されました。
- GitHub Pages相当のサブパス `http://127.0.0.1:4173/baseball-note/` では `http://127.0.0.1:4173/baseball-note/baseball-note-logo.svg` から読み込まれ、28×28pxで表示されました。
- ローカル開発環境とサブパス環境のブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 修正版は未コミット・未プッシュのため、実際のGitHub Pagesでの修正後表示はまだ確認できません。コミット・PushとGitHub Actionsのデプロイ後に最終確認が必要です。

#### 次回やること

- 利用者のローカル確認後、指示を受けてコミット・Pushします。
- デプロイ後、公開URLが `https://seista-jp.github.io/baseball-note/baseball-note-logo.svg` へ解決され、ロゴが表示されることを確認します。

### 2026-07-17: オリジナルロゴをサイドメニューへ反映

#### 実施内容

- PCサイドバーとスマートフォンのドロワーで共用しているブランド表示を、CSS製の「ホームベース×B」仮ロゴから `public/baseball-note-logo.svg` へ置き換えました。
- SVGは `/baseball-note-logo.svg` から読み込み、装飾画像として空の代替テキストを設定しました。
- ロゴを28×28px、文字との間隔を既存の10px、縦中央揃えとし、ロゴが縮まない既存レイアウトを維持しました。
- `public/icon.svg`、`public/icon-192.png`、`public/icon-512.png`、`public/manifest.webmanifest`、`index.html` のfavicon指定は変更していません。

#### 変更したファイル

- `public/baseball-note-logo.svg`（利用者が配置済み）
- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- PC幅1,280×800pxで、ロゴ画像が `/baseball-note-logo.svg` から読み込まれ、28×28px、文字との間隔10px、縦中央のずれ0pxで表示されることを確認しました。既存のサイドバー幅260pxも維持しています。
- スマートフォン幅390×844pxと320×844pxでドロワーを開き、ロゴが28×28pxのまま縮まず、文字、閉じるボタン、4つのメニュー項目と自然に収まることを確認しました。
- 3つの確認幅すべてで画面の `clientWidth` と `scrollWidth` が一致し、横方向のはみ出しがないことを確認しました。
- PC・スマートフォンの確認中にブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- 実スマートフォンのタッチ操作、OSの文字拡大、画面回転は利用者環境での確認が必要です。
- PWAアイコンとfaviconへのロゴ反映は今回の対象外です。ロゴ表示の確認後、必要な場合に別作業として検討します。

#### 次回やること

- 利用者のローカル画面でロゴ表示を確認し、PWAアイコンも統一するか相談します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-17: サイドメニューへLucide Iconsと仮ロゴを追加

#### 実施内容

- `lucide-react` を依存関係へ追加し、サイドメニューの「振り返り」に `CalendarDays`、「検索」に `Search`、「バックアップ」に `Download`、「読み込み」に `Upload` を表示しました。
- 4アイコンを21px、`strokeWidth` 1.8へ統一しました。メニュー4項目も高さ48px、文字15px、行高21px、内側余白12px、アイコンと文字の間隔12pxへ統一しました。
- 選択中とホバー時にだけ薄い背景を表示し、キーボード操作用のフォーカス表示を追加しました。
- Baseball Noteの左へ、28pxのホームベース形状に白いBを置いた塗り仮ロゴをCSSで追加しました。小さい表示で形と文字を保つため、画像や線画ではなく単色のシルエットにしています。
- 振り返り、検索、バックアップ、読み込みの既存イベント処理、期間指定、IndexedDB、バックアップ形式、PCの画面構成には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `package.json`
- `package-lock.json`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで1,793モジュールを変換）
- PC幅1,280×800pxで、サイドバー幅260pxと既存画面構成を維持し、4つのSVGがLucideの `calendar-days`、`search`、`download`、`upload` であることを確認しました。全項目のアイコン、文字、余白、行高、間隔も同じ値になっています。
- スマートフォン幅390×844pxと320×844pxでドロワーを開き、ロゴと4項目が自然に収まること、項目の左位置と高さが一致すること、横方向のはみ出しがないことを確認しました。
- 28×28pxの仮ロゴでホームベースの輪郭と中央のBを目視できることを、PCとスマートフォンの両方で確認しました。
- PC・スマートフォンで「振り返り」から既存の期間選択カレンダーが開くこと、スマートフォンではドロワーが閉じることを確認しました。検索画面の表示、スマートフォンで検索時にドロワーが閉じることも確認しました。
- バックアップで「18件を書き出しました」と表示されること、読み込み用の非表示ファイル入力が従来どおりJSONだけを受け付けることを確認しました。
- PC・スマートフォンの確認中にブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- 実スマートフォンのタッチ操作、OSの文字拡大、画面回転は未確認です。
- バックアップは完了表示まで確認しましたが、今回生成されたファイルそのものの再読み込みは行っていません。書き出し・読み込み処理には変更を加えていません。
- 今回のロゴは仮デザインです。正式な形状、色、文字バランスのブラッシュアップは今後の作業として残します。

#### 次回やること

- 利用者の普段使うスマートフォンとPCで、アイコンと仮ロゴの大きさ、余白、タップ時の印象を最終確認します。
- 仮ロゴの改善方針が決まったら、現在の認識しやすさを維持して正式版へ更新します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-17: サイドメニューを文字中心の4項目へ整理

#### 実施内容

- PCサイドバーとスマートフォンのドロワーメニューから「今日」と日付入力を削除しました。
- 上部のBマークと、各メニュー項目の記号・文字アイコンを削除し、「振り返り」「検索」「バックアップ」「読み込み」の4項目を文字だけで表示するようにしました。「Baseball Note」とスマートフォン用の「閉じる」は維持しています。
- メニューの「記録を振り返る」を「振り返り」へ変更し、既存の `showRecordReview` をそのまま使用して期間選択カレンダーを開く構造を維持しました。
- 4項目を高さ48px、文字15px、行間1.5、上下10px・左右12pxの余白へ統一しました。選択中の項目はカード状の背景や枠を付けず、文字の太さだけで示すようにしました。
- 検索、バックアップ書き出し、JSON読み込みのイベント処理と非表示ファイル入力、振り返りの期間選択、IndexedDB、バックアップ形式には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- PC幅1,280×800pxで、メニューが4項目だけであること、全アイコンと「今日」「日付」がないこと、各項目の左位置・高さ・文字サイズ・行間・余白が一致すること、選択中項目にカード状の背景・枠・影がないことを確認しました。
- スマートフォン幅390×844pxと320×844pxで、Baseball Noteと「閉じる」が残っていること、4項目の位置と寸法が揃っていること、画面全体に横スクロールがないことを確認しました。
- 「振り返り」を押すとPC・スマートフォンとも既存の期間選択カレンダーが開き、スマートフォンではドロワーが閉じることを確認しました。
- 2026年7月10日を開始日・終了日として同じ日を2回選び、その1日だけの期間表示になることを確認しました。2026年7月9日〜7月12日の複数日指定と期間表示も確認しました。
- 検索画面を開き、実データを「右足」で検索して1件へ絞り込まれることを確認しました。検索を閉じると今日の日別画面へ戻ることも確認しました。
- バックアップを押して「1件を書き出しました」と表示されること、読み込みを押せること、JSONだけを受け付ける既存ファイル入力が維持されていることを確認しました。
- PC・スマートフォンの確認中に横方向のはみ出し、ブラウザコンソールの警告・エラーはありませんでした。

#### 残っている問題

- ブラウザ自動操作環境ではバックアップのダウンロードイベントを取得できなかったため、今回生成されたファイルそのものの再読込は行っていません。実JSONの書き出し・読み込みは2026-07-13に利用者操作で確認済みで、今回は関連処理を変更していません。
- 実端末のタッチ操作、OSの文字拡大、画面回転は今回も未確認です。

#### 次回やること

- 利用者のローカル画面で、普段使うスマートフォンとPCのメニュー表示・操作感を最終確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-16: 振り返りの期間選択を1操作短縮

#### 実施内容

- メニューの「記録を振り返る」を押した時点では画面を切り替えず、現在の画面上へ期間選択カレンダーを直接開くようにしました。
- 「この期間を表示」で期間を確定した場合だけ、選択期間を保存して振り返り一覧へ移動するようにしました。
- カレンダーをキャンセルした場合は画面状態を変更しないため、追加の復帰用stateを持たずに直前の日別画面、検索画面、振り返り一覧へ戻る構造にしました。
- 日別画面に未保存の編集がある場合も、カレンダーを開く時点では編集状態を保持し、期間を確定して実際に移動する時点でだけ既存の未保存確認を行うようにしました。移動を取りやめた場合はカレンダーを閉じて編集画面へ戻ります。
- 振り返り一覧上部の「期間を選ぶ」を「期間を変更」へ変更しました。記録一覧のDOM、レイアウト、CSS、データ取得、カレンダー本体には変更を加えていません。

#### 変更したファイル

- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- PC表示で、日別画面のメニューからカレンダーが直接開き、背面が日別画面のままであること、キャンセル後も同じ日別画面が表示されることを確認しました。
- 期間確定後だけ振り返り一覧へ移動し、上部に「期間を変更」が表示され、既存の記録項目と日付グループが維持されることを確認しました。
- 「期間を変更」から開いたカレンダーをキャンセルすると、同じ期間と記録一覧が維持されることを確認しました。
- 検索画面からメニューでカレンダーを開き、キャンセル後も検索画面へ戻ることを確認しました。
- 日別画面で本文を未保存のままカレンダーを開いてキャンセルし、編集本文と保存バーが維持されることを確認しました。確認用の変更は保存せずキャンセルし、既存記録を元の状態に戻しました。
- スマートフォン幅390×844pxで、ドロワーメニューの「記録を振り返る」を押すとドロワーが閉じてカレンダーが直接開き、キャンセル後は元の検索画面へ戻ることを確認しました。画面の横スクロールとブラウザコンソールの警告・エラーはありませんでした。
- 自動テストコマンドとテストファイルは既存構成にないため、今回は型チェック、ビルド、ブラウザ操作で確認しました。

#### 残っている問題

- なし

#### 次回やること

- 利用者のローカル画面で、普段の操作感として1操作短縮されていることを確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-16: 週間記録を自由期間の「記録を振り返る」へ変更

#### 実施内容

- メニューと画面名を「記録を振り返る」へ変更し、「今週」「前の週」「次の週」などの週単位の操作と表示を削除しました。
- 開始日を選んでから終了日を選ぶ期間選択カレンダーを追加しました。月の前後移動、未来日の無効化、選択途中の案内、開始日・終了日の強調、期間内の日付の色付き表示、同日選択、開始日より前を押した場合の選び直しに対応しました。
- 期間の確定は「この期間を表示」だけで行い、「キャンセル」とカレンダー外側の操作では確定しない構造にしました。
- 初回は期間を自動選択せず、「期間を選んで、記録を振り返りましょう」と表示します。確定した期間は `baseball-note-record-review-range` をキーとして `localStorage` に保存し、再表示時に復元します。
- Dexieの既存 `date` インデックスへ、開始日と終了日の両方を含む範囲検索を行うよう変更しました。保存済みの `YYYY-MM-DD` の日付キー同士を比較するため、時刻やUTC変換を介さずローカル日付の両端を保持します。
- 記録の日付順・同日内の作成時刻順、日付ごとのグループ、通常一覧と同じ2列構成、画像サムネイル、記録から日別画面への移動を維持しました。
- 週の開始日計算、週範囲表示、前週・次週移動、週間専用stateとCSSを削除し、振り返り用途の名前へ整理しました。IndexedDBスキーマ、メモ型、画像形式、バックアップ形式、新しい依存関係は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/RecordReviewCalendar.tsx`（新規）
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで36モジュールを変換）
- 既存の自動テストコマンドとテストファイルはありません。代わりに日付処理を一時コンパイルして、日・月・年の繰り上がり、実在日付判定、月カレンダー、両端を含む比較の10ケースを確認しました。
- PC幅1,280×800px、スマートフォン幅390×844pxと320×844pxで、振り返り画面とカレンダーに横スクロールがなく、期間選択ボタン44px、カレンダー日付42px以上、確定ボタン48pxの操作領域があることを確認しました。
- 初回案内、開始日・終了日の自由選択、同日だけの選択、月またぎ、2025年12月31日〜2026年1月1日の年またぎ、開始日より前を押した選び直し、未来日と未来月の無効化、キャンセル、カレンダー外側で確定されないことを確認しました。
- 期間の確定後に画面を再読み込みし、前回の期間が復元されることを確認しました。記録がない2026年6月20日だけを選び、「この期間の記録はありません」と「期間を変更する」が表示されることも確認しました。
- 2026年7月6日と7月16日にテスト記録を作り、7月6日〜7月16日を選ぶと開始日・終了日の両方が日付順で表示されることを確認しました。通常画面で新規作成、本文の直接編集、削除も確認し、作成した2件は削除して既存記録を保持しました。
- ブラウザコンソールに警告・エラーはありませんでした。

#### 残っている問題

- 今回は新しい実画像ファイルの選択操作を再実施していません。振り返り画面は既存の `ImagePreview` と保存画像をそのまま再利用し、表示用クラス名と最大サムネイルサイズだけを維持する構造です。実画像付き記録の保存・再表示自体は2026-07-13に確認済みですが、変更後の画面での最終目視は利用者環境でも確認します。
- 実端末のタッチ操作、OSの文字拡大、画面回転は未確認です。

#### 次回やること

- 利用者のローカル画面で、実画像付き記録、実スマートフォンのタップ操作、PC表示の最終的な余白を確認します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-15: 週間記録を通常一覧に合わせてコンパクト化

#### 実施内容

- 週間記録の各メモを、通常一覧と同じ左列に時刻とタグ、右列に本文と画像を置く2列構成へ変更しました。
- 通常一覧の `log-entry`、`log-meta`、`log-content`、`log-body` を週間記録でも再利用し、PCの左列72px・列間14px、スマートフォンの左列62px・列間10px、本文幅、文字サイズ、行間、上下余白を揃えました。
- 週間記録のカード用の四辺の枠線、角丸、白背景を廃止し、各メモの下に薄い区切り線だけを表示するようにしました。日付見出しは維持し、日付グループ間の余白を縮めました。
- 画像表示部品に週間記録用の表示バリエーションを追加し、縦横比を維持したままPCでは最大160×100px、スマートフォンでは最大120×80pxのサムネイルとして表示するようにしました。通常一覧の画像サイズは変更していません。
- 記録全体を押して日別画面へ移動する操作、キーボードのフォーカス表示、週移動、日付指定、並び順、データ取得、IndexedDB、バックアップ形式は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- コード確認で、週間記録が通常一覧のレイアウト用クラスを再利用し、画像だけ週間用の最大サイズが適用される構造になっていることを確認しました。

#### 残っている問題

- PC幅とスマートフォン幅での最終的な見た目、画像サムネイルの大きさ、記録全体を押した日別遷移は、利用者のローカル画面での確認が必要です。

#### 次回やること

- 利用者のローカル画面確認結果に応じて余白や画像サイズを調整します。
- コミットとPushは利用者の確認と指示を受けてから行います。

### 2026-07-13: メモ一覧の直接編集と時刻下タグ表示

#### 実施内容

- 日別メモ一覧を、左列に時刻と保存済みタグ、右列に本文と画像を置く2列構成へ変更しました。通常表示の本文下にはタグ行を作らず、編集・削除アイコンも表示しません。
- 本文を押すと同じ位置へ自動伸長する `textarea` を表示し、タップした文字位置へカーソルを移すようにしました。標準の `caretPositionFromPoint` とSafari向けの `caretRangeFromPoint` を使い、新しいライブラリは追加していません。
- 本文はEnterで改行でき、メモの編集開始はEnterまたはSpace、編集終了はEscape、変更の保存はCtrl/Cmd+Enterでも操作できるようにしました。一度に編集できるメモは1件です。
- 本文またはタグが実際に変わった場合だけ、既存の新規入力欄の直上へ「キャンセル」「保存」の保存バーを表示するようにしました。保存失敗時は既存のエラー案内を使い、編集内容と編集状態を残します。
- 元々本文があるメモを空にして保存した場合は「本文が空です。このメモを削除しますか？」と確認し、了承された場合だけ既存のDexie削除処理を使うようにしました。画像だけのメモを含め、編集状態から削除できるテキスト操作も残しました。
- 未保存変更がある状態で、日付移動、検索・週間記録への移動、別メモの編集、バックアップ読み込みを行う場合だけ確認するようにしました。メニューを開閉するだけでは確認せず、編集内容を保持します。ブラウザ終了・再読み込みには `beforeunload` の標準警告を登録しました。
- 初回だけ「メモをタップすると編集できます」と表示し、閉じた状態または本文を編集したことを `localStorage` へ保存するようにしました。この設定はIndexedDBとJSONバックアップには含まれません。
- スマートフォンでは可視領域に追従する `100dvh` の画面構成にし、保存バーを新規入力欄の上へ独立した行として配置しました。320px幅で編集用タグが横にはみ出さないよう、編集時だけ3列2行にしました。
- `LogEntry`、IndexedDBスキーマ、JSONバックアップ形式、画像形式、検索条件、週間記録、PWA、公開設定、バージョン番号は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- PC幅1,280×800px: 時刻と本文先頭の上端が一致し、左72pxの時刻・タグ列と右680pxの本文列が重ならないこと、通常時の編集・削除アイコンが0件であること、長文・改行・タグ6件・区切り線・新規入力欄を確認しました。
- スマートフォン幅390×844px: 左62pxの時刻・タグ列と本文286pxが重ならず、長文が自然に折り返すこと、画面全体の横スクロールがないことを確認しました。
- スマートフォン幅320×844px: 本文216px、編集欄、3列2行の編集タグ、削除操作、保存バー、新規入力欄が画面内に収まり、一覧の `clientWidth` と `scrollWidth` がともに320pxであることを確認しました。
- 本文を押した文字位置へカーソルが移り、その位置で日本語を追記できること、改行、長文に応じた編集欄の伸長、Enterキーでの編集開始、Escapeキーでの編集終了を確認しました。
- 本文を押しただけでは保存バーがなく、文字変更後だけ表示されること、保存後に本文へ反映されること、キャンセル後に変更前の本文へ戻ることを確認しました。
- 本文を空にして保存した場合の削除確認について、確認のキャンセルでは空の編集内容とメモが残り、確認の了承では削除されて件数が更新されることを確認しました。
- 編集状態の削除操作についても、削除確認のキャンセルではメモが残り、了承した場合だけ削除されることを確認しました。
- 未保存のまま検索へ移動する確認を表示し、キャンセルでは編集内容を保持、了承では変更を保存せず検索画面へ移動することを確認しました。メニューの開閉だけでは未保存内容が残ることも確認しました。
- 初回案内を閉じて再読み込みした後に再表示されないことを確認しました。
- 既存機能の回帰確認として、新規メモ入力、キーワード検索、週間記録、バックアップ書き出し完了表示を確認しました。ブラウザ操作環境ではダウンロードイベントを取得できませんでしたが、アプリ上では対象1件の書き出し完了が表示されました。
- ブラウザコンソールの警告・エラーはありませんでした。
- コード確認: IndexedDB保存・削除の失敗時は編集内容または対象メモを残して既存のエラー通知を表示すること、画像表示、バックアップ読込、エラー通知、PWA関連の処理を変更していないことを確認しました。
- 画面確認で作成した当日分のテストメモはすべて削除し、確認前の0件へ戻しました。

#### 残っている問題

- 実スマートフォンでソフトウェアキーボードを表示した状態の保存バー操作は未確認です。`100dvh` と一覧内スクロールで追従する構造は実ブラウザの指定幅で確認しています。
- ブラウザ操作環境の再読み込みAPIはページの `beforeunload` 確認を表示しなかったため、実ブラウザを閉じる・再読み込みする際の標準警告は利用者環境での確認が必要です。
- IndexedDBの保存・削除失敗は実ブラウザでは発生させていません。失敗時に編集内容とメモを残す分岐はコードで確認しています。

#### 次回やること

- 利用者のスマートフォンで、日本語入力中の改行、保存バー、実ブラウザの再読み込み警告を確認します。
- 利用者の実ブラウザ確認が完了するまではコミットとPushを行いません。

### 2026-07-13: スマートフォンのメモ一覧をコンパクト化

#### 実施内容

- 日別メモ一覧の本文・画像の下を、左側に保存済みタグ、右側に編集・削除操作を置く1行構成へ変更しました。
- 保存済みタグ側は折り返し可能、操作側は縮まない構造にし、タグが多い場合も操作ボタンと重ならないようにしました。
- 通常表示時の編集を鉛筆、削除をゴミ箱のインラインSVGアイコンへ変更しました。新しいライブラリは追加していません。
- アイコンを17px、各ボタンの押せる範囲を44×44pxとし、2ボタン間に6px、タグ側との間に12pxの間隔を設けました。
- ボタン要素、時刻を含む編集・削除の `aria-label`、キーボード操作時のフォーカス表示を維持しました。
- 本文からタグ・操作行までの間隔を8pxにし、メモ間の間隔と下余白を狭めました。メモ同士は薄い区切り線で判別できるようにしました。
- 編集・削除処理、削除確認、保存形式、IndexedDB、バックアップ、検索、週間記録、画像、エラー通知、ヘッダー、入力欄は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- スマートフォン幅390×844px: 短いメモ、長いメモ、タグ1件、タグ3件、タグ6件を表示し、横方向のはみ出しがないことを確認しました。
- スマートフォン幅320×844px: 横方向のはみ出しがなく、タグ右端と操作部の間に12pxの間隔があり、操作部が押しつぶされないことを確認しました。
- タグ6件は390px幅で2行に折り返し、編集・削除操作と重ならないことを確認しました。
- PC幅1,280×800px: PC向けサイドバーと入力欄を維持し、本文、タグ、操作部に横方向のはみ出しや不自然な重なりがないことを確認しました。
- 各編集・削除ボタンの実測値は44×44px、SVGアイコンは17×17pxでした。
- 鉛筆アイコンから編集画面を開き、タグを追加して保存できることを確認しました。
- ゴミ箱アイコンからテストメモを削除し、件数が2件から1件、0件へ更新されることを確認しました。ブラウザ操作環境では確認ダイアログが自動処理されたため文言の目視確認はできませんでしたが、既存の `window.confirm` 処理を変更していないことをコードで確認しました。
- 画面確認後、作成したテストメモはすべて削除し、確認前の0件へ戻しました。
- ブラウザコンソールの警告・エラーはありませんでした。
- 差分確認: データ操作とエラー通知の処理に変更がなく、通常表示時のDOMとメモ一覧用CSSだけが変更されていることを確認しました。

#### 残っている問題

- なし

#### 次回やること

- 高優先TODOの「主要ロジックとデータ操作の自動テストを追加する」へ進みます。

### 2026-07-13: データ操作のエラー通知を統一

#### 実施内容

- 実画像付きメモの保存、日別・週間表示、アプリを開き直した後の画像保持、実JSONの書き出し・読み込み、同じデータの上書き案内、画像とタグを含む復元が、利用者の実操作ですべて問題なく動作したことを引き継ぎました。
- 日別画面、検索画面、週間記録のIndexedDB読み込みを例外処理で囲み、失敗時は空の記録と誤認させず、再読み込みや画面の開き直しを案内するようにしました。
- メモの新規保存と編集に失敗した場合は、入力・編集内容が残っていることを案内するようにしました。
- 保存容量不足を通常の保存失敗と区別し、先にバックアップを書き出してから不要な画像付きメモを整理する案内を追加しました。
- 削除に失敗した場合はメモを画面から消さず、「メモは残っています」と表示するようにしました。
- バックアップの書き出し・読み込みに失敗した場合は、スマートフォンでメニューを閉じた後でも確認できる共通エラー通知を表示するようにしました。
- 共通エラー通知には閉じる操作と `role="alert"` を追加しました。通知は失敗した場合だけ描画され、正常時の画面構成は変更していません。
- IndexedDBスキーマ、保存データ、バックアップ形式、画像処理、検索条件、週間記録の仕様は変更していません。新しいライブラリも追加していません。

#### 変更したファイル

- `src/App.tsx`
- `src/dataError.ts`（新規）
- `src/styles.css`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで35モジュールを変換）
- 容量不足判定: Dexieを想定した入れ子の `QuotaExceededError`、通常エラーの誤検出防止、容量不足の案内文、通常時の代替文言の4ケースが成功しました。
- コード確認: 日別・検索・週間の読み込みでは、失敗時にもローディング表示を終了し、各画面内に日本語の案内が表示される構造を確認しました。
- コード確認: 削除はIndexedDBから削除できた場合だけ画面から除去し、失敗時は対象メモを残す構造を確認しました。
- コード確認: 共通通知と読み込みエラー表示は、エラー文言がある場合だけ描画されるため、正常時の画面構成に新しい要素は追加されません。
- レスポンシブ確認: エラー通知はPCではサイドバーを避け、760px以下ではスマートフォンヘッダーの下へ表示するCSSになっていることを確認しました。
- 本番ビルド済み画面をPC幅・スマートフォン幅で開くためローカルプレビューを起動しましたが、実行環境のローカルポート待受制限で起動できませんでした。ビルド済みファイルの直接表示もブラウザの安全制限で拒否されたため、変更後の実画面確認は未実施です。

#### 残っている問題

- 変更後のエラー通知について、PC幅とスマートフォン幅での実表示、長い案内文の折り返し、閉じる操作の実画面確認が残っています。
- IndexedDBや保存容量の実際の失敗をブラウザ上で発生させた確認は未実施です。

#### 次回やること

- ローカル画面を開ける環境で、PC幅とスマートフォン幅のエラー通知を確認します。
- 高優先TODOの「主要ロジックとデータ操作の自動テストを追加する」へ進みます。

### 2026-07-11: 週間記録画面の追加

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` と現在のコードを確認し、既存の設計、未コミット変更、開発ルールを維持して実装しました。
- 三本線メニューとPCサイドバーへ「週間記録」を追加しました。
- 月曜日から日曜日までを1週間とし、Dexieの既存 `date` インデックスから対象期間の記録だけを読み込む週間記録画面を追加しました。
- 記録を日付の古い順、同じ日では作成時刻の古い順に並べ、日付ごとにタグ、本文全文、保存画像を表示する構造にしました。
- 前の週・次の週への移動、今週より未来への移動防止、期間表示からの日付指定に対応しました。
- 週間記録のメモを押すと、編集画面ではなく対象日の日別画面へ移動し、対象メモを一時的に強調する既存処理を再利用しました。
- 記録がない週には「この週の記録はありません」と表示します。
- バックアップ読込を週間画面で行った場合も、対象週の一覧を即時更新するようにしました。
- AI、外部API、新しい要約処理、新しい保存データは追加していません。IndexedDBスキーマとバックアップ形式も変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで34モジュールを変換）
- 週計算: 土曜・日曜・月曜、月をまたぐ日付、期間表示、曜日見出しの6ケースが想定どおりになりました。
- PC幅: サイドバーの「週間記録」から今週を開き、`7/6〜7/12`、月曜日の日付見出し、保存済み本文、無効な次週ボタンを確認しました。
- スマートフォン幅390px: 三本線メニューから週間記録を開き、専用画面の表示、無効な次週ボタン、入力欄が表示されないこと、横方向のはみ出しがないことを確認しました。
- スマートフォン幅320px: 週移動3列、本文カード、画面全体に横方向のはみ出しがないことを確認しました。
- 週移動: 前週へ移動すると `6/29〜7/5` と空週案内が表示され、次週ボタンで戻れる状態になることを確認しました。
- 日付指定: 2026-06-18を指定すると、その日を含む月曜日から日曜日の `6/15〜6/21` が表示されました。
- 日付順・複数件・タグ: 7月6日の既存記録と7月11日のテスト記録2件が古い日付順に並び、同日の2件と「打撃」「守備」タグがすべて表示されました。
- 日別遷移: 7月6日の週間メモを押すと `2026/07/06` の日別画面が開きました。
- 既存操作: 日別画面でテスト記録2件の作成・保存・削除を行い、確認後に削除しました。
- 画像表示: 既存の `ImagePreview` を週間カード内でも使用し、画像幅をカードと画面幅以内に制限する既存CSSが適用される構造を確認しました。
- ブラウザコンソールエラーはありませんでした。

#### 残っている問題

- ブラウザ操作環境でローカル画像ファイルを選択できないため、実画像を保存した記録の週間画面表示は実操作で未確認です。既存の画像表示部品を変更せず再利用しています。

#### 次回やること

- ローカルファイル選択を操作できる環境で、画像の保存・日別表示・週間表示を一連で確認します。
- 高優先TODOのデータ操作エラー通知または自動テスト追加へ進みます。

### 2026-07-11: バックアップ読込検証の強化と画面回帰確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を確認し、仕様、前回作業、残件、開発ルールを引き継ぎました。
- バックアップのバージョン、必須項目、実在日付、ID、画像Data URL、タグ、メモID重複を、IndexedDBへの書き込み前に検証する処理を追加しました。
- 過去データで画像またはタグが省略されている場合は、従来どおり空配列へ補完します。
- 読込前に新規件数と上書き件数を表示し、利用者が確認した場合だけ読み込むようにしました。
- 画像変換を含む全データの準備後、Dexieトランザクション内で一括反映するようにしました。
- 不正ファイル、未対応バージョン、利用者による中止では、理由と既存データを変更していないことを画面に表示します。

#### 変更したファイル

- `src/backup.ts`（新規）
- `src/App.tsx`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで34モジュールを変換）
- 検証ロジック: 正常なバージョン1、画像・タグを省略した旧形式、未対応バージョン、不正日付、不正ID、不正画像Data URL、不正タグ、重複IDの8ケースが想定どおり成功または停止しました。
- PC幅1,280×800: サイドバーと通常ヘッダーが表示され、横スクロールがないことを確認しました。
- スマートフォン幅320・390・420・760px: 1行ヘッダーが表示され、横スクロールがないことを確認しました。390×844では実画面も目視確認しました。
- ブラウザ操作: テストメモの作成、タグ付与、本文・タグ編集、キーワード検索、削除を確認し、作成したテストメモは削除しました。
- バックアップ書き出し: 画面に対象件数の完了表示が出ることを確認しました。ブラウザ操作環境ではダウンロードイベントを取得できなかったため、保存ファイルそのものは未確認です。
- ブラウザコンソールエラーはありませんでした。

#### 残っている問題

- ブラウザ操作環境でローカルファイルを選択できないため、画像の縮小・保存・再表示と、正常・不正なJSONファイルを選択した読込画面の一連の操作は未確認です。
- 上記が残るため、`TODO.md` の「主要操作をブラウザで確認する」と「バックアップ読み込み時のデータ検証を強化する」は未完了のままです。

#### 次回やること

- ローカルファイル選択を操作できる環境で画像とバックアップ復元を確認します。
- バックアップ読込で新規・上書き件数、確認の中止、検証エラー理由、正常復元を実画面で確認し、問題がなければ該当TODOを完了にします。

### 2026-07-11: スマートフォン上部UIの最終1行化

#### 実施内容

- スマートフォンヘッダーからBアイコンを外し、メニューボタン、Baseball Note、前日、短縮日付、今日表示、翌日の順に1行で配置しました。
- 「今日」は選択日が今日の場合だけ表示し、過去日では表示しないようにしました。
- 検索画面では日付操作全体を描画せず、メニューボタンとBaseball Noteだけを表示する構造にしました。
- 760px以下では従来の `.topbar` を非表示にし、区切り線、件数、日付操作、余白を画面から除きました。
- スマートフォン用の「今日のログ／過去のログ」は、画面には表示されないスクリーンリーダー向け `h1` として残しました。
- 前後移動には既存の `moveSelectedDate` を使用し、今日の翌日ボタンを無効のまま維持しました。
- PC表示、メモ一覧、下部入力欄、データ・検索・編集・削除・タグ・画像・バックアップ処理は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）
- コード確認: 今日では `M/D 今日` と無効な翌日ボタン、過去日では `M/D` と有効な翌日ボタンになる条件を確認しました。
- コード確認: 検索画面では日付操作をレンダリングせず、通常画面へ戻ると選択日に応じた日付操作が再表示される構造を確認しました。
- コード確認: 0件・複数件にかかわらずスマートフォンの件数表示はなく、メモ一覧とデータ処理のコードに変更がないことを確認しました。
- 幅確認: 760px以下は44px、可変幅、固定幅の3列です。420px以下と340px以下で余白を段階的に狭め、タイトルは18pxのまま省略可能、日付操作と40pxの前後ボタンは縮まない指定にしました。
- PC確認: スマートフォン専用要素は非表示で、従来の `.topbar`、年月日、件数、36pxの前後ボタンを維持する構造を確認しました。
- 実ブラウザ確認: 実行環境のローカルサーバー待受制限とローカルファイル表示制限により実施できませんでした。

#### 残っている問題

- 320px、390px、420px、760px、PC幅での実画面表示、横スクロール、タップ操作の確認が残っています。
- 今日、過去日、0件、複数件、検索画面、検索からの復帰、前日・翌日移動の実ブラウザ回帰確認が残っています。

#### 次回やること

- ローカル画面を開ける環境で指定幅と状態を実確認します。
- 問題があればスマートフォン上部UIのCSSだけを必要最小限修正します。

### 2026-07-11: スマートフォン上部UIのコンパクト化

#### 実施内容

- 760px以下のスマートフォンヘッダーへ、選択日を `M/D` 形式で表示する短縮日付を追加しました。
- 検索画面では短縮日付を表示しないようにしました。
- スマートフォンの日付操作を、前日ボタン、ログ見出し、翌日ボタン、件数の1行表示にしました。
- スマートフォンでは年月日の大きな表示を隠し、「今日のログ」または「過去のログ」を `h1` 内の見出しとして表示するようにしました。
- 今日の翌日ボタンを無効化し、日付入力の最大値を今日に制限しました。
- PC向けの年月日表示と配置、メモ一覧、編集・削除、下部入力欄は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/date.ts`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）
- 差分確認: `selectedDate`、`moveSelectedDate`、日付別のIndexedDB読み込み、保存データ型、編集・削除・タグ・画像・検索処理を変更していないことを確認しました。
- コード確認: 760px以下だけでコンパクト表示が有効になり、PCでは従来の年月日見出しが表示される構造を確認しました。
- コード確認: 320px幅では左右12pxの余白、40pxの前後ボタン、縮小可能な中央見出し、固定幅の件数表示となり、ヘッダーは44px、可変幅ブランド、固定幅短縮日付の3列となる構造を確認しました。
- 実ブラウザ確認: 実行環境でローカルサーバーのポート待受が許可されず、ビルド済みファイルの直接表示もブラウザの安全制限で拒否されたため実施できませんでした。

#### 残っている問題

- 320px、390px、420px、760px、PC幅での実表示と横スクロール、タップ領域の実画面確認が残っています。
- 今日、過去日、0件、複数件、検索からの復帰、前後移動、編集、削除、タグ、画像、日付入力のブラウザ回帰確認が残っています。
- 過去に未来日へ保存したデータや、未来日を含むバックアップを検索結果から開く場合の扱いは、既存データを隠さないため今回変更していません。通常の日付入力と前後ボタンから未来日へ進むことはできません。

#### 次回やること

- ローカル画面を開ける環境で指定された画面幅と操作状態を確認します。
- 表示上の問題が見つかった場合は、上部UIのCSSだけを必要最小限修正します。

### 2026-07-11: 引き継ぎと主要操作の追加確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を確認し、現在の仕様、前回までの作業、残件、開発ルールを引き継ぎました。
- ローカル開発画面でテスト用メモを作成し、編集、日付移動、検索、タグ絞り込み、削除を確認しました。
- 確認後にテスト用メモを削除し、作業前のデータ状態へ戻しました。

#### 変更したファイル

- `DEVLOG.md`

#### 確認結果

- PC幅1,280×800: 本文と「打撃」タグを付けたメモを作成でき、一覧へ本文とタグが表示されました。
- スマートフォン幅390×844: メモ本文の編集、「守備」タグの追加、前日・今日への移動、ドロワーメニューからの検索画面表示を確認しました。
- 検索: キーワードで対象メモが1件に絞られ、該当しない「投球」タグを追加すると0件表示になることを確認しました。
- 削除: テスト用メモを削除し、当日の件数が0件へ戻ることを確認しました。
- ブラウザコンソールエラーはありませんでした。
- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）

#### 残っている問題

- ローカルファイル選択を伴う画像の縮小・保存・再表示と、バックアップの書き出し・復元は今回のブラウザ操作環境では確認できていません。
- 上記が残っているため、`TODO.md` の「主要操作をブラウザで確認する」は未完了のままです。

#### 次回やること

- ローカルファイル選択を操作できる環境で画像とバックアップ入出力を確認し、主要操作確認を完了します。
- 完了後、バックアップ読み込み時のデータ検証強化へ着手します。

### 2026-07-11: 引き継ぎと公開画面のレスポンシブ確認

#### 実施内容

- `PROJECT.md`、`DEVLOG.md`、`TODO.md`、`RULES.md` を読み、現在の仕様・作業履歴・優先順位・開発ルールを確認しました。
- 公開中のGitHub Pagesで、タグUI整理後の通常画面と検索画面を確認しました。
- PC幅1,280×800とスマートフォン幅390×844でレスポンシブ表示を確認しました。

#### 変更したファイル

- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- 公開画面に通常画面上部の常設タグ絞り込みがなく、最新の変更がGitHub Pagesへ反映されていることを確認しました。
- PC幅ではサイドバー、スマートフォン幅では三本線メニューが表示されました。
- 両方の画面幅で横方向のはみ出しはありませんでした。
- 検索画面にキーワード入力欄と折りたたみ式のタグ絞り込みがあり、6種類のタグが表示されました。
- 表示確認中のブラウザコンソールエラーはありませんでした。
- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換）

#### 残っている問題

- 実データを使ったメモの作成・編集・削除、タグ検索、画像、バックアップ入出力の一連の操作確認が残っています。
- GitHub Actionsの実行履歴そのものは今回確認していません。

#### 次回やること

- テスト用データを使い、主要操作をPC幅とスマートフォン幅で一通り確認します。
- 確認で問題が見つかった場合は、仕様を変えずに修正し、型チェック・ビルド・文書更新まで行います。

### 2026-07-11: 本文を主役にしたタグUIへの整理

#### 実施内容

- 通常画面上部に常時表示されていたタグ絞り込みを削除しました。
- キーワード検索とタグ絞り込みを検索画面へ集約しました。
- タグ絞り込みを再利用可能な `TagFilter` 部品にし、将来ホーム画面にも配置しやすい構成にしました。
- 保存済みメモの表示順を本文、画像、控えめなタグ、編集・削除へ変更しました。
- 入力時と編集時の任意タグ選択、保存済みタグ、検索対象としてのタグを維持しました。
- IndexedDBの型・スキーマとバックアップ形式は変更していません。

#### 変更したファイル

- `src/App.tsx`
- `src/styles.css`
- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`

#### 確認結果

- `npm exec tsc -- --noEmit`: 成功
- `npm run build`: 成功（Viteで33モジュールを変換し、`dist` を生成）
- コード確認: 通常画面に常設タグ絞り込みがなく、検索画面ではキーワード単独・タグ単独・併用で絞り込める構造になっていることを確認しました。
- コード確認: メモ表示が本文、画像、タグの順であり、タグが11pxの控えめな色と背景になっていることを確認しました。
- データ互換性: `LogEntry`、Dexie定義、バックアップ入出力処理に変更がないことを確認しました。
- PC・スマートフォン表示: 開発サーバーの起動とビルド済み画面の表示を試みましたが、実行環境のローカルアクセス制限により実画面確認は実施できませんでした。

#### 残っている問題

- PC幅とスマートフォン幅での実画面確認が残っています。
- 実データを使った検索結果のタグ絞り込み操作確認が残っています。

#### 次回やること

- ローカル画面を開ける環境で、PC幅とスマートフォン幅の表示を確認します。
- キーワードのみ、タグのみ、キーワードとタグの併用、条件解除を実データで確認します。

### 2026-07-11: 長期開発向け文書の整備

#### 実施内容

- 現在の仕様と未確定の将来候補を `PROJECT.md` で分離しました。
- 設計方針、優先順位付きTODOの完了条件、開発ルールを明文化しました。
- 日付ごとに同じ形式で開発記録を残せるようにしました。

#### 変更したファイル

- `PROJECT.md`
- `DEVLOG.md`
- `TODO.md`
- `RULES.md`（新規）

#### 確認結果

- 指定された4つの文書だけを変更対象にしていることを確認しました。
- 現在の実装済み機能と将来候補が別の章に分かれていることを確認しました。
- アプリ本体のコードと依存関係は変更していません。
- 文書だけの変更のため、TypeScript型チェックとビルドは実施していません。

#### 残っている問題

- アプリの未完成・未確認項目は `TODO.md` に残っています。

#### 次回やること

- 次の開発作業を始める前に `PROJECT.md`、`TODO.md`、`DEVLOG.md` を確認します。
- 作業対象のTODOについて、完了条件と確認方法を先に決めます。

## 追記用テンプレート

以下をコピーし、`YYYY-MM-DD` と内容を書き換えて「日付別の開発記録」の先頭に追加します。

```markdown
### YYYY-MM-DD: 作業名

#### 実施内容

- 実際に行った作業を書く

#### 変更したファイル

- `変更したファイル名`

#### 確認結果

- 実行した型チェック、ビルド、テスト、画面確認とその結果を書く

#### 残っている問題

- 未解決の問題を書く。なければ「なし」と書く

#### 次回やること

- 次に着手する具体的な作業を書く
```
