# AGENTS.md

## 目的と役割

このファイルは、短い依頼から作業する場合でも、Baseball Note の既存機能、利用者データ、操作方法を守って安定して開発するための実行手順です。プロジェクト全体に適用します。

- このファイルでは、作業の開始から終了までの確認手順と、このプロジェクト固有の保護対象を定めます。
- TypeScript、レスポンシブ表示、依存関係、変更範囲などの詳細な開発規則は `RULES.md` に従います。
- 個別の依頼に明確な変更範囲がある場合は、その範囲を守ります。既存データや主要仕様への影響が不明な場合は、依頼を拡大解釈しません。

## 作業前の手順

1. `PROJECT.md`、`RULES.md`、`TODO.md`、`DEVLOG.md` を確認し、現在の仕様、開発ルール、優先順位、過去の経緯を把握します。
2. 変更前に、依頼に関係するファイルと処理を確認します。ファイル名だけで判断せず、呼び出し元、状態管理、データの読み書き、関連するスタイルも必要に応じて追います。
3. 現在のGit差分を確認し、利用者が作業中の変更や依頼外の変更を上書きしません。
4. 依頼の完了条件と確認方法を整理し、必要最小限の変更範囲を決めます。

主な確認先は次のとおりです。

- 画面、状態、CRUD、検索、画像、バックアップ: `src/App.tsx`
- 期間選択カレンダー: `src/RecordReviewCalendar.tsx`
- PC・スマートフォンの表示: `src/styles.css`
- 日付処理: `src/date.ts`
- IndexedDB: `src/db.ts`、`src/types.ts`
- バックアップ形式と検証: `src/backup.ts`
- データ操作エラー: `src/dataError.ts`
- 起動とService Worker登録: `src/main.tsx`
- ビルドや依存関係: `package.json`、`vite.config.js`、`tsconfig.json`

## 既存機能と操作方法の保護

- 既存機能を、明確な許可なく削除、無効化、別機能へ置き換えません。
- 既存のデザイン、文言、画面構成、操作手順を不用意に変更しません。特に、スマートフォン優先の表示、少ない操作で記録できる流れ、PC表示との両立を維持します。
- 短い依頼は、`PROJECT.md` に記載された現在の仕様を保つ最小の変更として解釈します。
- 依頼と無関係なリファクタリング、ファイル分割、命名変更、整形、依存関係更新を同時に行いません。
- `App.tsx` の責務分割は `TODO.md` の独立した作業です。別の依頼へ便乗して実施しません。

## 利用者データと互換性の保護

利用者の記録を最優先で保護します。次の項目は既存データとの契約として扱います。

- Dexieのデータベース名: `baseballNote`
- テーブル名: 通常メモの `logs`、AI分析の `aiAnalyses`
- 現在のIndexedDBスキーマ: バージョン2。`logs` は既存の `id, date, createdAt`、`aiAnalyses` は `id, createdAt` のインデックス
- メモの基本項目: `id`、`date`、`createdAt`、`text`、`images`、`tags`
- AI分析の基本項目: `id`、`startDate`、`endDate`、`tag`、`text`、`createdAt`
- バックアップ形式: 新規書き出しはバージョン2のJSON。バージョン1と2を読み込み可能
- 既存バックアップで省略された画像とタグを空配列として補完し、バージョン1で省略されたAI分析を空配列として扱う互換処理
- 振り返り期間のlocalStorageキー: `baseball-note-record-review-range`
- 本文の直接編集案内のlocalStorageキー: `baseball-note-inline-edit-hint-dismissed`

明確な許可と移行方針なしに、次の操作を行いません。

- IndexedDB、localStorage、既存レコードの削除、初期化、一括上書き
- データベース名、テーブル名、localStorageキーの変更
- 保存項目の削除、型や意味の互換性を壊す変更
- バックアップのバージョンやJSON構造の互換性を壊す変更
- 既存データを読めなくするスキーマ変更

保存形式を変更する必要がある場合は、既存データの移行方法、旧バックアップの読込可否、失敗時の復旧方法を先に示し、実装前に確認を求めます。

## 実装時の方針

- `RULES.md` と既存コードの型・構造・命名に合わせます。
- 既存の処理を再利用できる場合は再利用し、同じ目的の別実装を不用意に増やしません。
- UI変更では、通常画面、検索、振り返り、メニュー、未保存編集の流れへの影響を確認します。
- データ操作の変更では、作成、読込、編集、削除、検索、振り返り、バックアップ書き出し・読込への影響を確認します。
- 新しいライブラリ、外部API、クラウド保存、認証、同期機能は、明確な依頼と事前確認なしに追加しません。
- 不明点があり、判断によって仕様、操作方法、データ互換性が大きく変わる場合は、推測で変更せず、影響範囲を説明して質問します。

## 作業後の確認

- コードを変更した場合は、原則として `npm run build` を実行します。このコマンドにはTypeScriptの型チェックとViteのプロダクションビルドが含まれます。
- テストが追加済み、または依頼でテスト方法が指定されている場合は、関係するテストも実行します。
- UIを変更した場合は、PC幅とスマートフォン幅の両方で、表示崩れ、横方向のはみ出し、主要操作を確認します。
- データ処理を変更した場合は、関係する作成・読込・編集・削除と、IndexedDB・バックアップ互換性を確認します。
- 文書だけを変更した場合は、内容、リンク、Markdown構造、Git差分を確認します。
- 実行環境などの理由で確認できない項目は、成功したと記載せず、未確認の内容と理由を明記します。

## コミット前の標準手順

アプリのコードや画面を変更した場合は、利用者が毎回あらためて依頼しなくても、次の順番を標準として実施します。

1. 依頼範囲の実装を完了する。
2. `npm run build` と、利用できる関係テストを実行する。
3. Git差分を確認し、必要に応じてAIコードレビューを行う。
4. PC幅とスマートフォン幅で実画面を開き、主要表示、横方向のはみ出し、ブラウザの警告・エラーを確認する。
5. スマートフォン幅の確認画像を利用者へ提示し、確認結果とコミット用Summary候補を短く報告する。
6. 利用者の明確な承認または依頼を受けてから、コミット、Push、公開を行う。
7. Pushや公開を行った場合は、GitHub Actionsと公開後の実画面を改めて確認する。

文書だけの変更など実画面に影響しない作業では、画面画像が不要であることを短く報告し、差分確認を優先します。確認できなかった工程がある場合は省略せず、その理由を伝えます。

## 利用者との進め方

- 利用者は開発初心者であることを前提に、専門用語を短く説明します。
- 次に決めることや実行することは一度に1つだけ質問し、一問一答で進めます。
- 複数の候補を一度に大量提示せず、推奨する1項目から順に扱います。
- 会話が長くなった場合は新しいチャットへの移動を提案し、これまでの決定、現在の状態、次の1問を含む貼り付け用の短い引き継ぎ文を作ります。

## 文書の更新

- 作業内容、変更したファイル、確認結果、残っている問題、次回やることを、既存テンプレートに従って `DEVLOG.md` の日付別記録の先頭へ追記します。
- 未完了の作業、新しく判明した課題、後続確認は `TODO.md` に記録します。完了条件を満たしていない項目を完了扱いにしません。
- 現在の仕様や設計方針を変更した場合は `PROJECT.md` を更新します。
- 実行方法、公開方法、依存関係など利用者向け手順を変更した場合は `README.md` も確認します。
- 個別の依頼で変更可能なファイルが限定されている場合は、その指示を優先し、変更できなかった文書更新を終了報告で明示します。

## 作業範囲と外部操作

- コミット、Push、デプロイ、公開、実データの削除は、明確に依頼された場合だけ実行します。
- 依頼外のファイルは変更しません。自動生成ファイルが変わった場合も差分を確認し、不要な変更を残しません。
- 利用者の未コミット変更がある場合は保持し、必要な範囲だけを慎重に編集します。

## 終了時の報告

作業終了後は、日本語で次の内容を報告します。

- 実施した変更の概要
- 変更したファイル
- 実行したビルド、テスト、動作確認と結果
- 未確認の項目、残っている問題、`TODO.md` へ記録した内容

変更していない機能やデータ保護上の重要事項がある場合は、それも簡潔に明示します。
