# Baseball Note プロジェクト概要

## このアプリの目的

Baseball Note は、野球の練習や試合で得た感覚を、その日のうちに短いメモとして残すためのWebアプリです。打撃・守備・走塁・投球・体調・フィジカルといった観点で記録し、あとから日付やキーワード、タグで振り返れることを目的としています。

サーバーやユーザー登録を必要とせず、同じブラウザ上ですぐ使える個人向けの記録ツールとして構成されています。

## 現在の仕様

この章に書かれている内容は、現在のアプリですでに実装されている仕様です。まだ実装が決まっていない構想は、後半の「今後の方向性」に分けて記載します。

### 主な機能

- 日付ごとのメモ作成と時刻順の一覧表示
- 前日・翌日ボタン、日付入力、今日ボタンによる日付移動（未来日は選択不可）
- 日別一覧の本文をタップまたはキーボード操作して、その場で編集
- 本文またはタグを変更した場合だけ表示される、一覧下部の保存・キャンセル操作
- 本文を空にして保存した場合と、編集状態から削除する場合の確認ダイアログ付き削除
- 「打撃」「守備」「走塁」「投球」「体調」「フィジカル」の複数タグ付与
- タグを選ばずにメモを保存することも可能
- 全期間を対象とした本文・タグ・日付のキーワード検索
- 検索画面での複数タグによる絞り込み（いずれかのタグに一致）
- メニューの「振り返り」から期間選択カレンダーを直接開き、開始日と終了日を自由に選んで、両端の日付を含む保存済みメモを読み返せる振り返り画面
- 振り返り画面での月移動、選択範囲の色付き表示、未来日の選択防止、前回選択期間の端末内保存、メモから日別画面への移動
- 振り返り画面での「すべて」、既存6タグ、「その他（タグなし）」による期間内メモの絞り込み
- 選択中の期間・タグ・対象メモと分析用プロンプトを、外部AIへ貼り付ける文章としてクリップボードへコピーする「AI用にコピー」。選択期間の振り返り内容は維持したまま、保存されている全期間・全カテゴリーのメモも「長期判定用記録」として追加し、期間だけに偏らず「今、一番意識していること」を1つ判断するよう指定
- AIの回答では、固定形式の `【今、一番意識していること】内容` をコードブロックの1行目、その後に30秒程度で読める「まず見る要点」と、同じ内容の根拠を1〜9で説明する「詳しく読む振り返り」を出力するよう指定
- サイドメニューの「AI分析」から、外部AIで得た分析結果を期間・タグと一緒に保存し、新しい順の一覧と全文画面で読み返す機能
- AI分析保存画面では、振り返りで選択中の期間とタグを初期値として引き継ぎ、分析本文はMarkdown変換せずプレーンテキストのまま保存・表示
- 保存済みAI分析を新しい順に確認し、固定形式の「今、一番意識していること」が見つかった場合は、その内容を今日の日別画面上部へ表示。新しいAI分析に固定行がない場合は、それ以前の判定を維持
- 振り返り画面では日付見出しの下へ、通常一覧と同じ時刻・タグ／本文・画像の2列構成でメモを表示（画像は縦横比を維持したサムネイル表示）
- 1件のメモにつき画像1枚の添付と表示
- 添付画像の最大辺を1,400pxに縮小し、JPEG品質0.82で保存
- 全メモ、画像、AI分析をJSONファイルへ書き出すバックアップ
- 「データを保存」の実行前に、バックアップファイルの役割、保存件数、最後に保存した日時を説明する確認画面
- JSONバックアップの読み込み（読込前にメモとAI分析それぞれの新規・上書き件数を案内し、同じIDのデータは確認後に上書き）
- 「データを戻す」のファイル選択前に、追加・上書きの扱いと事前保存を説明する確認画面
- バックアップ読込時のバージョン、必須項目、日付、ID、画像Data URL、タグ、AI分析期間・本文・保存日時の検証
- 日別一覧で時刻とタグを左列、本文を右列にまとめ、通常時の編集・削除アイコンを表示しないコンパクト表示
- 初めて開いたときに、「タグを選ぶ」「感じたことを書く」「あとから振り返る」を1画面で説明する使い方案内。「始める」の後はタグと文章入力欄を初回限定の吹き出しで示し、「あとで見る」では吹き出しを出さず、どちらの場合も次回から自動表示しない
- 「振り返り」「AI分析」「検索」「データを保存」「データを戻す」と、少し間を空けた「使い方」をLucideの統一アイコンと文字で並べ、ホームベースとBを組み合わせたオリジナルSVGロゴを備えたPC向けサイドバーとスマートフォン向けドロワーメニュー
- メニューの「使い方」から初回と同じ案内を何度でも開き、「閉じる」で元の画面へ戻る機能
- 初回案内とメニューから開ける「安全とデータについて」。端末内保存、通信、外部AI、バックアップ、問い合わせ先を説明し、プライバシーポリシーと利用規約もアプリ内で表示
- 公開URLの `#safety`、`#privacy`、`#terms` から各説明ページを直接開ける構成
- アプリ名・短縮日付・前後移動を1行にまとめたスマートフォン向けヘッダーを備えたレスポンシブ表示
- 初回だけ本文の直接編集を案内し、閉じた状態をメモデータとは別のブラウザ内設定へ保存
- Web App Manifest、アイコン、Service WorkerによるPWA対応
- GitHub Actionsを使ったGitHub Pagesへの自動ビルド・公開

### 保存方法

- メモ、画像、AI分析は、Dexieを介してブラウザ内のIndexedDBに保存されます。
- データベース名は `baseballNote` です。通常メモは既存の `logs`、AI分析は独立した `aiAnalyses` テーブルで管理し、IndexedDBスキーマはバージョン2です。
- 各メモにはID、対象日、作成日時、本文、画像、タグが保存されます。
- 各AI分析にはID、開始日、終了日、1つのタグ、分析本文、保存日時が保存されます。通常メモの構造は変更していません。
- 「今、一番意識していること」はAI分析本文の固定行から読み取ります。専用テーブルや保存項目は追加していないため、既存AI分析とバックアップ形式はそのまま維持されます。
- 画像はファイルそのものではなく、縮小・JPEG変換したBlobとしてメモ内に保持されます。
- サーバー保存やアカウント同期はありません。同じURLでも、ブラウザ・端末・ブラウザプロファイルが変わると別データになります。
- 端末間の移行やデータ保全には、アプリのバックアップ書き出し・読み込みを使用します。新しい書き出し形式はバージョン2のJSONで、画像はData URL、AI分析は `aiAnalyses` 配列として含まれます。
- バージョン1と2のバックアップを読み込めます。バージョン1でAI分析データが省略されている場合は空配列として扱い、既存のAI分析は削除しません。省略されたメモ画像とタグを空配列として補完する従来の互換処理も維持します。
- 未対応バージョンや不正なバックアップは理由を画面に表示して停止し、既存データへ書き込みません。
- 「振り返り」で最後に確定した開始日と終了日は、`baseball-note-record-review-range` をキーとして `localStorage` に保存します。この画面設定はメモデータやJSONバックアップには含まれません。
- 初回の使い方案内を閉じた状態は、`baseball-note-onboarding-completed` をキーとして `localStorage` に保存します。この画面設定もメモデータやJSONバックアップには含まれません。
- 最後に「データを保存」を実行した日時は、`baseball-note-last-backup-at` をキーとして `localStorage` に保存します。この日時は端末ごとの案内用設定であり、メモデータやJSONバックアップには含まれません。

### 現在の構成

#### 技術構成

- React 18 + TypeScript
- Vite 6
- Dexie 4（IndexedDB操作）
- Lucide React（サイドメニューアイコン）
- CSSによるレスポンシブUI
- Service Worker + Web App Manifest
- GitHub Actions + GitHub Pages

#### 主なファイル

```text
remote-app/
├── .github/workflows/deploy.yml  # GitHub Pagesのビルド・公開
├── public/
│   ├── manifest.webmanifest      # PWA設定
│   ├── baseball-note-logo.svg    # サイドメニューのオリジナルロゴ
│   ├── sw.js                     # キャッシュとオフライン表示
│   └── icon.*                    # アプリアイコン
├── src/
│   ├── App.tsx                   # 画面、状態、CRUD、検索、振り返り、AI分析、画像、バックアップ
│   ├── InformationScreen.tsx     # 安全とデータ、プライバシーポリシー、利用規約
│   ├── RecordReviewCalendar.tsx  # 開始日・終了日を選ぶ期間選択カレンダー
│   ├── date.ts                   # 日付キー・表示・日付／月移動・カレンダー・時刻整形
│   ├── db.ts                     # Dexieデータベース定義
│   ├── main.tsx                  # React起動とService Worker登録
│   ├── styles.css                # PC・スマートフォン向けスタイル
│   └── types.ts                  # メモ、画像、タグ、AI分析の型定義
├── index.html                    # HTMLエントリーポイント
├── package.json                  # 依存関係と開発・ビルドコマンド
├── PROJECT.md                    # 現在の仕様、設計方針、将来候補
├── DEVLOG.md                     # 完成機能と日付別の作業記録
├── TODO.md                       # 優先順位付きの作業項目と将来アイデア
├── RULES.md                      # AIを含む開発作業のルール
├── tsconfig.json                 # TypeScript設定
└── vite.config.js                # Vite設定
```

現在はフロントエンドだけで完結する単一画面アプリです。主要な画面処理は `src/App.tsx` に集約されています。

## 設計方針

今後の変更でも、次の方針を優先します。

- 中学生でも説明なしで迷わず使える、分かりやすい操作にする
- スマートフォンでの利用を優先し、PCでも使いやすい表示を保つ
- 記録までに必要な入力やタップをできるだけ少なくする
- 現在のシンプルなUIと落ち着いたデザインを維持する
- 通常画面ではメモ本文を主役にし、タグや検索は補助機能として扱う
- 入力時のタグは記録の整理と、外部AIへ渡す振り返り記録に利用できる形で維持する
- 普段の記録操作を増やさず、期間を選んでAI用にコピーする既存の振り返り操作から、全期間で最も多く長く意識していることを確認できるようにする
- Baseball Note自体にはAIや外部APIを組み込まず、記録と振り返りを主役にして、必要な分析は利用者が外部AIへコピーして行い、得た分析結果だけを端末内へ保存する
- 現段階では、共有サービスよりも個人利用での使いやすさを優先する
- 記録したデータを失わないことを重要視し、保存形式の互換性とバックアップ手段を守る

## 今後の方向性

以下は、長期的に検討する「将来候補」です。現時点では実装決定済みの仕様ではなく、通常の開発TODOとは分けて扱います。実装する場合は、目的、利用場面、データの保存方法、使いやすさへの影響を確認してからTODOへ移します。

- **音声入力（将来候補）**: 練習直後でも、文字入力を減らして素早く記録できるようにする
- **まとめノート（将来候補）**: 振り返りや外部AIで得た内容を、元記録との関係を保ちながら残せる方法を検討する
- **選手の成長記録（将来候補）**: メモを時系列で整理し、変化や継続している課題を振り返れるようにする
- **画像や動画を使った記録の拡張（将来候補）**: 現在の画像1枚の添付に加え、複数画像、動画、比較表示などを検討する
- **複数端末利用（将来候補）**: スマートフォンやPCの間で安全に同じ記録を利用できる同期方法を検討する
